# idl1 M0 — Foundations Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Stand up the idl1-app repository, retire the video-export and FRB-bridge code from idl-rs on a tagged, reversible line, prove binary IPC through a Tauri v2 + React scaffold, and write the four contracts (C1–C4) that wave-1 lanes build against.

**Architecture:** idl1-app is a new repo with `app/` (Tauri v2, React/TS/Vite), `docs/`, and the shared `rust/` submodule (idl-rs). On idl-rs `main` the FRB bridge and video/overlay painters are deleted (tagged first so idl0-app keeps building at its pin) and two crates are added: `idl-transport` (I/O, stub) and `idl-rs-tauri` (commands; first binary `ipc::Response`). Contracts are Markdown specs drafted by implementers from the design doc and finalised by the lead.

**Tech Stack:** Rust 2021 (cargo workspace), Tauri v2, React + TypeScript + Vite, vitest, git submodules. Versions are pinned from the ecosystem report produced in Task 1.

**Spec:** `docs/superpowers/specs/2026-09-02-idl1-rewrite-design.md` (design), `docs/superpowers/specs/2026-09-02-idl1-inventory.md` (carry-forward inventory). Both are copied into idl1-app in Task 3; executors read both.

## Global Constraints

- New repo path: `C:\Users\isaac\Documents\Saucy\saucyeng\idl1-app`. idl0-app path: `C:\Users\isaac\Documents\Saucy\saucyeng\idl0-app`. The idl-rs submodule is checked out at `<repo>/rust` in both; remote `https://github.com/saucyeng/idl-rs.git`.
- **No AI attribution trailers** in any commit (`Co-Authored-By`, `Claude-Session`, etc.). Plain commit messages.
- **Never `git push`.** Every push is Isaac's step; tasks stop at local commits and tags.
- **idl0-app pins idl-rs at `1e55bace2faa62949da0d74aad781224af64ea79` = tag `idl0-final` and never bumps again.** idl1 work is on idl-rs `main` after that commit. Fixes idl0 needs go to branch `idl0-maint`.
- **`idl-rs` core stays pure**: no Tauri, no async runtime, no I/O beyond `std::fs`. Transport and Tauri glue live in their own crates.
- **All JavaScript is bundled.** No CDN `<script>` or stylesheet, ever (design §3 "offline-first means bundled").
- **idl-rs is not rustfmt-formatted.** Do not run `cargo fmt`; match surrounding style by hand.
- Doc comments on every public symbol, units on every numeric value, `// TODO(idl0):` never bare `// TODO` (idl1 keeps the `idl0` tag for grep continuity).
- Every version string in Tasks 4–5 is copied from the `pin` column of `docs/superpowers/specs/2026-09-02-idl1-m0-ecosystem.md` (Task 1). Do not guess versions.
- Working directory is stated per task. Use absolute paths in commands; never rely on a `cd` persisting.

---

## File Structure

**idl-rs (submodule, on `main` after Task 2):**
- Modify: `Cargo.toml` — members become `["core", "cli", "transport", "tauri"]`; profile entries for `idl_rs_bridge` and `idl-rs-video-export` removed.
- Delete: `bridge/`, `video-export/`, `core/src/overlay/`, `core/src/video/`.
- Modify: `core/Cargo.toml` (drop `tiny-skia`, `fontdue`), `core/src/lib.rs` (drop two `pub mod`, add `VERSION`), `core/src/workbook/model.rs` (drop `overlay_layouts` + `overlay_layout()`), `cli/Cargo.toml` (drop video-export dep), `cli/src/main.rs` (drop `Video`/`Overlay` commands and handlers).
- Create: `transport/Cargo.toml`, `transport/src/lib.rs`, `transport/src/error.rs` — crate `idl-transport`, typed error only.
- Create: `tauri/Cargo.toml`, `tauri/src/lib.rs`, `tauri/src/commands.rs` — crate `idl-rs-tauri`, two commands.

**idl1-app (new repo):**
- Create: `README.md`, `CLAUDE.md`, `TASKS.md`, `CHANGELOG.md`, `.gitignore`, `LICENSE`, `CLA.md`, `CONTRIBUTING.md` (copied), `docs/**` (copied + this plan), `tools/**` (copied), `rust` (submodule).
- Create: `app/` via create-tauri-app; then `app/src/ipc/tiles.ts`, `app/src/ipc/tiles.test.ts`, `app/src/App.tsx` (replaced), `app/src-tauri/src/lib.rs` (modified), `app/src-tauri/Cargo.toml` (modified), `app/src-tauri/tauri.conf.json` (modified), `app/vitest.config.ts`.
- Create: `docs/superpowers/specs/2026-09-0N-idl1-c1-session-schema.md`, `…-c2-workbook-v3.md`, `…-c3-ipc-surface.md`, `…-c4-data-directory.md`.

---

### Task 1: Ecosystem verification report

Research only; no code. Dispatch as a Sonnet subagent with web access (`subagent_type: "general-purpose"`, `model: "sonnet"`). The report is the single source for every version pin in later tasks.

**Files:**
- Create: `C:\Users\isaac\Documents\Saucy\saucyeng\idl0-app\docs\superpowers\specs\2026-09-02-idl1-m0-ecosystem.md`

**Interfaces:**
- Produces: a Markdown file with exactly two sections, consumed by Tasks 4, 5 and the wave-1 lane briefs.

- [ ] **Step 1: Dispatch the research brief**

Brief text (paste verbatim):

```
Produce C:\Users\isaac\Documents\Saucy\saucyeng\idl0-app\docs\superpowers\specs\2026-09-02-idl1-m0-ecosystem.md
with exactly these two sections. Use web search; cite a URL for every row and finding.
Today is 2026-09-02. "Latest stable" means the newest non-prerelease release on crates.io / npm.

## 1. Versions
A Markdown table with columns: name | registry | latest stable | released | license | pin | source URL.
`pin` is the exact string to paste into Cargo.toml / package.json (crates: caret-free exact
"x.y.z"; npm: "^x.y.z"). Rows, in this order:
tauri, tauri-build, tauri-plugin-shell, @tauri-apps/cli, @tauri-apps/api, create-tauri-app,
btleplug, mdns-sd, axum, tokio, arrow, parquet, rusqlite, fitparser, pulldown-cmark, notify,
serde, serde_json, thiserror, @observablehq/runtime, @observablehq/plot, @observablehq/inputs,
d3, codemirror, @codemirror/lang-javascript, @codemirror/lang-markdown, react, react-dom,
vite, @vitejs/plugin-react, vitest, typescript.

## 2. Findings
Five numbered findings, each: a one-line yes/no/partial verdict, then 2–5 lines of evidence
with URLs. Do not speculate; if you cannot find evidence, say "unverified".
1. Tauri v2 mobile (Android and iOS): does `tauri::ipc::Response` (raw bytes) reach the
   webview as an ArrayBuffer without base64 or JSON, same as desktop?
2. In a sandboxed <iframe sandbox="allow-scripts"> (no allow-same-origin) inside WKWebView
   (iOS) and Android System WebView: does postMessage with a transferable ArrayBuffer work?
3. btleplug: platform support matrix (Windows, macOS, Linux, Android, iOS) and the state of
   its Android/iOS support.
4. Tauri v2 mobile plugins for BLE: which exist, their maintenance state, and whether they
   cover scan + connect + GATT read/write/notify on both Android and iOS.
5. The `parquet` crate: does its reader support column projection, row-group statistics
   filtering (predicate pushdown on min/max), and DELTA_BINARY_PACKED for INT64 columns?
Return only: "written" plus the count of rows with a pin and the count of findings marked
unverified.
```

- [ ] **Step 2: Verify the report shape**

Run (Git Bash): `grep -c "^| " "C:/Users/isaac/Documents/Saucy/saucyeng/idl0-app/docs/superpowers/specs/2026-09-02-idl1-m0-ecosystem.md"`
Expected: at least 34 (32 rows + header + separator).

Run: `grep -n "unverified" "C:/Users/isaac/Documents/Saucy/saucyeng/idl0-app/docs/superpowers/specs/2026-09-02-idl1-m0-ecosystem.md"`
Expected: zero lines, or each listed line is a finding the lead has read and accepted (a finding marked unverified for items 1, 2 or 5 blocks Task 5; report it, do not proceed silently).

- [ ] **Step 3: Commit (idl0-app)**

Working directory: `C:\Users\isaac\Documents\Saucy\saucyeng\idl0-app`
```bash
git add docs/superpowers/specs/2026-09-02-idl1-m0-ecosystem.md
git commit -m "docs: idl1 M0 ecosystem verification report"
```

---

### Task 2: idl-rs — pin, tag, delete D9, add `idl-transport`

Working directory: `C:\Users\isaac\Documents\Saucy\saucyeng\idl0-app` for steps 1–2 (idl0-app commits), `C:\Users\isaac\Documents\Saucy\saucyeng\idl0-app\rust` for the rest (the submodule checkout, branch `main`, currently at `1e55bac`, clean, in sync with `origin/main`).

**Files:**
- Modify (idl0-app): `rust` submodule pointer; `docs/design_rationale.md` (append); `TASKS.md`; `CHANGELOG.md`.
- Modify (idl-rs): `Cargo.toml`, `core/Cargo.toml`, `core/src/lib.rs`, `core/src/workbook/model.rs`, `cli/Cargo.toml`, `cli/src/main.rs`.
- Delete (idl-rs): `bridge/`, `video-export/`, `core/src/overlay/`, `core/src/video/`.
- Create (idl-rs): `transport/Cargo.toml`, `transport/src/lib.rs`, `transport/src/error.rs`.

**Interfaces:**
- Produces: `idl_rs::VERSION: &str` (Task 5 uses it); crate `idl-transport` with `idl_transport::TransportError { kind: TransportErrorKind, message: String }` and `TransportErrorKind::{Ble, Wifi, Config, Sync}`; workspace members `["core", "cli", "transport"]` (Task 5 adds `"tauri"`).

- [ ] **Step 1: Pin idl0-app at the final idl-rs commit and record the pivot**

The working tree already has the submodule at `1e55bac` (one ahead of the committed pin `b814995`); this commits that bump as idl0's last engine pin.

Append to `docs/design_rationale.md`:
```markdown

## idl0-app ends; idl1-app begins (2026-09-02)

idl0-app is retired in favour of a new repository, idl1-app: Rust backend in
Tauri v2, Observable Plot/D3 notebook cells, Parquet canonical store, LAN sync.
The reasoning, decisions and decomposition are in
`docs/superpowers/specs/2026-09-02-idl1-rewrite-design.md`; this entry only
records that the turn happened and what it does to this repo. The engine
submodule is pinned at `1e55bac` (idl-rs tag `idl0-final`) and is never bumped
again; engine fixes idl0 still needs come from the idl-rs `idl0-maint` branch.
The composition roadmap (2026-07-29) is superseded: its on-screen tiny-skia
migration is abandoned, its insights (one vocabulary, tile decimation, hover
from per-column stats, translation-not-redraw) carry into idl1.
```

In `TASKS.md`, directly under the line `### Composition roadmap (design_rationale 2026-07-29)`, insert:
```markdown

> **Superseded 2026-09-02.** idl0-app is being replaced by idl1-app; see
> `docs/superpowers/specs/2026-09-02-idl1-rewrite-design.md`. Sub-projects 1–7
> below are not being worked. Only bug fixes land in this repo from here on.
```

In `CHANGELOG.md`, directly under `## [Unreleased]`, insert:
```markdown

### Changed

- **idl0-app is feature-frozen (2026-09-02).** The app is being succeeded by
  idl1-app (design: `docs/superpowers/specs/2026-09-02-idl1-rewrite-design.md`).
  The idl-rs submodule is pinned at `1e55bac` (`idl0-final`) for good.
```

```bash
git add rust docs/design_rationale.md TASKS.md CHANGELOG.md
git commit -m "app: pin idl-rs at idl0-final; record the idl1 pivot"
```

- [ ] **Step 2: Verify the pin**

Run: `git ls-tree HEAD rust`
Expected: `160000 commit 1e55bace2faa62949da0d74aad781224af64ea79	rust`

- [ ] **Step 3: Tag and branch idl-rs (no push)**

Working directory: `C:\Users\isaac\Documents\Saucy\saucyeng\idl0-app\rust`
```bash
git tag -a idl0-final 1e55bac -m "Last idl-rs commit consumed by idl0-app. idl0-app pins here forever."
git tag -a idl0-video-export 1e55bac -m "Last commit containing the tiny-skia overlay painters, video/ (gpmf, mp4box, sync, render), video-export/ and the CLI overlay command. Restore from here when reels return."
git branch idl0-maint 1e55bac
git tag -n1 idl0-final idl0-video-export
```
Expected: both tags listed with their messages.

- [ ] **Step 4: Delete the D9 trees and the bridge**

```bash
git rm -r -q bridge video-export core/src/overlay core/src/video
ls bridge video-export core/src/overlay core/src/video 2>&1 | grep -c "No such file"
```
Expected: `4`.

- [ ] **Step 5: Edit the workspace manifest**

In `Cargo.toml`, change `members = ["core", "bridge", "cli", "video-export"]` to:
```toml
members = ["core", "cli", "transport"]
```
Delete these two blocks entirely:
```toml
[profile.dev.package.idl_rs_bridge]
opt-level = 3

[profile.dev.package.idl-rs-video-export]
opt-level = 3
```
In the comment above `[profile.dev]`, replace the sentence `Dev (flutter run) builds: cargokit maps Flutter debug → cargo dev profile.` with `Dev builds (tauri dev) use the cargo dev profile.` and `The cdylib Flutter loads needs no Rust symbol info.` with `The library the app loads needs no Rust symbol info.`

- [ ] **Step 6: Edit core**

`core/Cargo.toml`: delete the lines `tiny-skia = "0.11"` and `fontdue = "0.9"`.

`core/src/lib.rs`: delete line `pub mod overlay;` and line `pub mod video;`. Immediately after the last `pub mod` line (`pub mod workbook;`), add:
```rust

/// The engine's crate version, stamped into derived-file hashes and reported
/// over IPC so every device can prove it computes with the same engine.
pub const VERSION: &str = env!("CARGO_PKG_VERSION");
```

`core/src/workbook/model.rs`:
- delete line 8: `use crate::overlay::model::OverlayLayout;`
- delete the doc line 12 fragment referring to `overlay_layouts` (SPEC §33.1) so the struct doc no longer mentions it;
- delete the field and its doc comment (lines 38–40): the `/// Overlay layouts (SPEC §33.1); empty when absent (v1 files).` line, the `#[serde(default)]` attribute line directly above the field if present, and `pub overlay_layouts: Vec<OverlayLayout>,`;
- delete the whole `pub fn overlay_layout(&self, name: Option<&str>) -> Result<&OverlayLayout, String>` method (starts at the doc comment on line 124, ends at the method's closing brace).
Leave `overlay_target_id` on blocks untouched — it is worksheet placement, not the overlay module.

- [ ] **Step 7: Edit the CLI**

`cli/Cargo.toml`: delete `idl-rs-video-export = { path = "../video-export" }`.

`cli/src/main.rs`:
- delete the imports at lines 31, 36, 37 and 41 (`idl_rs::overlay::sample::SampleContext`, `idl_rs::video::render::render_overlay_frame`, `idl_rs::video::{gpmf, mp4box, sync::estimate_sync, VideoError, VideoErrorKind}`, `idl_rs_video_export::{...}`);
- in `enum Command`, delete the `Video(VideoCmd)` variant and the whole `Overlay { ... }` variant with their doc comments;
- delete `enum VideoCmd` and its doc comment (`/// \`video\` subcommands (SPEC 33.6).`);
- in the `match` over `Command`, delete the arms for `Command::Video(VideoCmd::Probe {..})`, `Command::Video(VideoCmd::Sync {..})` and `Command::Overlay {..}`;
- delete `fn cmd_overlay(` (line 1225) through its closing brace, `fn cmd_video_probe(` (line 1375) through its closing brace, and `fn cmd_video_sync(` (line 1409) through its closing brace;
- delete any now-unused `use` lines the compiler reports (Step 9 will list them).

`cli/src/table_cmd.rs` mentions `overlay` only via block `overlay_target_id` — leave it.

- [ ] **Step 8: Create the `idl-transport` stub**

`transport/Cargo.toml`:
```toml
[package]
name = "idl-transport"
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0-or-later"
description = "Device transport (BLE, WiFi transfer, config push) and LAN sync for idl1. I/O only; no DSP."

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
```

`transport/src/lib.rs`:
```rust
//! Device transport and LAN sync for idl1.
//!
//! This crate owns every byte that leaves or enters the machine: BLE to the
//! logger, WiFi transfer against the device's HTTP protocol (SPEC §6), config
//! push (SPEC §8), and peer sync on the pit-lane LAN. It never processes
//! signals — that is `idl-rs` — and `idl-rs` never depends on this crate.
//!
//! M0 ships only the typed error; lanes L4 and L11 fill it in.

pub mod error;

pub use error::{TransportError, TransportErrorKind};
```

`transport/src/error.rs`:
```rust
//! Typed transport failures. Every fallible transport operation returns
//! [`TransportError`]; free-form `Err(String)` is not allowed (CLAUDE.md §5).

use std::fmt;

/// Which subsystem failed. Coarse on purpose: the UI routes on this, and the
/// message carries the detail.
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "snake_case")]
pub enum TransportErrorKind {
    /// BLE scan, connect, GATT or transfer failure.
    Ble,
    /// WiFi network binding or HTTP transfer failure against the device.
    Wifi,
    /// Config push rejected or malformed (SPEC §8).
    Config,
    /// LAN peer sync failure (pairing, manifest, blob transfer).
    Sync,
}

/// A transport failure: a kind the UI can route on plus a human-readable
/// message. Serialisable so it crosses IPC unchanged (contract C3).
#[derive(Debug, Clone, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct TransportError {
    /// Subsystem that failed.
    pub kind: TransportErrorKind,
    /// What went wrong, for the user. No stack traces.
    pub message: String,
}

impl TransportError {
    /// Builds an error of `kind` with `message`.
    pub fn new(kind: TransportErrorKind, message: impl Into<String>) -> Self {
        Self { kind, message: message.into() }
    }
}

impl fmt::Display for TransportError {
    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
        write!(f, "{:?}: {}", self.kind, self.message)
    }
}

impl std::error::Error for TransportError {}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn transport_error_display_carries_kind_and_message() {
        // Arrange
        let err = TransportError::new(TransportErrorKind::Ble, "device not found");

        // Act
        let text = err.to_string();

        // Assert
        assert_eq!(text, "Ble: device not found");
    }

    #[test]
    fn transport_error_serialises_kind_as_snake_case() {
        // Arrange
        let err = TransportError::new(TransportErrorKind::Wifi, "timeout");

        // Act
        let json = serde_json::to_string(&err).unwrap();

        // Assert
        assert_eq!(json, r#"{"kind":"wifi","message":"timeout"}"#);
    }
}
```
Add to `transport/Cargo.toml` under `[dev-dependencies]`: `serde_json = "1"`.

- [ ] **Step 9: Build and test the whole workspace**

Run: `cargo build --workspace 2>&1 | tail -20`
Expected: `Finished`. If the CLI reports unused imports from Step 7, delete exactly those lines and rebuild.

Run: `cargo test --workspace 2>&1 | grep -E "^test result|FAILED|error" `
Expected: every `test result:` line shows `0 failed`; no `error` lines. The two new transport tests pass.

- [ ] **Step 10: Commit on idl-rs `main` (no push)**

```bash
git add -A
git commit -m "engine: retire the video overlay, video-export and FRB bridge; add idl-transport stub

idl1 line. The deleted trees are preserved at tag idl0-video-export; idl0-app
pins idl0-final and never sees this commit. VERSION is exported for IPC and
derived-file hashing."
git log --oneline -1
```

---

### Task 3: Create the idl1-app repository

Working directory: `C:\Users\isaac\Documents\Saucy\saucyeng` for step 1, then `C:\Users\isaac\Documents\Saucy\saucyeng\idl1-app`.

**Files:**
- Create: everything listed under "idl1-app (new repo)" in File Structure except `app/`, the contracts, and the IPC files.

**Interfaces:**
- Produces: the repo, with `rust/` submodule at the Task 2 commit on `main`; `CLAUDE.md` standing orders every later task and lane obeys.

- [ ] **Step 1: Initialise and copy**

```bash
mkdir -p "C:/Users/isaac/Documents/Saucy/saucyeng/idl1-app"
cd "C:/Users/isaac/Documents/Saucy/saucyeng/idl1-app" && git init -b main
SRC="C:/Users/isaac/Documents/Saucy/saucyeng/idl0-app"
cp "$SRC/LICENSE" "$SRC/CLA.md" "$SRC/CONTRIBUTING.md" .
mkdir -p docs/legacy docs/superpowers/specs docs/superpowers/plans tools
cp "$SRC/docs/IDL0_SPEC.md" "$SRC/docs/design_rationale.md" "$SRC/docs/signal_pipeline.md" "$SRC/docs/prompt_template.md" docs/
cp -r "$SRC/docs/datasheet" docs/datasheet
cp "$SRC/docs/workbook_format.md" docs/legacy/idl0-workbook_format.md
cp "$SRC"/docs/superpowers/specs/2026-09-02-idl1-*.md docs/superpowers/specs/
cp "$SRC/docs/superpowers/plans/2026-09-02-idl1-m0.md" docs/superpowers/plans/
cp -r "$SRC/tools/estimator_sim" tools/estimator_sim
cp "$SRC/tools/imu_budget_model.py" "$SRC/tools/imu_drop_analysis.py" "$SRC/tools/README.md" tools/
ls docs docs/superpowers/specs
```
Expected: `docs/` lists `IDL0_SPEC.md design_rationale.md signal_pipeline.md prompt_template.md datasheet legacy superpowers`; specs lists the design, inventory, ecosystem report and (after Task 1) nothing else yet. `tools/idl0_dump.dart` is deliberately not copied (Dart).

- [ ] **Step 2: Write `.gitignore`**

```gitignore
# Rust
target/
# Node
node_modules/
dist/
# Tauri
app/src-tauri/gen/schemas/
# OS / editors
.DS_Store
Thumbs.db
.vscode/
.idea/
# Local agent state
.superpowers/
```

- [ ] **Step 3: Write `README.md`**

```markdown
# idl1

Trackside race-engineering analysis for the IDL0 data logger — and for a decade
of FIT/GPX files from anything else. Rust engine (`idl-rs`), Tauri v2 shell,
Observable Plot/D3 notebook cells, Parquet canonical store, pit-lane LAN sync.

**Status:** M0 (foundations). Not yet usable. idl0-app (Flutter) remains the
shipping app until M2.

- Design: `docs/superpowers/specs/2026-09-02-idl1-rewrite-design.md`
- Standing orders for contributors and agents: `CLAUDE.md`
- Device, wire and firmware contract: `docs/IDL0_SPEC.md` Parts 1–2 and §3–10
  (app-side parts of that document describe idl0 and are being rewritten lane by lane)
- Work queue: `TASKS.md`

## Build

Requires a Rust toolchain, Node.js, and the Tauri v2 prerequisites for your
platform (see the Tauri docs). Then:

    git submodule update --init
    cd app && npm install && npm run tauri dev

## Licence

AGPL-3.0-or-later. See `LICENSE` and `CLA.md`.
```

- [ ] **Step 4: Write `CLAUDE.md`**

```markdown
# CLAUDE.md — idl1 Standing Orders

Read this file and `docs/superpowers/specs/2026-09-02-idl1-rewrite-design.md`
before any task. The design doc is the architecture; `docs/IDL0_SPEC.md` is the
device/wire contract (Parts 1–2, §3–10 are current; the app-side parts describe
idl0 and are rewritten lane by lane — a lane's SPEC section, once written, wins).

## 1. Ambiguity policy

A wrong assumption costs more than a question. If a field type, an edge-case
behaviour, a layer placement or a signature is not stated in the design doc, a
contract (C1–C4), or the lane's SPEC section — stop and ask. Do not infer, do
not pick "the reasonable one", do not treat silence as approval.

## 2. Layers

    rust/core/       idl-rs        PURE. Signal processing, parsing, importers, store
                                   (Parquet/CAS/catalog), workbook evaluation. No Tauri,
                                   no async runtime, no network. std::fs only.
    rust/transport/  idl-transport I/O: BLE, WiFi transfer, config push, LAN sync.
                                   Never depends on Tauri. Never does DSP.
    rust/tauri/      idl-rs-tauri  #[tauri::command] glue over core + transport. The only
                                   crate the frontend sees. Thin.
    rust/cli/        idl-rs-cli    The idl-rs binary. Depends on core (and transport when needed).
    app/src-tauri/                 Tauri app crate: builder, plugin registration, mobile plugins.
    app/src/                       TypeScript UI. Talks only to idl-rs-tauri commands.

**Rust = numbers, JS = pictures.** No number the sync model depends on is
computed in JavaScript; no chart is drawn in Rust. Heavy arrays cross IPC as raw
bytes (`tauri::ipc::Response`), never JSON. No IPC on the interaction path
(hover/pan/zoom are local; IPC on gesture settle).

**Decision rule:** physics of the bike or bytes on disk → `core`. Bytes on the
wire → `transport`. Pixels or clicks → `app/src`.

## 3. Principles (quote these in reviews)

- The workbook is a file; the app is a live viewer of it.
- Sync sources; sync outputs only when content-addressed by their inputs.
- The catalog is an index — deletable, rebuildable, never synced.
- Time is recorded, not assumed: every sample keeps its hardware timestamp.
- Log files and blobs are immutable. `data.parquet` is a function of (blob, importer version).
- Offline-first means bundled: no CDN, ever.
- No renderer-only parameters.

## 4. Testing

Arrange / Act / Assert with blank lines between. Names: `thing — condition — result`.
Rust tests inline `#[cfg(test)]`; TypeScript tests beside the module as `*.test.ts` (vitest).
Test what we own: wiring, physics, round-trips, merge rules. Not sci-rs, nalgebra,
Tauri, Plot. Coverage: Rust > 90 % (`cargo tarpaulin`); pure TS modules > 80 %;
UI rendering is not unit-tested.

## 5. Documentation and errors

Doc comment on every public symbol; units on every numeric value; `// TODO(idl0):`
never bare `// TODO`. All failures are typed (`TransportError`, core error enums,
the C3 IPC error shape) — never `Err(String)`, never a crash on bad data.

## 6. Spec discipline

Every task declares one of: spec-first (write the SPEC section before code),
spec-during (SPEC section in the same PR), or "no spec change needed" — said out
loud. Every task touching shipped behaviour updates `CHANGELOG.md` and/or `TASKS.md`.

## 7. Repo hygiene

- idl-rs is not rustfmt-formatted; never run `cargo fmt`. Match style by hand.
- No AI attribution trailers in commits. Never push; Isaac pushes.
- Lanes touch only their own crate/directory. Cross-lane needs are contract changes, through the lead.
- After changing an `idl-rs-tauri` command signature, update `app/src/ipc/` and the C3 contract together.
```

- [ ] **Step 5: Write `TASKS.md` and `CHANGELOG.md`**

`TASKS.md`:
```markdown
# idl1 Build Tasks

Read `CLAUDE.md` and the design doc before starting any task. Lanes and waves
are defined in `docs/superpowers/specs/2026-09-02-idl1-rewrite-design.md` §10.

## M0 — foundations (`docs/superpowers/plans/2026-09-02-idl1-m0.md`)

- [x] Task 1 ecosystem report
- [x] Task 2 idl-rs pin/tag/delete + idl-transport stub
- [x] Task 3 repository created
- [ ] Task 4 Tauri scaffold
- [ ] Task 5 idl-rs-tauri + binary IPC smoke
- [ ] Task 6 contract C1 session schema
- [ ] Task 7 contract C2 workbook v3
- [ ] Task 8 contract C3 IPC surface
- [ ] Task 9 contract C4 data directory
- [ ] Task 10 M0 exit

## Wave 1 (after M0)

- [ ] L1 core `store/`
- [ ] L2 importers
- [ ] L3 core workbook v3
- [ ] L4 `idl-transport` desktop
- [ ] L5 Tauri scaffold hardening
- [ ] L10 docs (runs alongside)

## Wave 2

- [ ] L6 notebook UI · L7 device/data/settings UI · L9 mobile scaffold · L11 LAN sync

## Wave 3

- [ ] L9 mobile plugins · L12 in-app agent (optional)
```

`CHANGELOG.md`:
```markdown
# Changelog

All notable changes to idl1 are recorded here. Format: Semantic Versioning.

## [Unreleased]

### Added

- **Repository created (2026-09-02).** From the idl1 rewrite design
  (`docs/superpowers/specs/2026-09-02-idl1-rewrite-design.md`). Docs carried
  over from idl0-app: SPEC, design rationale, signal pipeline, datasheet,
  tools. The idl-rs engine is the same submodule idl0-app used, continued on
  `main` after tag `idl0-final`.
```

- [ ] **Step 6: Add the submodule at the Task 2 commit**

```bash
cd "C:/Users/isaac/Documents/Saucy/saucyeng/idl1-app"
git submodule add https://github.com/saucyeng/idl-rs.git rust
```
The clone will not contain the local (unpushed) Task 2 commit. Point it at the local checkout's `main` instead:
```bash
git -C rust remote add local "C:/Users/isaac/Documents/Saucy/saucyeng/idl0-app/rust"
git -C rust fetch local main
git -C rust checkout -B main FETCH_HEAD
git -C rust log --oneline -1
```
Expected: the "engine: retire the video overlay…" commit from Task 2.

Then verify the engine builds from the new location:
```bash
cd "C:/Users/isaac/Documents/Saucy/saucyeng/idl1-app/rust" && cargo test -p idl-rs -p idl-transport 2>&1 | grep -E "^test result"
```
Expected: `0 failed` on every line.

- [ ] **Step 7: Commit the repository**

```bash
cd "C:/Users/isaac/Documents/Saucy/saucyeng/idl1-app"
git add -A
git commit -m "repo: idl1-app created from the 2026-09-02 rewrite design

Docs, licence and tools carried from idl0-app; idl-rs submodule on main after
idl0-final; standing orders in CLAUDE.md."
git submodule status
```
Expected: the submodule line shows the Task 2 commit hash with no `+` or `-` prefix.

---

### Task 4: Tauri v2 + React scaffold

Working directory: `C:\Users\isaac\Documents\Saucy\saucyeng\idl1-app`.

**Files:**
- Create: `app/` (template), then modify `app/src-tauri/tauri.conf.json`, `app/package.json`, `app/vitest.config.ts`, `app/src/App.tsx`, `app/src/App.css`.

**Interfaces:**
- Produces: `npm run tauri dev` boots a window; `npm test` runs vitest; `app/src-tauri` is a cargo crate Task 5 wires commands into.

- [ ] **Step 1: Check the scaffolder's flags**

Run: `npm create tauri-app@latest -- --help`
Expected: usage lists `--template`, `--manager`, `--identifier` (names may be `-t`, `-m`, `--identifier`). If any of the three is missing, use the interactive prompts with the same values instead of flags.

- [ ] **Step 2: Scaffold**

```bash
cd "C:/Users/isaac/Documents/Saucy/saucyeng/idl1-app"
npm create tauri-app@latest app -- --template react-ts --manager npm --identifier com.saucyeng.idl1 --yes
ls app app/src app/src-tauri
```
Expected: `app/package.json`, `app/src/App.tsx`, `app/src-tauri/Cargo.toml`, `app/src-tauri/tauri.conf.json` exist.

- [ ] **Step 3: Pin versions from the report and install**

In `app/package.json`, set every dependency that appears in the ecosystem report's Versions table to that row's `pin` value (`@tauri-apps/api`, `@tauri-apps/cli`, `react`, `react-dom`, `vite`, `@vitejs/plugin-react`, `typescript`). Add under `devDependencies`: `"vitest": "<pin>"` and under `scripts`: `"test": "vitest run"`.

In `app/src-tauri/Cargo.toml`, set `tauri` and `tauri-build` to their `pin` values.

In `app/src-tauri/tauri.conf.json`, set `"productName": "idl1"`, `"identifier": "com.saucyeng.idl1"`, and the main window `"title": "idl1"`.

```bash
cd "C:/Users/isaac/Documents/Saucy/saucyeng/idl1-app/app" && npm install
```
Expected: no `ERESOLVE` errors.

- [ ] **Step 4: Add vitest config and a smoke test**

`app/vitest.config.ts`:
```ts
import { defineConfig } from "vitest/config";

export default defineConfig({
  test: {
    include: ["src/**/*.test.ts"],
    environment: "node",
  },
});
```

`app/src/smoke.test.ts`:
```ts
import { describe, expect, it } from "vitest";

describe("vitest wiring", () => {
  it("runs — trivially true — passes", () => {
    // Arrange
    const a = 1;

    // Act
    const b = a + 1;

    // Assert
    expect(b).toBe(2);
  });
});
```

Run: `npm test`
Expected: `1 passed`.

- [ ] **Step 5: Replace the template UI**

`app/src/App.tsx`:
```tsx
import "./App.css";

/** Root of the idl1 UI. M0: a placeholder that Task 5 turns into the IPC smoke panel. */
export default function App() {
  return (
    <main className="idl1-root">
      <h1>idl1</h1>
      <p>M0 scaffold. Engine not connected yet.</p>
    </main>
  );
}
```

`app/src/App.css`:
```css
.idl1-root {
  font-family: system-ui, sans-serif;
  padding: 1.5rem;
}
```
Delete `app/src/assets/react.svg` and `app/public/vite.svg` and `app/public/tauri.svg` if the template created them, and remove their imports.

- [ ] **Step 6: Build the Rust side and type-check the frontend**

```bash
cd "C:/Users/isaac/Documents/Saucy/saucyeng/idl1-app/app/src-tauri" && cargo build 2>&1 | tail -3
cd "C:/Users/isaac/Documents/Saucy/saucyeng/idl1-app/app" && npx tsc --noEmit
```
Expected: `Finished`; `tsc` prints nothing.

Manual (Isaac, optional now, required before M0 exit): `npm run tauri dev` opens a window titled "idl1" showing the placeholder.

- [ ] **Step 7: Commit**

```bash
cd "C:/Users/isaac/Documents/Saucy/saucyeng/idl1-app"
git add -A
git commit -m "app: Tauri v2 + React/TS/Vite scaffold with vitest"
```

---

### Task 5: `idl-rs-tauri` and the first binary IPC round-trip

Working directory: `C:\Users\isaac\Documents\Saucy\saucyeng\idl1-app\rust` for the crate, `…\idl1-app\app` for the frontend.

**Files:**
- Create: `rust/tauri/Cargo.toml`, `rust/tauri/src/lib.rs`, `rust/tauri/src/commands.rs`
- Modify: `rust/Cargo.toml` (members), `app/src-tauri/Cargo.toml`, `app/src-tauri/src/lib.rs`
- Create: `app/src/ipc/tiles.ts`, `app/src/ipc/tiles.test.ts`; modify `app/src/App.tsx`

**Interfaces:**
- Consumes: `idl_rs::VERSION` (Task 2).
- Produces: commands `engine_version() -> String` and `smoke_tile(n: u32) -> tauri::ipc::Response`; TS `decodeTile(buf: ArrayBuffer): Float32Array` and `fetchSmokeTile(n: number): Promise<Float32Array>`. The smoke byte layout (bare little-endian `f32`s) is M0-only and is superseded by C3's tile layout.

- [ ] **Step 1: Write the failing Rust test**

`rust/tauri/src/commands.rs`:
```rust
//! `#[tauri::command]` handlers. Thin: convert, call the engine, return.

/// Reports the engine crate version so the UI (and sync peers) can prove
/// they compute with the same engine.
#[tauri::command]
pub fn engine_version() -> String {
    idl_rs::VERSION.to_string()
}

/// Encodes `values` as bare little-endian `f32` bytes.
///
/// M0 smoke layout only — contract C3 defines the real tile layout (header,
/// min/max pairs, per-column stats). Kept as a pure function so it is testable
/// without a Tauri runtime.
pub fn encode_f32_le(values: &[f32]) -> Vec<u8> {
    let mut out = Vec::with_capacity(values.len() * 4);
    for v in values {
        out.extend_from_slice(&v.to_le_bytes());
    }
    out
}

/// Returns `n` ascending `f32` values `0.0, 1.0, …` as raw bytes, proving the
/// binary IPC path (`tauri::ipc::Response` → `ArrayBuffer` → `Float32Array`).
#[tauri::command]
pub fn smoke_tile(n: u32) -> tauri::ipc::Response {
    let values: Vec<f32> = (0..n).map(|i| i as f32).collect();
    tauri::ipc::Response::new(encode_f32_le(&values))
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn encode_f32_le_four_values_roundtrips_bytes() {
        // Arrange
        let values = [0.0f32, 1.0, 2.0, 3.0];

        // Act
        let bytes = encode_f32_le(&values);
        let decoded: Vec<f32> = bytes
            .chunks_exact(4)
            .map(|c| f32::from_le_bytes([c[0], c[1], c[2], c[3]]))
            .collect();

        // Assert
        assert_eq!(bytes.len(), 16);
        assert_eq!(decoded, values);
    }

    #[test]
    fn engine_version_matches_core_crate_version() {
        // Arrange / Act
        let v = engine_version();

        // Assert
        assert_eq!(v, idl_rs::VERSION);
        assert!(!v.is_empty());
    }
}
```

`rust/tauri/src/lib.rs`:
```rust
//! `idl-rs-tauri` — the only crate the idl1 frontend sees.
//!
//! Every function here is a `#[tauri::command]` wrapper over `idl-rs` (numbers)
//! or `idl-transport` (bytes on the wire). Heavy arrays return
//! `tauri::ipc::Response` raw bytes, never JSON (design §4). Register with:
//!
//! ```ignore
//! tauri::Builder::default().invoke_handler(idl_rs_tauri::handler())
//! ```

pub mod commands;

/// The invoke handler covering every command in this crate. The app crate
/// passes it to `tauri::Builder::invoke_handler` so new commands never touch
/// `app/src-tauri`.
pub fn handler<R: tauri::Runtime>() -> impl Fn(tauri::ipc::Invoke<R>) -> bool + Send + Sync + 'static {
    tauri::generate_handler![commands::engine_version, commands::smoke_tile]
}
```

`rust/tauri/Cargo.toml` (replace `<pin>` with the report's `tauri` pin):
```toml
[package]
name = "idl-rs-tauri"
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0-or-later"
description = "Tauri command glue over idl-rs and idl-transport. The only crate the idl1 frontend sees."

[dependencies]
idl-rs = { path = "../core" }
idl-transport = { path = "../transport" }
tauri = "<pin>"
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
```

In `rust/Cargo.toml`, change members to `["core", "cli", "transport", "tauri"]`.

- [ ] **Step 2: Run the Rust tests**

Run: `cd "C:/Users/isaac/Documents/Saucy/saucyeng/idl1-app/rust" && cargo test -p idl-rs-tauri 2>&1 | grep -E "^test |^test result"`
Expected: both tests `ok`, `0 failed`.

- [ ] **Step 3: Wire the app crate**

`app/src-tauri/Cargo.toml`: add `idl-rs-tauri = { path = "../../rust/tauri" }` under `[dependencies]`.

`app/src-tauri/src/lib.rs` — replace the template's `greet` command and builder with:
```rust
//! Tauri app crate. Thin by design: registers the engine's commands and, in
//! later lanes, the mobile plugins. Nothing else lives here.

#[cfg_attr(mobile, tauri::mobile_entry_point)]
pub fn run() {
    tauri::Builder::default()
        .invoke_handler(idl_rs_tauri::handler())
        .run(tauri::generate_context!())
        .expect("error while running idl1");
}
```
If the template created `app/src-tauri/src/main.rs` calling `app_lib::run()` (name from the template's `[lib] name`), leave it; otherwise ensure `main.rs` calls this `run()`.

Run: `cd "C:/Users/isaac/Documents/Saucy/saucyeng/idl1-app/app/src-tauri" && cargo build 2>&1 | tail -3`
Expected: `Finished`.

- [ ] **Step 4: Write the failing TypeScript test**

`app/src/ipc/tiles.test.ts`:
```ts
import { describe, expect, it } from "vitest";
import { decodeTile } from "./tiles";

describe("decodeTile", () => {
  it("four little-endian f32s — decodes — returns [0,1,2,3]", () => {
    // Arrange
    const bytes = new Uint8Array(16);
    const view = new DataView(bytes.buffer);
    [0, 1, 2, 3].forEach((v, i) => view.setFloat32(i * 4, v, true));

    // Act
    const tile = decodeTile(bytes.buffer);

    // Assert
    expect(Array.from(tile)).toEqual([0, 1, 2, 3]);
  });

  it("byte length not a multiple of 4 — throws — typed error", () => {
    // Arrange
    const bytes = new Uint8Array(6);

    // Act / Assert
    expect(() => decodeTile(bytes.buffer)).toThrowError(/multiple of 4/);
  });
});
```

Run: `cd "C:/Users/isaac/Documents/Saucy/saucyeng/idl1-app/app" && npm test`
Expected: FAIL — `Cannot find module './tiles'`.

- [ ] **Step 5: Implement `tiles.ts`**

`app/src/ipc/tiles.ts`:
```ts
import { invoke } from "@tauri-apps/api/core";

/**
 * Wraps raw IPC bytes as a Float32Array view without copying.
 *
 * M0 smoke layout: bare little-endian f32s. Contract C3 replaces this with the
 * real tile layout (header, min/max pairs, per-column stats). All Tauri targets
 * are little-endian, so the platform-endian Float32Array view is correct.
 *
 * @throws Error if the byte length is not a multiple of 4.
 */
export function decodeTile(buf: ArrayBuffer): Float32Array {
  if (buf.byteLength % 4 !== 0) {
    throw new Error(`tile byte length ${buf.byteLength} is not a multiple of 4`);
  }
  return new Float32Array(buf);
}

/** Fetches `n` ascending values from the engine over binary IPC. */
export async function fetchSmokeTile(n: number): Promise<Float32Array> {
  const buf = await invoke<ArrayBuffer>("smoke_tile", { n });
  return decodeTile(buf);
}

/** The engine crate version, from `idl_rs::VERSION`. */
export async function fetchEngineVersion(): Promise<string> {
  return invoke<string>("engine_version");
}
```

Run: `npm test`
Expected: `3 passed` (two here plus the Task 4 smoke test).

- [ ] **Step 6: Show it in the UI**

`app/src/App.tsx`:
```tsx
import { useEffect, useState } from "react";
import "./App.css";
import { fetchEngineVersion, fetchSmokeTile } from "./ipc/tiles";

/** Root of the idl1 UI. M0: proves JSON and binary IPC against the engine. */
export default function App() {
  const [version, setVersion] = useState<string>("…");
  const [tile, setTile] = useState<number[]>([]);
  const [error, setError] = useState<string | null>(null);

  useEffect(() => {
    fetchEngineVersion().then(setVersion).catch((e) => setError(String(e)));
    fetchSmokeTile(8)
      .then((t) => setTile(Array.from(t)))
      .catch((e) => setError(String(e)));
  }, []);

  return (
    <main className="idl1-root">
      <h1>idl1</h1>
      <p>Engine {version}</p>
      <p>Smoke tile: {tile.join(", ")}</p>
      {error && <p className="idl1-error">{error}</p>}
    </main>
  );
}
```
Add to `app/src/App.css`:
```css
.idl1-error {
  color: #b00020;
}
```

Run: `cd "C:/Users/isaac/Documents/Saucy/saucyeng/idl1-app/app" && npx tsc --noEmit && npm test`
Expected: clean, `3 passed`.

Manual (Isaac, required before M0 exit): `npm run tauri dev` shows `Engine 0.x.y` and `Smoke tile: 0, 1, 2, 3, 4, 5, 6, 7`. If the tile line is empty and the error mentions `ArrayBuffer`, the binary path is broken — stop and report; do not fall back to JSON.

- [ ] **Step 7: Commit (both repos)**

```bash
cd "C:/Users/isaac/Documents/Saucy/saucyeng/idl1-app/rust" && git add -A && git commit -m "tauri: idl-rs-tauri crate with engine_version and the binary IPC smoke tile"
cd "C:/Users/isaac/Documents/Saucy/saucyeng/idl1-app" && git add -A && git commit -m "app: wire idl-rs-tauri; binary IPC round-trip shown in the UI"
```

---

### Task 6: Contract C1 — session schema

Working directory: `C:\Users\isaac\Documents\Saucy\saucyeng\idl1-app`. Drafted by an implementer; reviewed by a reviewer against design §5 and §9; finalised by the lead. A contract task is done when the reviewer's verdict is clean and the lead has signed the "Status" line.

**Files:**
- Create: `docs/superpowers/specs/2026-09-03-idl1-c1-session-schema.md`

**Interfaces:**
- Consumes: design §5 (time model, Parquet layout, materialised channels), inventory §2 (engine `Session`/`Channel`/`RawColumn`), SPEC §5.5–5.6 (IMU/GPS record timing), §5 channel tables.
- Produces: the Arrow schema and metadata every L1/L2/L3 implementer codes against.

- [ ] **Step 1: Draft with this exact outline**

```markdown
# C1 — Session schema

**Status:** draft | reviewed | signed (lead) · **Date:** · **Owner:** lead

## 1. Scope
What this contract fixes and what it leaves to L1's SPEC section.

## 2. Canonical in-memory model
`Session` and `Channel` as they will be after L1 (fields, types, units). State the one change
from today: per-sample time is mandatory (`t_us: Vec<i64>`, µs since the session's first
sample); `nominal_rate_hz: f64` is metadata. Keep `RawColumn` variants and say which survive
the Parquet round-trip bit-exact.

## 3. Time model
3.1 Definition of `t` (int64 µs from first sample; origin `timestamp_utc_ms` in file metadata).
3.2 Recorded stamps: per-source `<source>_t_recorded_us` columns, verbatim from `timestamp_us`.
3.3 Burst-seam correction: inputs, algorithm (fit burst read-instants → effective ODR →
    monotonic per-sample time), version string, where the version is stored, what happens
    on version change (regenerate `data.parquet` from the blob).
3.4 Non-device sources (FIT, GPX): their recorded timestamps map to `t` how; what is stored
    in `_t_recorded_us` for them.
3.5 Invariants: `t` strictly increasing within a source; union axis sorted; no synthesis from
    nominal rate anywhere.

## 4. `data.parquet` schema
4.1 Columns: `t` (INT64, DELTA_BINARY_PACKED), one per channel with physical type by
    `RawColumn` variant (INT16/INT32/FLOAT/DOUBLE), per-source `_t_recorded_us`.
4.2 Column key-value metadata: `scale`, `offset`, `nominal_rate_hz`, `unit`, `source_kind`,
    `channel_kind` (fixed-rate | event).
4.3 File key-value metadata: `session_id`, `timestamp_utc_ms`, `device_id?`,
    `config_checksum?`, `blob_sha256`, `source_format`, `importer_version`,
    `engine_version`, `seam_correction_version`.
4.4 Row groups: target size; `t` statistics required.
4.5 Nulls: definition-level encoding; the read rule (drop nulls → compact column).

## 5. `derived/<hash>.parquet`
Same layout rules; the hash recipe `sha256(input column hashes ‖ config json ‖ engine_version)`
with exact byte order; file metadata listing inputs.

## 6. `session.json`
Fields (from inventory: laps, track visits, lap flags, rider, bike profile snapshot), types,
units, and what from `.idl0w` is dropped.

## 7. Round-trip guarantees
What L1 must prove with tests: recorded stamps bit-exact, raw counts bit-exact, scale/offset
metadata preserved, `nominal_rate_hz` preserved, gaps preserved.

## 8. Open questions
Each assigned to a lane or to Isaac, none unassigned.
```
Every section must contain the actual values; a section that says only "see design" is a failure.

- [ ] **Step 2: Reviewer checklist**

The reviewer writes `docs/superpowers/reviews/2026-09-03-c1-review.md` with a verdict line and confirms, with quotes: (a) every fact listed in design §9 row C1 appears; (b) §3.5 invariants contradict nothing in §4; (c) no "TBD"/"TODO"/"decide later"; (d) units on every numeric field; (e) the hash recipe is byte-exact reproducible from the text.

- [ ] **Step 3: Lead signs and commits**

```bash
git add docs/superpowers/specs/2026-09-03-idl1-c1-session-schema.md docs/superpowers/reviews/2026-09-03-c1-review.md
git commit -m "docs: contract C1 session schema (signed)"
```

---

### Task 7: Contract C2 — workbook v3

Working directory, roles and done-criteria as Task 6.

**Files:**
- Create: `docs/superpowers/specs/2026-09-03-idl1-c2-workbook-v3.md`

**Interfaces:**
- Consumes: design §5 (workbook), §6 (cells, editor, `plotForm` subset), D8, D13, the worked example; `docs/legacy/idl0-workbook_format.md`; inventory rows `math_channel.dart`, `workbook.dart`, `worksheet.dart`.
- Produces: the grammar L3 parses, L6 edits, L11 merges.

- [ ] **Step 1: Draft with this exact outline**

```markdown
# C2 — Workbook v3 (`.idl1wb`)

**Status:** draft | reviewed | signed (lead) · **Date:** · **Owner:** lead

## 1. Container
Framework-compatible Markdown. Front matter: YAML with keys `id` (UUIDv4, required),
`name`, `constants` (map name → number, with an optional unit suffix syntax), `units`
(`si` | `imperial`), `version: 3`. Encoding UTF-8, LF.

## 2. Cells
2.1 Kinds: prose, `math`, `table`, `js`. Only fenced blocks execute.
2.2 Cell id: carried in the fence info string as `id=<8 hex>`; e.g. ```` ```js id=3fa9c12e ````.
    Assigned on first save when absent; never changed; the unit of diff and merge.
2.3 Fence info grammar (EBNF).
2.4 Ordering and document flow.

## 3. Math cells
3.1 Definition syntax: one `name = expression` per line; blank lines and `#` comments allowed.
3.2 The expression grammar: the existing evaluator's, by reference to its module, plus
    constants (front matter and `const name = value` lines) — say which.
3.3 The builtin catalog: the full list of functions with signature and units (moved from
    Dart's `MathChannel.knownFunctions`), marked implemented / NotImplemented.
3.4 Unit table (from `math_quantity.dart`): quantity → default unit per unit system.
3.5 Validation errors: kind, message, cell-scoped.

## 4. Table cells
The existing table model, as JSON inside the fence, with its schema.

## 5. JS cells
5.1 Runtime: Observable Runtime semantics; the host-provided variables (one per math
    definition, plus `channel`, `laps`, `session`, `constants`, `Plot`, `d3`, `Inputs`, `html`).
5.2 Inline `${…}` in prose: JS expressions, same scope.
5.3 The `plotForm` subset: the exact Plot code the Properties form generates (marks list,
    option keys, scale keys), as a grammar plus three worked examples, and the rule for when
    a cell is "custom" (anything the parser does not recognise).

## 6. Migration from `.idl0wb` v2
Field-by-field mapping; what is dropped; the CLI (`idl-rs migrate-workbook`) vs app (chart
slots on first open) split.

## 7. Merge semantics (for L11)
Per-cell three-way merge over the last-synced base: the decision table for
(unchanged/changed/added/deleted) × (same on both sides); conflict-cell marker
`<!-- conflict from <peer> -->`; ordering of unioned cells.

## 8. Open questions
Each assigned.
```

- [ ] **Step 2: Reviewer checklist**

`docs/superpowers/reviews/2026-09-03-c2-review.md`: (a) the worked example from design §5 parses under §2–§5 as written; (b) §5.3 examples round-trip conceptually (props → code → props); (c) §7 decision table has all 16 cells filled; (d) no placeholders; (e) §3.3 lists every function in idl0's `MathChannel.knownFunctions` (the reviewer reads `app/lib/data/math_channel.dart` in idl0-app to check).

- [ ] **Step 3: Lead signs and commits**

```bash
git add docs/superpowers/specs/2026-09-03-idl1-c2-workbook-v3.md docs/superpowers/reviews/2026-09-03-c2-review.md
git commit -m "docs: contract C2 workbook v3 (signed)"
```

---

### Task 8: Contract C3 — IPC surface

Working directory, roles and done-criteria as Task 6.

**Files:**
- Create: `docs/superpowers/specs/2026-09-03-idl1-c3-ipc-surface.md`

**Interfaces:**
- Consumes: design §4 (IPC, data path), §6 (interaction rules), Task 5's smoke commands, `TransportError` (Task 2).
- Produces: the command list L5 implements and L6/L7 call.

- [ ] **Step 1: Draft with this exact outline**

```markdown
# C3 — IPC surface

**Status:** draft | reviewed | signed (lead) · **Date:** · **Owner:** lead

## 1. Conventions
Command naming (`snake_case` verbs), argument objects, JSON for control, `tauri::ipc::Response`
for binary, `tauri::ipc::Channel<Progress>` for streams; the TS wrapper module per command
group under `app/src/ipc/`.

## 2. Error shape
One JSON error crossing every command: `{ "kind": string, "message": string, "detail"?: object }`,
with the `kind` vocabulary as the union of core's error enums and `TransportErrorKind`
(list every value).

## 3. Commands
For each: name, args (typed), return (typed), binary layout if any, errors it can raise,
which lane implements it. Groups:
3.1 Engine: `engine_version`.
3.2 Catalog (L1): `list_sessions`, `get_session`, `rebuild_catalog`, `list_workbooks`, `list_tracks`.
3.3 Import (L2): `import_file` (streams progress), `list_importers`.
3.4 Workbook (L3): `open_workbook`, `eval_workbook` (returns per-cell outputs), `save_workbook`,
    `watch_workbook` (event channel).
3.5 Tiles (L3): `fetch_tile` — args `(session_id, channel, tier, tile_index)`; binary layout:
    header (magic, version, tier, tile_index, sample_count, column_count) then `f32` min/max
    pairs then per-pixel-column `(min, max, mean)` — give byte offsets.
3.6 Rasters (L3): `fetch_raster` — args, RGBA layout with width/height header.
3.7 Cursor (L3): `cursor_readout` — values at time `t` for a channel list.
3.8 Device (L4): `ble_scan`, `ble_connect`, `list_device_files`, `download_file` (progress),
    `push_config`.
3.9 Sync (L11): `sync_status`, `sync_now`, `pair_peer`.

## 4. Interaction budget
Which commands may be called on gesture settle only; which never from a hot path (design §6).

## 5. Versioning
How a breaking change to a command is made (rename + deprecate window + C3 revision line).

## 6. Open questions
Each assigned.
```

- [ ] **Step 2: Reviewer checklist**

`docs/superpowers/reviews/2026-09-03-c3-review.md`: (a) every command has typed args and return; (b) §3.5 byte offsets sum correctly for a worked example (tier 3, 512 samples, 256 columns); (c) §2 `kind` list includes every `TransportErrorKind` value from `rust/transport/src/error.rs`; (d) no placeholders.

- [ ] **Step 3: Lead signs and commits**

```bash
git add docs/superpowers/specs/2026-09-03-idl1-c3-ipc-surface.md docs/superpowers/reviews/2026-09-03-c3-review.md
git commit -m "docs: contract C3 IPC surface (signed)"
```

---

### Task 9: Contract C4 — data directory

Working directory, roles and done-criteria as Task 6.

**Files:**
- Create: `docs/superpowers/specs/2026-09-03-idl1-c4-data-directory.md`

**Interfaces:**
- Consumes: design §5 (data directory proposal, catalog tables), §7 (what sync moves), D10.
- Produces: the on-disk layout L1 writes, L11 syncs, and the CLI reads.

- [ ] **Step 1: Draft with this exact outline**

```markdown
# C4 — Data directory

**Status:** draft | reviewed | signed (lead) · **Date:** · **Owner:** lead

## 1. Root
Per-platform default `<data>` path (Windows, macOS, Linux, Android, iOS) via Tauri's
app-data dir; override in Settings; one root per user.

## 2. Layout
The tree from design §5 with every path pattern fixed:
`blobs/sha256/<2 hex>/<62 hex>`; `sessions/<session_id>/{session.json,data.parquet,derived/<64 hex>.parquet}`;
`workbooks/<name>.idl1wb`; `tracks/<id>.idl0t`; `catalog.sqlite`; `tmp/` for atomic writes.

## 3. Identity
`session_id`: the device's session id for `.idl0`; the first 16 hex of the blob hash for
FIT/GPX/CSV; collision rule. Blob hash: SHA-256 over the file bytes.

## 4. Atomic writes
Write to `tmp/<uuid>`, fsync, rename; the app ignores its own writes via an expected-hash set
(design §7); how the watcher distinguishes external edits.

## 5. Catalog
SQLite schema DDL for `sessions`, `blobs`, `workbooks`, `tracks`, `laps`, `lap_summary`;
indexes; the rebuild procedure (scan order, what is derived from what); `PRAGMA` settings;
the rule that nothing reads the catalog for truth.

## 6. Sync scope
What moves (blobs, `data.parquet`, `derived/*.parquet`, `workbooks/*.idl1wb`, `tracks/*`,
`session.json`), what never moves (`catalog.sqlite`, `tmp/`), and the manifest entry format
per file class.

## 7. Retention and repair
Orphaned derived files; missing blobs; a `verify` procedure listing every check.

## 8. Open questions
Each assigned.
```

- [ ] **Step 2: Reviewer checklist**

`docs/superpowers/reviews/2026-09-03-c4-review.md`: (a) every path in design §5 appears with a pattern; (b) §5 DDL is valid SQLite (reviewer runs it through `sqlite3 :memory:` if available, else checks syntax by eye and says so); (c) §6 agrees with design §7 and D6; (d) no placeholders.

- [ ] **Step 3: Lead signs and commits**

```bash
git add docs/superpowers/specs/2026-09-03-idl1-c4-data-directory.md docs/superpowers/reviews/2026-09-03-c4-review.md
git commit -m "docs: contract C4 data directory (signed)"
```

---

### Task 10: M0 exit

Working directory: `C:\Users\isaac\Documents\Saucy\saucyeng\idl1-app`.

**Files:**
- Modify: `TASKS.md`, `CHANGELOG.md`, `docs/superpowers/specs/2026-09-02-idl1-rewrite-design.md` (copy in idl1-app: mark §16 items resolved by contracts).

- [ ] **Step 1: Confirm the manual checks happened**

Isaac has run `npm run tauri dev` (Task 5 step 6) and seen the smoke tile. Record the date and platform in `CHANGELOG.md` under a new `### Verified` heading:
```markdown
### Verified

- Binary IPC (`tauri::ipc::Response` → `Float32Array`) on Windows desktop, <date>.
```

- [ ] **Step 2: Tick TASKS.md and log the milestone**

Tick Tasks 4–10 in `TASKS.md`. Add to `CHANGELOG.md` under `### Added`:
```markdown
- **M0 complete (<date>).** Tauri v2 scaffold, `idl-rs-tauri` with the binary IPC
  smoke path, `idl-transport` stub, and contracts C1–C4 signed. Wave 1 may start.
```

- [ ] **Step 3: Resolve design §16**

In the idl1-app copy of the design doc, replace each §16 bullet that a contract resolved with `— resolved in C<n> §<m>` (keep the bullet text). Leave truly open items (firmware burst-boundary stamping is Isaac's).

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "docs: M0 complete — contracts signed, IPC verified"
git log --oneline -12
```

- [ ] **Step 5: Hand back to Isaac**

Report: the three repos' unpushed state (idl0-app: 2 commits; idl-rs `main`: 2 commits + 2 tags + 1 branch; idl1-app: all commits) and the exact push commands, which Isaac runs:
```bash
cd "C:/Users/isaac/Documents/Saucy/saucyeng/idl0-app/rust" && git push origin main idl0-final idl0-video-export idl0-maint
cd "C:/Users/isaac/Documents/Saucy/saucyeng/idl0-app" && git push
# then create github.com/saucyeng/idl1-app and:
cd "C:/Users/isaac/Documents/Saucy/saucyeng/idl1-app" && git remote add origin <url> && git push -u origin main
```
After the idl-rs push, `git -C rust remote remove local` in idl1-app and re-point the submodule at `origin`.

---

## Self-review

**Spec coverage (design §11 M0):** contracts C1–C4 → Tasks 6–9; repo created with docs, submodule, Tauri template → Tasks 3–4; idl-rs tags `idl0-final`/`idl0-video-export` and D9 deletions → Task 2; ecosystem verification → Task 1; binary IPC proof (design §4 "verified at M0") → Task 5; idl0-app housekeeping the design implies (rationale entry, TASKS supersession, CHANGELOG) → Task 2 step 1. `idl0-maint` branch (design §13) → Task 2 step 3. Wave-1 lane briefs are not M0 (design §11 lists them nowhere in M0); they are the first act of wave 1.

**Placeholder scan:** the only `<…>` tokens are `<pin>` (defined: copy from the Task 1 report), `<date>` (the day the step runs), `<url>`/`<peer>` (Isaac's inputs at hand-back / C2's merge marker). No TBD/TODO.

**Type consistency:** `idl_rs::VERSION` defined Task 2 step 6, used Task 5; `TransportErrorKind::{Ble,Wifi,Config,Sync}` defined Task 2 step 8, referenced by C3 §2 (Task 8); `decodeTile`/`fetchSmokeTile`/`fetchEngineVersion` defined Task 5 step 5, used step 6; `idl_rs_tauri::handler()` defined step 1, used step 3; workspace members updated in Task 2 (`transport`) and Task 5 (`tauri`).
