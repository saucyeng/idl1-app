# idl1 Wave 1 — L10 (docs) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development
> (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use
> checkbox (`- [ ]`) syntax for tracking.

**Goal:** L10 is the cross-cutting docs lane (design §10 row: "idl1-app SPEC (copy Parts
1–2/3–10; rewrite app parts per lane); CLAUDE.md layer rules; README; CHANGELOG/TASKS
discipline; fix idl0 CLAUDE.md §2 staleness. Done when: every shipped lane has its SPEC
section."). It does not implement L1–L5's SPEC sections — each of those lanes writes its own
(spec-first, CLAUDE.md §6). L10's job is everything no lane owns: the small doc/test-name
carry-overs M0 deliberately deferred, CHANGELOG/TASKS discipline for the wave, and — last,
gated on L1–L5 landing — verifying every lane actually did write its SPEC section where the
design doc says it should have, plus polishing SPEC §11 (App Architecture) for consistency
with CLAUDE.md §2's layer table.

**Architecture:** No code architecture changes. This plan touches three repos:
`idl1-app` (docs, tests-in-name-only, TASKS/CHANGELOG), the `rust` submodule (`idl-rs`, one
test-name-only change), and `idl0-app` (a different repo, one CLAUDE.md fix, its own commit).

**Tech Stack:** N/A — Markdown and one Rust test-function-name edit (no logic change, no
`cargo fmt`).

**Spec:** `docs/superpowers/specs/2026-09-02-idl1-rewrite-design.md` §10 (L10 row), §11
(milestones — L10 "all" wave), §12 (agent-team operating model — L10 is the lane that verifies
every other lane followed it); `CLAUDE.md` (this repo's standing orders, especially §6 spec
discipline and §7 repo hygiene); `docs/IDL0_SPEC.md` (the document L10 verifies coverage of,
touching only §11 here — Parts 1–2/§3–10 are current and untouched; §15/§16.3/§18 → L1;
a new importers section → L2; §17a → L3).

## Global Constraints

- Repo paths: `idl1-app` at `C:\Users\isaac\Documents\Saucy\saucyeng\idl1-app` (submodule
  `rust` checked out inside it); `idl0-app` at
  `C:\Users\isaac\Documents\Saucy\saucyeng\idl0-app` — a **separate git repository**, touched
  only by Task 4, in its own commit, never via idl1-app's history.
- **No AI attribution trailers** in any commit, in either repo.
- **Never `git push`.** Every task stops at a local commit.
- **`idl-rs` is not rustfmt-formatted.** Task 3 changes two identifiers only — do not run
  `cargo fmt`, do not let an editor reformat the surrounding file.
- **Never touch a repo without checking its state first.** Not written verbatim anywhere in
  CLAUDE.md, but implied by its ambiguity policy (§1) and hygiene rules (§7: lanes touch only
  their own directory; a repo whose current state is unknown is exactly the case where an
  assumption is riskiest) and modelled by M0 Task 2 (which begins "the working tree already
  has the submodule at …" — established by inspection, not assumed) and Task 4's own working
  Isaac gave this task ("confirm idl0-app's current branch/state first ... as your task's
  first step"). Every task below whose working directory is a repo root begins with a
  `git status` (and, for idl0-app specifically, `git branch --show-current`) check.
- **Branches.** `idl1-app`: `wave1-l10-docs`. `rust` submodule (`idl-rs`): also
  `wave1-l10-docs` (a second branch there, independent of whatever branch L1–L4 are using in
  the same submodule checkout — different files, no collision). `idl0-app`: **no branch** —
  Isaac's instruction is explicit that this is its own separate commit directly on `main`,
  matching how M0 Task 2 Step 1 recorded the idl1 pivot directly on idl0-app's `main`.
- Spec discipline (CLAUDE.md §6): every task below states spec-first / spec-during / "no spec
  change needed" explicitly — L10 is the lane whose job *is* spec discipline, so no task needs
  more framing than that one line.

---

## File Structure

**idl1-app (branch `wave1-l10-docs`):**
- Modify: `tools/README.md`, `app/README.md`, `CHANGELOG.md`, `TASKS.md`,
  `docs/IDL0_SPEC.md` (§11 only, Task 6).
- No new files.

**rust submodule / idl-rs (branch `wave1-l10-docs`):**
- Modify: `transport/src/error.rs` (two test function names; no other line changes).

**idl0-app (commit on `main`, no branch):**
- Modify: `CLAUDE.md` (§2 "Layer Separation" subsection only), `CHANGELOG.md`.

---

### Task 1: Confirm CLAUDE.md layer rules need no change

**Spec discipline:** no spec change needed — this task is a confirmation, not an edit; said
out loud per CLAUDE.md §6 rather than silently skipped, per the scope line that named this
explicitly.

Working directory: `C:\Users\isaac\Documents\Saucy\saucyeng\idl1-app` (read-only).

**Files:** none modified.

**Interfaces:** none.

- [ ] **Step 1: Diff CLAUDE.md §2 against the design doc's target architecture**

Run (Git Bash):
```bash
sed -n '/^## 2\. Layers/,/^## 3\./p' "C:/Users/isaac/Documents/Saucy/saucyeng/idl1-app/CLAUDE.md"
sed -n '/^## 4\. Target architecture/,/^## 5\./p' "C:/Users/isaac/Documents/Saucy/saucyeng/idl1-app/docs/superpowers/specs/2026-09-02-idl1-rewrite-design.md"
```
Compare by eye: `rust/core` → `idl-rs` (pure, no Tauri/async/network, `std::fs` only);
`rust/transport` → `idl-transport` (I/O, never Tauri, never DSP); `rust/tauri` →
`idl-rs-tauri` (thin glue, only crate the frontend sees); `rust/cli` → `idl-rs-cli`;
`app/src-tauri` (Tauri app crate); `app/src` (TypeScript UI). Both documents state the same
six rows, the same "Rust = numbers, JS = pictures" line, and the same decision rule
(bike physics/bytes on disk → core; wire bytes → transport; pixels/clicks → app/src). As of
this plan's drafting (2026-09-03) they match verbatim in substance — no wording drift found.

- [ ] **Step 2: Record the finding**

No file changes. The finding ("CLAUDE.md §2 needs no change; confirmed against design §4,
2026-09-03") is recorded in this plan (here) and repeated in the L10 BRIEF.md's done-criteria.
If a future re-run of this step finds drift (e.g. a lane's contract changes a crate's role),
that is a contract change through the lead (CLAUDE.md §7), not something L10 edits unilaterally
— stop and report instead of editing CLAUDE.md.

**Done when:** the diff has been read and either confirmed identical (this run: yes) or
reported as drifted, with no CLAUDE.md edit made either way by this task.

---

### Task 2: idl1-app README fixes (`tools/README.md`, `app/README.md`)

**Spec discipline:** no spec change needed — these are project docs, not `docs/IDL0_SPEC.md`
sections.

Working directory: `C:\Users\isaac\Documents\Saucy\saucyeng\idl1-app`.

**Files:**
- Modify: `tools/README.md`, `app/README.md`, `CHANGELOG.md`.

**Interfaces:** none — pure documentation.

- [ ] **Step 1: Check repo state and branch**

```bash
cd "C:/Users/isaac/Documents/Saucy/saucyeng/idl1-app" && git status
```
Expected: clean working tree (per this session's git status snapshot: clean, branch `main`).
If not clean or not `main`, stop and report rather than branching from unknown state.

```bash
git checkout -b wave1-l10-docs
```

- [ ] **Step 2: Fix `tools/README.md` — drop the `idl0_dump.dart` section**

M0 Task 3 Step 1 deliberately did not copy `tools/idl0_dump.dart` into idl1-app (it is Dart,
and idl1 has no Dart layer — CLAUDE.md §2). The README still documents it as if present.
Confirmed present at drafting time (`## \`idl0_dump.dart\`` heading, lines 7–14).

Replace:
```markdown
## `idl0_dump.dart`

Dump and inspect a `.idl0` binary log (and legacy `.espl` files): header fields,
record boundaries, and per-channel sample counts.

```bash
dart run tools/idl0_dump.dart path/to/session.idl0
```
```
with:
```markdown
## Log inspection

`idl0_dump.dart` (header fields, record boundaries, `.espl` legacy support) is a Dart tool and
was not carried into idl1-app (idl1 has no Dart layer — CLAUDE.md §2); it remains available in
`idl0-app/tools/idl0_dump.dart` for anyone still working against that repo. The overlapping,
Rust-side subset — session metadata and per-channel sample counts — is available here via the
CLI: `idl-rs info <file.idl0>` and `idl-rs channels <file.idl0>` (see `rust/cli/src/main.rs`).
```
Also update the file's opening line (`Developer utilities for inspecting IDL0 logs and
prototyping the processing pipeline.`) — no change needed there, it still describes the
remaining contents accurately.

- [ ] **Step 3: Fix `app/README.md` — replace the scaffolder's generic template**

M0 Task 4 replaced `app/src/App.tsx`/`App.css` but never touched `app/README.md`, which is
still `npm create tauri-app`'s stock text. Confirmed present at drafting time (the exact
"Tauri + React + Typescript" / "Recommended IDE Setup" boilerplate).

Replace the entire file with:
```markdown
# idl1 app

The Tauri v2 + React/TypeScript frontend for idl1. Talks only to `idl-rs-tauri` commands
(`rust/tauri/`) — never to `idl-rs` or `idl-transport` directly (CLAUDE.md §2).

**Status:** M0 scaffold. See the repository root `README.md` for project status and
`../CLAUDE.md` for the layer rules this crate lives under.

## Develop

    npm install
    npm run tauri dev

## Test

    npm test

## Recommended IDE setup

[VS Code](https://code.visualstudio.com/) + [Tauri](https://marketplace.visualstudio.com/items?itemName=tauri-apps.tauri-vscode) + [rust-analyzer](https://marketplace.visualstudio.com/items?itemName=rust-lang.rust-analyzer)
```

- [ ] **Step 4: CHANGELOG entry**

Add to `CHANGELOG.md` under `## [Unreleased]` → `### Changed` (create the `### Changed`
subheading under `[Unreleased]` if it does not yet exist there):
```markdown

### Changed

- **Doc carry-over fixes (2026-09-03, L10).** `tools/README.md` no longer documents the
  uncarried `idl0_dump.dart`; points at `idl-rs info`/`idl-rs channels` for the overlapping
  functionality. `app/README.md` replaced (was still the Tauri scaffolder's generic template).
```

- [ ] **Step 5: Verify and commit**

```bash
grep -c "idl0_dump.dart" "C:/Users/isaac/Documents/Saucy/saucyeng/idl1-app/tools/README.md"
```
Expected: `1` (only the new cross-reference sentence mentions the filename; the old `##`
heading and `dart run` fence are gone).

```bash
grep -c "Tauri + React + Typescript" "C:/Users/isaac/Documents/Saucy/saucyeng/idl1-app/app/README.md"
```
Expected: `0`.

```bash
git add tools/README.md app/README.md CHANGELOG.md
git commit -m "docs: fix stale tool/app README carry-overs from M0"
```

---

### Task 3: Rename the two `transport/src/error.rs` test functions

**Spec discipline:** no spec change needed — test names only, test logic (Arrange/Act/Assert
bodies, assertions) is byte-for-byte unchanged.

Working directory: `C:\Users\isaac\Documents\Saucy\saucyeng\idl1-app\rust` (the `idl-rs`
submodule — a separate git repository from `idl1-app` itself).

**Files:**
- Modify: `transport/src/error.rs`.

**Interfaces:** none — private `#[cfg(test)]` function names, not part of any crate's public
surface or any contract.

- [ ] **Step 1: Check submodule state and branch**

```bash
cd "C:/Users/isaac/Documents/Saucy/saucyeng/idl1-app/rust" && git status && git branch --show-current
```
Expected (this session's snapshot): clean, `main`, up to date with `origin/main`. If another
lane's branch is currently checked out here, stop — do not branch from a lane's in-flight
work; report instead.

```bash
git checkout -b wave1-l10-docs
```

- [ ] **Step 2: Rename, matching the codebase's own test-naming convention**

CLAUDE.md §4 states test names as `thing — condition — result`; Rust identifiers cannot
contain em dashes, so the convention actually visible in this codebase renders that as
concatenated snake_case, e.g. `rust/core/src/chart_decimation.rs`'s
`decimate_tile_pure_all_finite_bucket_returns_min_and_max` (thing=`decimate_tile_pure`,
condition=`all_finite_bucket`, result=`returns_min_and_max`) and
`rust/core/src/calibration.rs`'s `rotation_from_gravity_90_degree_rotation_when_sensor_x_points_up`.
The two transport names are flat `snake_case_with_underscores` without that three-part shape
(no explicit condition segment).

In `transport/src/error.rs`, rename (test bodies unchanged):

```rust
    fn transport_error_display_carries_kind_and_message() {
```
→
```rust
    fn transport_error_display_ble_kind_with_message_returns_kind_colon_message() {
```

```rust
    fn transport_error_serialises_kind_as_snake_case() {
```
→
```rust
    fn transport_error_serialize_wifi_kind_returns_snake_case_json() {
```

No other line in the file changes — not the `use` lines, not the struct/enum, not doc
comments, not formatting/whitespace. Do not run `cargo fmt` (CLAUDE.md §7 / this repo-wide
rule; `idl-rs` is hand-formatted).

- [ ] **Step 3: Run the tests**

```bash
cargo test -p idl-transport 2>&1 | grep -E "^test |^test result"
```
Expected: two `test transport::...ok` lines under the new names, `test result: ok. 2 passed;
0 failed`.

- [ ] **Step 4: Confirm no unrelated diff**

```bash
git diff --stat transport/src/error.rs
```
Expected: `1 file changed, 2 insertions(+), 2 deletions(-)` (exactly the two renamed lines).

- [ ] **Step 5: Commit**

```bash
git add transport/src/error.rs
git commit -m "transport: rename error tests to the thing_condition_result convention (CLAUDE.md §4)"
git log --oneline -1
```

Note for the lead: this commit lands on the submodule's `wave1-l10-docs` branch, independent
of whichever branch L1/L4 use in the same checkout. It merges to `idl-rs` `main` on its own —
no coordination needed since it touches only `transport/src/error.rs` and no other wave-1 lane
owns that file's test names.

---

### Task 4: Fix idl0-app `CLAUDE.md` §2 staleness (separate repo, separate commit)

**Spec discipline:** no spec change needed — idl0-app has no `docs/IDL0_SPEC.md` app-parts
rewrite obligation (it is frozen); this is a standing-orders correction, not a spec section.

Working directory: `C:\Users\isaac\Documents\Saucy\saucyeng\idl0-app`. **A different repository
from idl1-app** — its own git history, its own remote, never touched via idl1-app commits.

**Files:**
- Modify: `CLAUDE.md` (the "### Layer Separation" subsection of §2 only), `CHANGELOG.md`.

**Interfaces:** none.

- [ ] **Step 1: Check repo state and branch first**

```bash
cd "C:/Users/isaac/Documents/Saucy/saucyeng/idl0-app" && git status && git branch --show-current
```
Expected (this session's snapshot, 2026-09-03): clean working tree, branch `main`, up to date
with `origin/main`. Re-run this check at execution time rather than trusting the snapshot —
per the "never touch a repo without checking its state first" reasoning in Global Constraints;
if the state has changed (dirty tree, different branch, behind origin), stop and report rather
than committing over it.

- [ ] **Step 2: What's stale**

`CLAUDE.md` §2's "### Layer Separation" subsection (lines ~71–114) has no mention anywhere
that idl0-app was frozen on 2026-09-02 (confirmed by grep: zero hits for
`freeze|frozen|idl1|pivot|idl0-final|superseded` in the whole file). Its prose narrates an
*ongoing* migration in present tense: "Still migrating into the engine: lap/track analysis and
the `.idl0w` workspace model — these remain in Dart until their phases land. GPX parsing also
stays Dart for now." That migration does not happen — idl0-app is feature-frozen and the
`rust/` submodule is pinned at `idl0-final` forever (recorded in `docs/design_rationale.md`,
`TASKS.md`, and `CHANGELOG.md` by M0 Task 2 Step 1, but never in `CLAUDE.md` itself — the file
whose own first line says "Read this file... before starting any task"). An agent picking up
a idl0-maint bug fix today would read "for now" and reasonably infer migration work is still
live.

- [ ] **Step 3: Edit `CLAUDE.md`**

Insert immediately after the `### Layer Separation` heading (before "The processing layer is
Rust. Everything else is Dart."):
```markdown
**idl0-app is feature-frozen (2026-09-02).** It is superseded by idl1-app
(`docs/superpowers/specs/2026-09-02-idl1-rewrite-design.md`, written into this repo's history
at the pivot); only bug fixes land here now. The `rust/` submodule is pinned at `idl0-final`
forever (§8 Key Files); engine fixes this repo still needs come from the idl-rs `idl0-maint`
branch, never from `main`.

```
Replace the sentence:
```markdown
recursive math-channel dependencies resolve Rust-side in one pass. Still
migrating into the engine: lap/track
analysis and the `.idl0w` workspace model — these remain in Dart until their
phases land. GPX parsing also stays Dart for now.
```
with:
```markdown
recursive math-channel dependencies resolve Rust-side in one pass. Lap/track
analysis and the `.idl0w` workspace model stay in Dart **permanently** — the
migration into the engine planned before the freeze does not happen in this
repo. GPX parsing stays Dart permanently for the same reason.
```
No other line in §2 changes (the "Rust Processing Rules" subsection, including the
`flutter_rust_bridge_codegen` instruction, remains accurate: `bridge/` still exists at the
frozen `idl0-final` pin, and an `idl0-maint` cherry-pick that touches the bridged surface still
needs that regeneration step).

- [ ] **Step 4: CHANGELOG entry**

Add to `CHANGELOG.md` under `## [Unreleased]` (alongside the existing 2026-09-02 freeze entry),
new `### Changed` bullet:
```markdown
- **CLAUDE.md §2 corrected (2026-09-03).** "Layer Separation" no longer describes an ongoing
  Dart→Rust migration; states the freeze explicitly and that the migration does not happen.
```

- [ ] **Step 5: Verify and commit**

```bash
grep -n "feature-frozen" "C:/Users/isaac/Documents/Saucy/saucyeng/idl0-app/CLAUDE.md"
```
Expected: one match, inside `### Layer Separation`.

```bash
git add CLAUDE.md CHANGELOG.md
git commit -m "docs: CLAUDE.md Layer Separation reflects the 2026-09-02 feature freeze"
git log --oneline -1
```
No branch created — this is Isaac's explicit instruction for this task; the commit lands
directly on idl0-app `main`, matching how M0 Task 2 Step 1 recorded the pivot itself.

---

### Task 5: CHANGELOG.md / TASKS.md — wave-1 kickoff entry and the per-lane completion template

**Spec discipline:** no spec change needed — CHANGELOG/TASKS discipline is explicitly L10's
scope (design §10 row), not a `docs/IDL0_SPEC.md` section.

Working directory: `C:\Users\isaac\Documents\Saucy\saucyeng\idl1-app`, branch
`wave1-l10-docs` (continue from Task 2's branch — same repo, same session).

**Files:**
- Modify: `CHANGELOG.md`.
- No `TASKS.md` change in this task — its wave-1 section is already correct (six unticked
  boxes; nothing has landed yet), confirmed rather than silently left alone.

**Interfaces:**
- Produces: the exact line templates Task 6 instantiates per lane once each is verified.

- [ ] **Step 1: Confirm TASKS.md needs no edit yet**

```bash
sed -n '/^## Wave 1/,/^## Wave 2/p' "C:/Users/isaac/Documents/Saucy/saucyeng/idl1-app/TASKS.md"
```
Expected: six unticked `- [ ] L<n> ...` lines (L1–L5, L10), matching the design §10 lane list
and R1 in `runs/2026-09-03/decisions.md`. Correct as-is; no lane has landed, so no box ticks
yet — recorded here rather than skipped.

- [ ] **Step 2: Add the wave-1 kickoff line**

Add to `CHANGELOG.md` under `## [Unreleased]` → `### Added` (after the existing M0-complete
bullet):
```markdown
- **Wave 1 started (2026-09-03).** L1 core `store/`, L2 importers, L3 core workbook v3, L4
  `idl-transport` desktop, L5 Tauri scaffold hardening, L10 docs (cross-cutting, this lane).
  Plans: `docs/superpowers/plans/2026-09-03-idl1-wave1-l{1,2,3,4,5,10}-*.md`. Decisions ledger:
  `runs/2026-09-03/decisions.md`.
```

- [ ] **Step 3: Fix the per-lane completion line templates (used by Task 6, not applied here)**

These are not applied to `TASKS.md`/`CHANGELOG.md` now — no lane has landed. They are fixed
here so Task 6 does not improvise wording under time pressure, matching M0's own practice of
writing `<date>`-templated CHANGELOG lines in Task 10 ahead of the day they're used.

`TASKS.md`, per lane once its Task 6 checklist row is clean: change
```markdown
- [ ] L<n> <lane name>
```
to
```markdown
- [x] L<n> <lane name>
```
exactly as printed in the existing `## Wave 1` list — no rewording.

`CHANGELOG.md`, under `## [Unreleased]` → `### Added`, one bullet per lane once clean,
matching M0's Task 10 style (bold lead-in with date, one-line summary, explicit SPEC pointer):
```markdown
- **L<n> <lane name> complete (<date>).** <one-line summary of what shipped>. SPEC:
  `docs/IDL0_SPEC.md` §<section(s)> <written|updated>.
```
Worked example (illustrative only — Task 6 fills in the real date and summary when L1 is
actually verified clean):
```markdown
- **L1 core `store/` complete (2026-09-05).** Arrow↔Parquet round-trip, CAS blob store, SQLite
  catalog + rebuild, `session.json`. SPEC: `docs/IDL0_SPEC.md` §15, §16.3, §18 written.
```

- [ ] **Step 4: Verify and commit**

```bash
grep -c "Wave 1 started" "C:/Users/isaac/Documents/Saucy/saucyeng/idl1-app/CHANGELOG.md"
```
Expected: `1`.

```bash
cd "C:/Users/isaac/Documents/Saucy/saucyeng/idl1-app"
git add CHANGELOG.md
git commit -m "docs: log wave-1 kickoff; fix the per-lane completion line templates for L10's own use"
```

---

### Task 6: Wave-1 SPEC verification pass (Group B — gated on L1–L5 landing)

**Dependency gate:** does not start until every one of L1, L2, L3, L4, L5 has landed (merged
to its target branch, reviewer verdict clean, tests green — design §12's per-lane gate). Check
before doing anything else in this task; if any lane is not yet landed, stop here and re-run
this task later. This is the last task in this plan — Group A (Tasks 1–5) has no dependency on
this task or on any other lane and may run, and land, at any time before or during wave 1.

**Spec discipline:** spec-during for §11 (App Architecture) — L10 polishes L5's draft in the
same pass that verifies it, per the scope line's explicit carve-out ("only §11 ... which L5
drafts first and you polish/verify"); "no spec change needed" for L1/L2/L3's sections — L10
reads and verifies them but never edits another lane's landed SPEC content (CLAUDE.md §7:
lanes touch only their own directory; a missing or thin section is reported to the lead, not
silently patched by L10).

Working directory: `C:\Users\isaac\Documents\Saucy\saucyeng\idl1-app`, branch
`wave1-l10-docs` (continue from Task 5).

**Files:**
- Read: `runs/2026-09-03/lanes/<lane>/BRIEF.md` for each of L1–L5; `docs/IDL0_SPEC.md`.
- Modify: `docs/IDL0_SPEC.md` (§11 only, small consistency edits); `TASKS.md`; `CHANGELOG.md`.

**Interfaces:**
- Consumes: each lane's BRIEF.md; `git log` on `docs/IDL0_SPEC.md`; Task 5's line templates.
- Produces: the status table (Step 5) and, for lanes verified clean, the TASKS.md tick and
  CHANGELOG.md line from Task 5's template.

- [ ] **Step 1: Confirm the gate and re-sync the branch**

```bash
cd "C:/Users/isaac/Documents/Saucy/saucyeng/idl1-app" && git status
sed -n '/^## Wave 1/,/^## Wave 2/p' TASKS.md
```
Confirm L1–L5 (not L10, that's this lane) are ticked or otherwise independently confirmed
landed by the lead before proceeding — TASKS.md ticks alone are not sufficient proof (that's
exactly what this task exists to check is trustworthy for the SPEC criterion; landedness
itself — merge + reviewer verdict + green tests — is the lead's gate per design §12, not
re-verified here).

- [ ] **Step 2: Locate each lane's BRIEF**

```bash
ls "C:/Users/isaac/Documents/Saucy/saucyeng/idl1-app/runs/2026-09-03/lanes/"
```
At this plan's drafting time (2026-09-03) that directory is empty — no lane has written a
BRIEF yet. Expected paths, by the same slug convention this plan's own BRIEF uses
(`runs/2026-09-03/lanes/l10-docs/BRIEF.md`):
```
runs/2026-09-03/lanes/l1/BRIEF.md   (or l1-<slug>/ — list the directory first; match by prefix)
runs/2026-09-03/lanes/l2/BRIEF.md
runs/2026-09-03/lanes/l3/BRIEF.md
runs/2026-09-03/lanes/l4/BRIEF.md
runs/2026-09-03/lanes/l5/BRIEF.md
```
Field to check in each: a line naming which `docs/IDL0_SPEC.md` section(s) the lane touched —
by the convention this plan's own BRIEF uses ("SPEC section(s) touched/verified"), grep
case-insensitively for `spec section` in each file since the exact label is not independently
confirmed for other lanes yet (see Open questions).

- [ ] **Step 3: Per-lane expected SPEC section and verification command**

| Lane | Expected SPEC section(s) | Verify with |
|---|---|---|
| L1 | §15 Session & File Model, §16.3 (Track Entity subsection), §18 Bike Profiles & Riders | `grep -n "^## 15\|^### 16.3\|^## 18" docs/IDL0_SPEC.md`; `git log --oneline -- docs/IDL0_SPEC.md` filtered to L1's commits; read the section text for leftover idl0 Flutter/Dart/Riverpod/`.idl0w`-as-JSON-blob wording that should be gone |
| L2 | A new importers section (number/placement is L2's own choice — not fixed by the design doc; see Open questions) | `git log --oneline -- docs/IDL0_SPEC.md` for a commit adding a new `## ` heading; confirm the heading matches whatever L2's BRIEF claims and is not a stub (>1 paragraph, covers FIT/GPX/CSV per design §10 L2 row) |
| L3 | §17a Workbook Entity | `grep -n "^## 17a" docs/IDL0_SPEC.md`; read for `.idl1wb`/C2 grammar content replacing the old `.idl0wb` v2 description |
| L4 | None expected — SPEC §6–8 (device/wire) are current and L4 implements against them without rewriting them (design §10 L4 row lists "Needs: SPEC §6–8", not "Produces") | Confirm L4's BRIEF explicitly states "no spec change needed" (CLAUDE.md §6) rather than being silently missing a section; this is the one lane where absence is the correct, verified outcome, not a gap |
| L5 | §11 App Architecture (draft) | `grep -n "^## 11" docs/IDL0_SPEC.md`; read for the same staleness this plan's own Task 1 checked CLAUDE.md against — no more Flutter/Riverpod/`flutter_adaptive_scaffold`/FRB language; the layer table matches CLAUDE.md §2 |

For each lane, also run:
```bash
git log --oneline --all -- docs/IDL0_SPEC.md | head -20
```
to sanity-check that the commit(s) claimed in the BRIEF actually touch the file (a BRIEF
claiming a section was written with no corresponding commit is not clean, regardless of what
the prose says).

- [ ] **Step 4: Polish/verify §11 specifically**

Read `docs/IDL0_SPEC.md` §11 (App Architecture) in full. Check, and fix only wording-level
inconsistencies (not structural rewrites — that was L5's job):
- The layer table names match CLAUDE.md §2's six rows and crate names exactly
  (`idl-rs`/`idl-transport`/`idl-rs-tauri`/`idl-rs-cli`, `app/src-tauri`, `app/src`).
- No reference to Flutter, Dart, Riverpod, `flutter_adaptive_scaffold`, or
  `flutter_rust_bridge` survives (all idl0-only).
- §11 does not restate facts that now live in another landed section in a way that could
  drift out of sync (e.g. `.idl0w`/`session.json` field lists belong in whatever L1 wrote in
  §15/§16.3/§18, not duplicated in §11) — if it does, this is a finding to report, not a
  unilateral rewrite of §11's structure (small wording fixes only; anything larger goes back
  to L5/the lead).
- §11 does not contradict §17a's workbook grammar or the new importers section's format list.

Any wording-level fix found is made directly (this is the one section L10 may edit); anything
beyond wording is logged in Step 5's table as "needs lead follow-up," not silently left or
silently fixed.

- [ ] **Step 5: Status table and TASKS.md/CHANGELOG.md updates**

Produce a table, one row per lane:

| Lane | BRIEF found? | SPEC section(s) claimed | Section(s) present in `docs/IDL0_SPEC.md`? | Content check | Verdict |
|---|---|---|---|---|---|
| L1 | | | | | clean / missing / thin |
| L2 | | | | | clean / missing / thin |
| L3 | | | | | clean / missing / thin |
| L4 | | | | | clean (no-spec-change confirmed) / unexpectedly missing a needed section |
| L5 | | | | | clean / missing / thin |

For every lane verdict `clean`: apply Task 5's templates — tick that lane's `TASKS.md` box,
add its `CHANGELOG.md` completion line with the real date and a real one-line summary (from
its BRIEF, not invented).

For every lane not `clean`: do **not** tick its box or add a CHANGELOG line. Do not write the
missing/thin SPEC content yourself (CLAUDE.md §7 — that lane's directory/section is not L10's
to write). Report it — to the lead in this task's own summary, and as a line item the lead can
turn into a `runs/2026-09-03/decisions.md` entry if it needs a ruling.

- [ ] **Step 6: Verify and commit**

```bash
grep -c "^- \[x\] L" TASKS.md
```
Reports the count of wave-1 lanes ticked (0–5, plus L10 itself once this task's own commit
lands — see Step 7).

```bash
git add docs/IDL0_SPEC.md TASKS.md CHANGELOG.md
git commit -m "docs: wave-1 SPEC verification — <clean-count>/5 lanes clean, §11 polished"
```

- [ ] **Step 7: Tick L10's own box**

Once this task's commit lands, tick `- [ ] L10 docs (runs alongside)` → `- [x]` in `TASKS.md`
and add its own CHANGELOG completion line via Task 5's template, in the same commit as Step 6
(L10 is the one lane allowed to mark itself done, since no other lane checks L10's SPEC
coverage — design §10's L10 row has no "Needs" column pointing at another lane's verification).

**Done when:** the status table exists (even if some lanes are `missing`/`thin` — that is a
valid, reported outcome, not a blocker to committing this task's own work), §11 has been read
and any wording fixes applied, and every lane found clean has its TASKS.md tick and CHANGELOG
line.

---

## Open questions

- **Lane BRIEF directory slugs** (Task 6 Step 2): assumed `l1`..`l5` (lowercase, matching this
  plan's own `l10-docs` slug), but each lane's own plan may have picked a different slug (e.g.
  `l1-core-store`). Not independently confirmed — no other lane's BRIEF exists yet at this
  plan's drafting time. Assigned to: **lead** (resolve by listing the directory at Task 6
  execution time and matching by `l<n>` prefix, already noted as the fallback in Step 2).
- **L2's new importers section — exact number and placement**: the design doc does not fix
  this (unlike L1/L3/L5, which get existing section numbers). Assigned to: **lead**, to confirm
  L2's own plan states its choice explicitly so Task 6 Step 3 has something concrete to check
  against, rather than L10 inventing a number itself.
- **Lane BRIEF field name for "which SPEC section(s) were touched"**: this plan assumes other
  lanes' BRIEF.md files carry a field comparable to the "SPEC section(s) touched/verified" line
  this plan's own BRIEF is asked to carry (per the instruction that produced this plan). Not
  confirmed for L1–L5 specifically. Assigned to: **lead**, to keep that field's wording
  consistent across all wave-1 lane briefs so Task 6's grep is reliable.
- **L4's "no spec change needed" claim**: this plan assumes L4 (`idl-transport` desktop) needs
  no `docs/IDL0_SPEC.md` edit because §6–8 (device/wire) are already current and L4 implements
  against them rather than defining new behaviour the SPEC should describe. Not verified against
  L4's actual plan (not yet written at this drafting time). Assigned to: **lead**, to confirm
  L4's plan agrees before Task 6 treats "L4 has no BRIEF SPEC-section line" as the *correct*
  outcome rather than an oversight.

---

## Self-review

**Spec coverage (design §10 L10 row):** "idl1-app SPEC (copy Parts 1–2/3–10; rewrite app
parts per lane)" — copying was M0 Task 3; rewriting per lane is L1/L2/L3/L5's own spec-first
work, verified here in Task 6, with §11 the one section L10 itself touches (polish only).
"CLAUDE.md layer rules" → Task 1 (confirmed, no change). "README" → Task 2. "CHANGELOG/TASKS
discipline" → Tasks 2 (per-task entries), 5 (kickoff + templates), 6 (per-lane completion
entries + L10's own). "fix idl0 CLAUDE.md §2 staleness" → Task 4. "Done when: every shipped
lane has its SPEC section" → Task 6's status table is exactly that check, made concrete
(grep + git log per lane, not a vague review).

**Placeholder scan:** the only `<…>` tokens are `<n>`/`<lane name>`/`<date>`/`<one-line
summary>`/`<section(s)>`/`<written|updated>` in Task 5's explicitly-templated lines (defined:
filled in by Task 6 from each lane's real BRIEF, exactly as M0 Task 10 templated `<date>`
ahead of the day it was used) and `<clean-count>` in Task 6 Step 6's own commit message
(defined: the count from that same step's table). No TBD/decide-later language.

**Cross-repo discipline:** Task 3's commit lands on the `rust` submodule's own branch, not
idl1-app's — the submodule pointer in idl1-app is not bumped by this plan (that's a separate,
later step for whoever integrates wave 1, not L10's job). Task 4's commit lands on idl0-app
`main` directly and never touches idl1-app's git history, matching the outer instruction's
explicit callout that this is "a DIFFERENT repo."

**Type/name consistency:** the renamed test functions in Task 3 are referenced nowhere else
(private `#[cfg(test)]` fns) — confirmed by Step 4's `git diff --stat` expecting exactly one
file, two lines changed. Task 5's templates are the same ones Task 6 instantiates — no
independent wording invented at Task 6 time.
