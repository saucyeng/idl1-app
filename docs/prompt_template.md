Agent: <name>

Spec disposition: <spec-first | spec-during | no spec change needed>
Relevant spec sections: §X.Y, §A.B
<Optional: 1–3 sentences of context the spec doesn't already cover>

Task:
<terse instruction — what to build / fix / change>

Constraints:
<things that aren't obvious from the spec>

Done checklist:
- [ ] <action> per §X.Y.A
- [ ] <action> per §X.Y.B
- [ ] tests pass for §X.Y (cite test file)
- [ ] spec updated if disposition is spec-during, OR no-change confirmed
- [ ] CHANGELOG + TASKS updated;
      SPEC updated if applicable;
      design_rationale updated if architectural;
      README updated if audience-facing description changed
