{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window, every platform",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "opener:allow-reveal-item-in-dir"
  ]
}
