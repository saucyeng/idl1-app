/** Placeholder — L7 (device/data/settings UI) replaces this with the
 *  device connection and source-config views. */
export default function DevicePage() {
  return <p>Device — built by L7.</p>;
}
