# L2 Task 6 review — post-import hook; `store::import::import_file`; Q2 Time-synthesis fallback

- Worktree: `C:\Users\isaac\Documents\Saucy\saucyeng\idl-rs-worktrees\wave1-l2-importers`, branch `wave1-l2-importers`.
- Commit under review: `b10d846c01208c96c1a649b83e2b4b4eb0380de8`.
- Files touched (matches the brief's `git add` list exactly): `core/src/import/hook.rs` (new), `core/src/import/mod.rs`, `core/src/session/handle.rs`, `core/src/session/synthesis.rs`, `core/src/store/import.rs`.
- Out of scope, present but not reviewed: the worktree currently has uncommitted WIP in `core/src/import/mod.rs` and `core/src/session/mod.rs` (an `ImporterInfo`/`IMPORTER_TABLE`/`importers()` addition for `list_importers`, R51 Q1/Q2 — evidently a later task's in-progress work). It is not part of `b10d846` and is ignored for this review; noted here only so it isn't mistaken for scope creep in this commit.

## Test command and result

Per CLAUDE.md §8 and the standing reviewer brief, reviewers do not build; the implementer's reported runs were verified by static inspection instead:

- `cargo test -p idl-rs import::` → reported 42 passed. Verified: `#[test]` count at this commit across every module whose path contains the substring `import::` (`import::hook` 1 + `import::mod` 4 + `import::gpx` 11 + `import::fit` 4 + `import::csv` 6 + `store::import` 16, since `store::import::tests::*` also matches the `import::` substring per the brief's own G2.2 note) = 1+4+11+4+6+16 = **42**. Exact match.
- `cargo test -p idl-rs store::import::` → reported 16 passed. Verified: `#[test]` count in `core/src/store/import.rs` at this commit = **16**. Exact match.
- `cargo test -p idl-rs session::synthesis::` → reported 5 passed. Verified: `#[test]` count in `core/src/session/synthesis.rs` at this commit = **5**. Exact match.
- End-of-lane gate `cargo test -p idl-rs -p idl-rs-cli -- --test-threads=4` → reported 894 passed / 1 ignored (idl-rs), 51 passed (cli). Not independently re-derivable without building; the three per-group counts above matching exactly gives high confidence the reported numbers are genuine, not fabricated.
- `cargo check -p idl-rs-cli --tests` → reported clean. Required by the R18-addendum rule since this commit adds a new `pub fn import_file`, a new `pub import_warnings` field, and new `ImportErrorKind` variants. Not independently re-run (forbidden); no `rust/cli/` file appears in the diff and the new symbols are additive (new fn, new enum variants, new struct field), so a `cargo check` regression there is implausible.

All three per-group counts reproduce exactly. No `0 passed` or filter-matched-nothing situation.

## Findings

| Severity | file:line | Finding | Fix |
|---|---|---|---|
| — | — | No Critical, Important, or Minor findings. | — |

## Checks performed (all pass)

- **L2-R12 (hook shape).** `core/src/import/hook.rs` ships exactly `PostImportHook` + `NoopPostImportHook`, no `import_with_hook`, matching plan:1630–1648 unchanged. Only test is `noop_post_import_hook_runs_without_panicking`, matching plan:1711–1727. `pub mod hook;` added alphabetically in `import/mod.rs` (`csv, fit, gpx, hook`).
- **L2-R13 write ordering (R18 addendum).** Traced `import_file`: extension lookup → `sha256_hex(bytes)` (hash only, no I/O) → `importer.import(bytes, &blob_sha256)?` (returns on `Err` before any write) → `synthesize_base_channels` (in-memory) → `NoopPostImportHook.on_imported` (in-memory) → `blob::write_blob` (first disk write, CAS) → `finish_import` (writes `data.parquet` then `session.json`). Blob is written strictly before `data.parquet`/`session.json`; catalog is untouched by this function. The malformed-input test (`import_file_malformed_gpx_leaves_no_orphan_blob`) asserts `!blob::blob_exists(&root, &would_be_digest)` after a `Err(ImportNotUtf8)` result — `blob_exists` is a real function (`core/src/store/blob.rs:54`) that checks the actual blob path on disk, not a stub — the assertion is a genuine proof, not decorative. GPX's own `std::str::from_utf8` rejection (`core/src/import/gpx.rs:44`) confirms non-UTF-8 bytes reach `ImporterError::NotUtf8` before any write is attempted.
- **`import_idl0` behaviour preserved.** Diffed `store/import.rs` line-by-line: the only removed lines are the ones directly replaced by the `finish_import` factoring (assigning `crate::parse::IDL0_IMPORTER_VERSION` → a passed `importer_version` parameter, and moving `truncation_warning`'s assignment out of the shared tail). No existing test body was touched, removed, or renamed in this file — every pre-existing `import_idl0` test is untouched, confirming outcome/warnings behaviour is unchanged.
- **`import_warnings` plumbing (G0.6).** `ImportReport.import_warnings: Vec<String>` added once; `import_idl0` populates it from `result.import_warnings.iter().map(|w| w.message.clone())`; `import_file` populates it from `outcome.warnings.into_iter().map(|w| w.message)`; both flow through `finish_import`'s `warnings` parameter, set verbatim on the returned report. `truncation_warning` is preserved separately (`import_idl0` sets it after calling `finish_import`, since only `.idl0` produces one) — not lost. A direct unit test (`import_idl0_populates_import_warnings_from_parse_result`) proves a non-empty `ParseResult.import_warnings` survives into `ImportReport`; a GPX-with-duplicate-timestamp fixture end-to-end test proves the same for `import_file`.
- **`ImportErrorKind`/`From<ImporterError>`.** All eight new variants (`ImportFitMalformed`, `ImportGpxMalformedXml`, `ImportGpxNoTrackpoints`, `ImportGpxMissingLatLon`, `ImportGpxUnparseableLatLon`, `ImportCsvMalformed`, `ImportNotUtf8`, `UnknownExtension`) are fieldless, matching the existing five. `From<ImporterError>` matches all seven `ImporterError` variants exhaustively (verified against `core/src/import/error.rs`'s actual definition) — a compiler-checked exhaustive match, no wildcard arm to silently swallow a future variant.
- **Q2 fallback.** `synthesize_base_channels`: when no channel has `nominal_rate_hz > 0.0`, falls back to the channel with the largest `len()` among all `session.channels` (first-encountered tie-break, matching the existing precedent above it), uses that channel's real `t_us` verbatim (not `i/rate`), and the synthesized `Time` channel's `nominal_rate_hz` stays `0.0`. The bail-out guard is narrowed to `max_rate_len == 0` only, as ruled. Two new tests cover the single-channel and multi-channel (longer-wins, non-monotonic-w.r.t.-index-order-safe) cases; `Distance`/`synthesize_distance_base` untouched (still gated on `GPS_SpeedKmh` at positive rate).
- **R61 (`session::handle.rs` restatement).** The one test edited (`resident_bytes_counts_columns_times_and_math_store`) is exactly the ledger-R61-authorized change: comment and assertion restated (240 → 400 bytes) to reflect that `from_channels` now also synthesizes a `Time` channel from the event-driven "E" channel's own `t_us`. Verified the arithmetic by hand: E (10×8+10×8=160) + synthesized Time (10×8+10×8=160, same length as E, the fallback's chosen source) + math-store "M" (5×8+5×8=80) = 400. Correct. No other line in `handle.rs` changed.
- **`import_file` end-to-end tests.** One per format (GPX/FIT/CSV), each using a minimal fixture engineered to also trigger a real importer warning (duplicate/non-monotonic timestamp), asserting `data.parquet`/`session.json` exist, `import_warnings` non-empty, and — after reading the parquet back and re-running `synthesize_base_channels` (matching C1 §2's "synthesized channels are not persisted" and mirroring `session_source.rs::load_session`'s real behaviour) — a `Time` channel is present. This is a correct proof of Q2 reaching end-to-end through the entry point, not a test papering over a missing persisted channel: C1 §2 explicitly rules `Time`/`Distance` are never persisted, so re-synthesizing on read-back is the only correct way to observe it post-round-trip.
- **`UnknownExtension`/C3 mapping.** Doc comment on the variant states the C3-mapping question is core-internal with no C3 §2 row, deferring the final IPC mapping decision to L5 Task 9 — this text is lifted verbatim from the brief's own L2-R13 ruling (not an invented assumption baked into shipped code), so it does not violate "do not assume it silently." The brief's own suggested mapping (`invalid_argument`, C3 §3.3's existing row for an unrecognised `importer_id`) is a reasonable candidate for that later ruling; nothing here forecloses it.
- **Signatures verified against real code.** `Importer::import(&self, bytes: &[u8], blob_sha256: &str) -> Result<ImportedSession, ImporterError>`, `importer_for_extension(ext: &str) -> Option<Box<dyn Importer>>`, `blob::write_blob`, `blob::blob_exists`, `atomic::sha256_hex` — all match `import_file`'s and its tests' usage exactly.
- **Hygiene.** Commit is a single line, no AI attribution trailer. `git add` used the five explicit paths named in the brief (confirmed via `git show --stat`) — no stray file. `Cargo.lock`/`Cargo.toml` untouched (no manifest diff). Diff is additive/surgical (347 added / 7 removed lines in `store/import.rs`, the 7 removed being exactly the lines the `finish_import` factoring subsumes) — no reformatting churn. Doc comments present on every new public symbol (`import_file`, `finish_import` — private but documented anyway, `ImportErrorKind`'s eight new variants, `ImportReport::import_warnings`, `PostImportHook`, `NoopPostImportHook`). No `Err(String)`, no unexplained `unwrap()`/`expect()` on production-path data (`import_file`/`finish_import` use `?` throughout; test-only `unwrap()` is fine per the standing brief). Tests named `thing — condition — result` (underscore-joined); Arrange/Act/Assert with blank lines confirmed by direct inspection of the new test bodies.
- **Downstream note.** The brief correctly required flagging, not silently absorbing, that this fallback now also fires inside `idl-rs-tauri`'s `session_source::load_session` (L5, out of this lane's gate). The implementer's report (per the dispatch summary) carries this flag forward; no `idl-rs-tauri` test run was added, correctly deferred.

## Verdict rationale

Every ruling in the Task 6 brief (L2-R12, L2-R13, Q2, R61, G0.6) is implemented exactly as specified, with no scope creep, no reformatting, and no behavioural drift in `import_idl0`. The write-ordering invariant is preserved and independently provable from the diff (parse-then-hook-then-write). The three per-group test counts the implementer reported are independently reproduced exactly by counting `#[test]` attributes in the relevant files at this commit, which is strong evidence the reported gate results are genuine. The `UnknownExtension`/C3-mapping ambiguity was correctly flagged rather than silently resolved in code. Nothing here needs a fix.

VERDICT: CLEAN
