# Review: L1 Task 13 — `verify` (C4 §7); `.idl0t` writer; bike-profile/app-settings persistence

**Worktree:** `C:\Users\isaac\Documents\Saucy\saucyeng\idl-rs-worktrees\wave1-l1-store`, branch `wave1-l1-store`.
**Commits reviewed:** `a91bf59` (Task 13 proper) on top of `ec2c82b` (one-line doc-comment fix, glanced) on top of `6ad093f`.
**Diff:** `git diff 6ad093f..a91bf59`

**Files touched (+/−):**

| File | +/− |
|---|---|
| `core/src/store/catalog.rs` | +14/−6 |
| `core/src/store/mod.rs` | +3/−0 |
| `core/src/store/profile.rs` | +227/−0 (new) |
| `core/src/store/settings.rs` | +177/−0 (new) |
| `core/src/store/verify.rs` | +386/−0 (new) |
| `core/src/track_artifact/mod.rs` | +5/−2 |
| `core/src/track_artifact/model.rs` | +79/−18 |
| `core/src/track_artifact/write.rs` | +150/−0 (new) |

All eight files are under `core/` (L1's own crate) — no cross-lane/`docs/` touches. Two single-line commit messages, no AI attribution trailers.

## Test command and result

```
cd core
cargo test -p idl-rs -- store::verify store::profile store::settings track_artifact::write store::catalog
```

**Result:** `test result: ok. 30 passed; 0 failed; 0 ignored; 0 measured; 628 filtered out` — matches the implementer's reported count exactly. Ran once, as instructed. (One pre-existing, out-of-scope `unused import: PathBuf` warning in `catalog.rs`, present before this task's diff at `6ad093f` already — not introduced here.)

## Findings

| Severity | file:line | Finding | Fix |
|---|---|---|---|
| Minor | `core/src/track_artifact/write.rs:104-135` | The round-trip test (`write_then_read_round_trips_every_field`) only exercises `LapTiming::Circuit`; `PointToPoint` is never constructed or asserted on the write side (read-side has its own `PointToPoint` test in `read.rs:84`, but that doesn't exercise `write_track`'s `From<&LapTiming> for LapTimingDto` conversion for that variant). | Add a second case (new test or an added assertion block) constructing a `Track` with `LapTiming::PointToPoint { start, finish }` and asserting both gates round-trip. |
| Minor | `core/src/store/verify.rs` | No dedicated test exercises check #2 (a malformed `session.json` reported as an error), #5 (a `derived/<hash>.parquet` whose filename doesn't match its content hash), or #8 (a `.idl0t` filename that doesn't match the `track_id` inside it) — only #1, #3, #4, and #10 have direct tests. The detection code for #2/#5/#8 is straightforward and mirrors #1's already-tested shape, so risk is low, but these are new logic paths with zero direct coverage right now. | Add one test per check, same shape as `verify_detects_a_corrupted_blob`. |
| Minor (informational, not a defect) | `core/src/store/verify.rs:54-68` (`check_blobs`) vs `:184-213` (`matches_layout`) | A blob file whose shard+name concatenation isn't a well-formed 64-hex digest (e.g. wrong length) can be reported twice: once as a #1 Error (`verify_blob` fails because the reconstructed `blob_path` doesn't yield matching content/length) and once as a #10 Info (`matches_layout` rejects the non-conforming name). Severities differ and both facts are independently true, so this isn't wrong, but it's untested and undocumented. | A one-line doc comment on `check_blobs` or `matches_layout` noting the overlap is enough; not worth restructuring. |

**Unlisted-but-judged deviation (not a finding):** `check_unexpected_paths` (#10) recurses the whole tree (`walk_unexpected`) rather than the plan's literal top-level-only draft. This is correct and necessary — C4 §7 #10's own text says "any path under `<data>`", which the plan's flat draft would have missed (e.g. a nested `.DS_Store` inside a session directory, which the task brief specifically asked me to check). It's tested (`verify_reports_unexpected_paths_recursively_including_nested_ones`) and correctly avoids descending into an already-flagged directory. Approved; flagged here only for the record since it wasn't in the implementer's self-reported list of extra changes (`track_artifact/mod.rs` doc fix, the E0505 field-reorder, the extra #4 test).

## Verification detail (R15/R16 cross-checks, all confirmed correct)

1. **R15 item 1 (overwrite correctness).** All three whole-document writers (`write_track`, `profile::save`, `settings::save`) compute `based_on = std::fs::read(&target).ok().map(sha256_hex)` (i.e. `Some(hash)` if the target exists, `None` if not) and pass it to `write_atomic_with_retry` with `rederive = |_current| bytes.clone()`. Matches `write_atomic`'s actual semantics (`core/src/store/atomic.rs:80-120`: `None` while target exists → `RenameConflict`). Each writer has an explicit overwrite test (`write_track_overwrites_an_existing_file_with_new_values`, `save_overwrites_an_existing_profile_with_new_values`, `save_overwrites_existing_settings_with_new_values`), each Arrange/Act/Assert, each writing twice and asserting the second content wins. No writer passes a bare `None`.
2. **R15 item 2 (`write_track`).** Two-arg signature `write_track(data_root: &Path, track: &Track)`; timestamps read from `Track.created_at_ms`/`updated_at_ms` (already on the struct from Task 12). Serialises via `TrackArtifact::from(&Track)` → the same private `TrackDto`/`LapGateDto`/`LapTimingDto`/`SectorGateDto`/`NeutralZoneDto`/`GpsFixDto` that `read.rs` deserialises, not a parallel `json!` literal — confirmed by reading `model.rs` side-by-side (wire field names match exactly: `lat1_deg`/`lon1_deg`/`lat2_deg`/`lon2_deg`, `latitude_deg`/`longitude_deg`, `kind: circuit|point_to_point`). Every field is populated by the conversion. The round-trip test covers id/name/venue/both timestamps/Circuit timing/sector gates/neutral zones/polyline (see Minor finding above re: `PointToPoint` not covered by this specific test).
3. **R15 item 3 (`profile.rs`).** Module doc cites "contract C4 §2 ... lead ruling R6", not "plan's own extension". `load_all` returns `ProfileLoad { profiles, skipped: Vec<(PathBuf, String)> }`. Zero `eprintln!`/`println!` in the diff (grepped). Sorted by `profile_name` ascending (`profiles.sort_by(|a, b| a.profile_name.cmp(&b.profile_name))`).
4. **`verify.rs` against C4 §7.** Severities match exactly: #1 error, #2 error, #3 warning, #4 error, #5 error, #8 error, #10 info; #6/#7/#9 deferred with an honest, correctly-numbered doc comment (note: this doc comment's numbering is actually *more* correct than the plan draft's, which mislabeled #6 as "workbook parse" — the landed code correctly attributes #6 to orphan detection and #7 to workbook parse, matching C4 §7's real numbering). `matches_layout` (verified against C4 §2's tree) accepts all of: `blobs/sha256/<2hex>/<62hex>`, `sessions/<id>/{session.json,data.parquet,derived/<64hex>.parquet}`, `workbooks/*.idl1wb`, `tracks/*.idl0t`, `profiles/*.idl0p`, `catalog.sqlite{,-wal,-shm}`, anything under `tmp/`; nested `.DS_Store` inside a session dir is flagged (tested). Double-reporting of a malformed blob filename by #1 and #10 is real (see Minor finding) — judged acceptable, not a bug. The implementer's judgment call — an unreadable/missing-key `data.parquet` metadata reported as a #4 error, while an *absent* `data.parquet` stays silent — is implemented exactly as described (`check_sessions`: `if !dp_path.is_file() { continue; }` before the metadata read; `Err(e)` from `read_data_parquet_session_fields` becomes a #4-shaped `Finding`), and is tested (`verify_reports_a_data_parquet_session_id_mismatch_as_an_error`; the "unreadable metadata" sub-case itself isn't separately tested, folded into the Minor coverage-gap finding above). No repair/quarantine behaviour anywhere in `verify.rs` — findings only.
5. **`catalog.rs`.** `DataParquetSessionFields`/`read_data_parquet_session_fields` are now `pub(crate)`; `session_id` added as a required field (`get("session_id")?`, no default) — confirmed C1 §4.3 lists `session_id` as an "Always"-present file metadata key, and `store/parquet.rs:211` writes it. No other catalog logic changed (diff is exactly this struct/fn visibility+field addition plus the pre-landed `ec2c82b` doc-comment fix). `catalog.rs:376`'s `write_atomic(…, None)` swap (R16) is untouched by this diff, confirmed by direct inspection — correctly left for its own queued fix-up.
6. **`settings.rs`.** Stages beside the file (`path.parent()`, passed as `data_root` to `write_atomic_with_retry`), not `<data>/tmp` — matches C4 §1's post-sign paragraph. `load` is lenient (`.ok().and_then(...).unwrap_or_default()` — absent or malformed both fall back to defaults, tested both ways). No `deny_unknown_fields` on `AppSettings` — unknown keys ignored by default serde behaviour.
7. **Hygiene.** No reformatting of untouched lines — `model.rs`'s +79/−18 is entirely: `Deserialize` → `Serialize, Deserialize` derives, doc-comment updates, five new `From<&T>` domain→wire conversions, and two `#[allow(dead_code)]` removals that are now genuinely dead-code-free because `Serialize` reads those fields. Doc comments present on every new public symbol (`Severity`, `Finding`, `verify`, `BikeProfile`, `ProfileLoad`, `load_all`, `save`, `delete`, `AppSettings`, `UnitSystem`, `load`, `save`, `write_track`, and all three `*ErrorKind`/`*Error` types). Typed errors throughout (`TrackWriteError`/`ProfileError`/`SettingsError`, each with an `Io`/`Encode`-style `Kind` enum, `Display`, `std::error::Error`) — no `Err(String)`. No `unwrap()`/`expect()`/`panic!`/`println!`/`eprintln!` outside `#[cfg(test)]` in any of the four new/rewritten files (grepped). All new tests follow Arrange/Act/Assert with blank-line separation and `thing — condition — result`-style names. Layer purity holds — no `tauri`/`tokio`/`async fn`/`reqwest`/`.await` anywhere in the new code (grepped).
8. **Unlisted changes beyond R15/the plan**, all judged: (a) `track_artifact/mod.rs` doc-comment update to describe the new write capability — correct, in scope. (b) The E0505 field-reorder in `check_derived`'s `Finding` literal (`message` computed and placed before `path` is moved) — a real, necessary borrow-checker fix (the plan's original field order doesn't compile once `stem`, which borrows `path`, is used inside `message`'s `format!` after `path` would otherwise have been moved) — correct. (c) The extra `verify_reports_a_data_parquet_session_id_mismatch_as_an_error` test for #4 — appropriate, matches R15 item 4's intent. (d) `check_unexpected_paths`'s recursion beyond the plan's flat draft — see "Unlisted-but-judged deviation" above; correct and necessary to satisfy C4 §7 #10's actual text.

## Verdict rationale

Every one of R15's four items (writer overwrite semantics, `write_track`'s two-arg/DTO-based shape, `profile.rs`'s C4-§2/R6 framing and `ProfileLoad`/no-`eprintln!`/sort order, and `verify`'s check #3/#4 implementation via the now-`pub(crate)` catalog helper) is implemented exactly as ruled, each with a passing, correctly-shaped test. `settings.rs` matches C4 §1's post-sign staging paragraph. The catalog change is minimal, additive, and doesn't touch the already-queued R16 bug. All 30 targeted tests pass in one run, matching the implementer's report. Hygiene is clean: no reformatting churn, doc comments everywhere required, typed errors, no bare `unwrap()`, correctly-shaped A/A/A tests, single-line commits with no AI trailers, no cross-lane or `docs/` touches. The three Minor findings above are real but narrow test-coverage gaps and one self-consistent (if undocumented) double-reporting behaviour — none would change a maintainer's decision to merge, and none contradict R15/C4 in a way that risks shipping wrong behaviour.

VERDICT: CLEAN
