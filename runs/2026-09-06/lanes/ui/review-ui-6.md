# Review: UI-6 — Data tab restyle

Commit: `ee02620` (`app: Data tab on the brand primitives, faceted browser layout (UI-6)`), branch `ui-6`, worktree `idl1-app-worktrees/ui-6`. Depends on UI-4 (present: `d43fddb`/`cc42ab8` in history) and the R77.1 pasted-path fix (`7c9a5f2`, ancestor).

Files touched: `CHANGELOG.md`, `app/src/routes/pages/Data/{ActiveChips,DetailPane,FilterRail,ImportPanel,LapTable,MaintenancePanel,MetadataForm,TrackDetailPane,TrackResults,index}.tsx`, new `layout.ts` + `layout.test.ts`. Nothing outside `Data/**` and `CHANGELOG.md`.

## Gate

```
cd app && npx tsc --noEmit && npx vitest run
```
`tsc`: clean. `vitest run src/routes/pages/Data`: 162/162 passed, 18 files (matches implementer's claim: 157 pre-existing + 5 new, non-zero). Whole-suite `npx vitest run`: 1005/1005 passed, 109 files (matches implementer's report exactly).

## Findings

| Severity | file:line | Finding | Fix |
|---|---|---|---|
| Important | `app/src/routes/pages/Data/index.tsx:426` (`selectSession`) | The brief calls this task "Presentation only: ... no change to any filter, facet, sort or import module" and "Do not change the selection model." This commit changes `selectSession` from always dispatching `SET_SELECTED_SESSION` with the clicked id to a ternary that dispatches `null` when the clicked row is already selected — i.e. it adds click-to-deselect on the gutter checkbox/row body, which did not exist before (verified: previous commit's `selectSession` had no ternary). This is a real interaction-behaviour change to `AppState.selection`, a cross-lane contract L6/Notebook reads (R53 Data Q3). It happens to restore idl0-parity (idl0's `SelectionNotifier.toggleSession` in `idl0-app/app/lib/providers/selection_provider.dart:92` does add/remove from a set), and the new comment says so ("Selecting the already-selected row closes it — the same toggle idl0's row tap/gutter checkbox both drive"), but it was made silently: not called out in the "Report back" template's normal fields, not flagged as a ruling question, and CHANGELOG's bullet describes only styling and the drag-drop addition, never this toggle. CLAUDE.md §1 and the brief's own scope say a behaviour question stops for a ruling, not a silent fix — even a correct one. | Either revert to pass-through (no toggle) and file the idl0-parity gap as a question for the lead per R53 Data Q3's owner, or (if kept) add one CHANGELOG line naming it as a deliberate parity fix with the ruling/citation, since it's a functional change to shared selection state that a lane boundary review needs to see explicitly. |
| Minor | `app/src/routes/pages/Data/index.tsx:142-144` (`SessionRowView` doc comment) | The doc comment states selection is "the existing single-session model (`AppState.selection`, unchanged by this restyle)" immediately above the row that now toggles-closes on re-click — the comment and the code disagree on whether this restyle changed the model. | Reword to acknowledge the added toggle-to-deselect behaviour, or drop "unchanged by this restyle" if the toggle is kept. |

No other findings. Specifically verified clean:

- **`layout.ts`** imports `resolveLayout` from `app/src/shell/layout.ts` rather than re-declaring 600/1200 px — the two ladders cannot drift. Boundary semantics (`< 600` narrow, `< 1200` medium, else wide) match the locked tests (600→panel, 1200→docked) exactly.
- **R55/R77.1 (pasted-path + Browse import seam):** `FilePicker.ts` has a zero-line diff (byte-identical to the parent commit). `ImportPanel.tsx` keeps `handleImportClick` (direct, verbatim `resolvePastedPath`, no dialog) and `handleBrowseClick` (`pickImportFile`, dialog) as two separate buttons, both intact.
- **Window-level drag-and-drop:** `getCurrentWebview().onDragDropEvent` is set up in a `useEffect` with an empty (data-only, mount-only) dependency array; its cleanup only calls `unlisten()`/sets `cancelled`, never touches the import queue or cancels an in-flight `runImport`. A stale-callback closure is avoided via `importerIdRef` rather than adding the callback to deps, matching the "callbacks live in refs" rule. Dropped paths dispatch `{type: "ENQUEUE", ...}` into the same `importQueueReducer` — no second queue. The overlay renders only `{dragActive && (...)}` with `pointer-events-none`, so it cannot swallow clicks when not dragging. It is window-scoped rather than results-panel-scoped, but the code comment explains why (Tauri's `DragDropEvent` fires for the whole webview, not a DOM node) — an honest, disclosed constraint, not a silent shortcut.
- **The queue-driving effect** (`useEffect(..., [state, onImported])`) is untouched by this commit except wrapping `dispatch` in `dispatchAndToast` (adds the toast side effect on `FAILED`, does not change what reaches the reducer). `onImported` (`handleImported` in `index.tsx`) is `useCallback`-stabilized over an empty-dep `loadSessions`, so it is a stable reference in practice; the effect has no cleanup that cancels in-flight work.
- **Toast:** exactly one call site (`showImportFailedToast` in `ImportPanel.tsx`), fired only from the `FAILED` branch of the dispatch wrapper, additional to (never replacing) the real reducer dispatch. Per-item error text still renders in `ImportRow`.
- **Selection bar reservation:** `DenseRow`'s permanent `border-l-[3px] border-l-transparent` / `data-[selected=true]:border-l-good` is used for session rows (`index.tsx`) and track rows (`TrackResults.tsx`); the inline lap sub-rows (`index.tsx:204`) carry the same `border-l-[3px] border-l-transparent` even though laps aren't independently selectable in this task (confirmed pre-existing, unmodified comment: "no lap UI in this task picks a main/overlay lap (deferred to L6...)"), so no layout shift occurs when a session with laps is selected.
- **Colour/style discipline:** no hex/`rgb()`/`rgba()`/`hsl()`/`hsla()` literals, no `box-shadow`/`shadow-` classes, no bare `text-accent`/`bg-accent`/`border-accent` (R94's collision) anywhere in the diff — only `text-brand-accent`/`border-brand-accent`. All icons are `lucide-react`. `BrandTableHeader` alias used consistently for both the shadcn `ui/table` `TableHeader` (`DetailPane.tsx`, `LapTable.tsx`) and the brand `DenseRow` `TableHeader` (`index.tsx`, `TrackResults.tsx`); no file imports both under one name, but the alias is applied uniformly across the module as directed.
- **Pure modules unmodified:** `importQueue.ts`, `importDriver.ts`, `maintenance.ts`, `lapDetailFormat.ts`, `trackDraft.ts`, `sessionRow.ts`, `filters.ts`, `facets.ts`, `sort.ts` are all byte-identical to the parent commit (`git diff` against each is empty) — the restyle did not touch any tested pure module.
- **Maps:** decision 36 (idl0 tile behaviour, no cache, no CDN change) is not touched — no basemap library added, nothing in this diff references map/tile code (DetailPane's map slot, if any, wasn't part of this diff's file list at all beyond styling wrappers already checked).
- **CHANGELOG:** the parity-gaps bullet (lap-mode selection deferred to L6, track-editor map deferred R54, has-GPS/has-gates/Track facets wave 3) matches the code and the pre-existing "deferred to L6" comment verbatim-in-spirit — honest, except it omits the click-to-deselect addition (see Important finding above).
- **Scope:** `App.tsx`, `App.css`, `main.tsx`, `state/`, `package.json`, other tabs — untouched (confirmed via `git diff --name-only` on the commit).

## Verdict rationale

The restyle itself is a clean, thorough presentational port: every changed component diff reduces to className/element swaps onto the brand primitives, all nine pure logic modules are byte-identical, the new `layout.ts` derives its breakpoints from the shell rather than re-declaring them, the drag-and-drop addition follows the operating brief's effect-safety rules to the letter and is honestly scoped to a webview-wide event, and the gate results match the implementer's report exactly (162 Data / 1005 whole-suite, tsc clean). The one real problem is a small, silent behavioural change — click-to-deselect on `selectSession` — introduced in a task explicitly scoped "presentation only" and "do not change the selection model," landed without a ruling or a CHANGELOG line naming it, even though it appears to be a legitimate idl0-parity fix on inspection. That is a scope/disclosure violation, not a functional defect, and it's isolated and easy to either keep-and-document or revert; it does not warrant NEEDS-REWORK.

VERDICT: NEEDS_FIXES
OUTPUT: C:\Users\isaac\Documents\Saucy\saucyeng\idl1-app\runs\2026-09-06\lanes\ui\review-ui-6.md
COUNTS: critical=0 important=1 minor=1
NOTES: Silent selection-model behaviour change (click-to-deselect) added outside the "presentation only" scope; appears to be a correct idl0-parity fix but needs a CHANGELOG line/ruling citation, not a silent landing.
