# Review — L11 Task 4: core `workbook::merge` (front matter + C2 §7.2 cell state table)

**Commits reviewed:**
- idl-rs `bdb24b4` on `l11-sync` (worktree `C:\Users\isaac\Documents\Saucy\saucyeng\idl-rs-worktrees\l11-sync`, based on `a3309a5`)
  — `core: workbook merge — front matter and cell states (L11)`
- idl1-app `ee482cd` (worktree `C:\Users\isaac\Documents\Saucy\saucyeng\idl1-app-worktrees\l11-sync`)
  — `docs: CHANGELOG bullet for L11 Task 4 workbook merge`

**Files touched:** `core/src/workbook/merge/mod.rs` (new), `core/src/workbook/merge/cells.rs` (new),
`core/src/workbook/merge/front_matter.rs` (new), `core/src/workbook/mod.rs` (+1 line, `pub mod merge;`),
`CHANGELOG.md` (idl1-app repo). No other files changed; `Cargo.lock` untouched (verified via `git diff a3309a5 bdb24b4 -- Cargo.lock`, empty). All files are on the brief's named list.

**Test command:** none run — Rust lane, cargo forbidden for reviewers. Verified statically instead:
`grep -c "#[test]" core/src/workbook/merge/cells.rs core/src/workbook/merge/front_matter.rs` → 11 + 6 = 17,
matching the implementer's reported "17 passed." Gate-entry commands (`git merge-base --is-ancestor 52efba8 HEAD`,
`grep -c "pub fn plan_sync" core/src/store/sync/diff.rs`) both re-run and both pass (`GATE-OK`, `1`).

| Severity | file:line | Finding | Fix |
|---|---|---|---|
| Important | `core/src/workbook/merge/cells.rs:204-413` | Brief demands "one test per row of C2 §7.2 that is reachable, named for the row." Four of the nine reachable real (base-has-id) rows have **zero** test coverage anywhere in the file, via `decide_cells` or otherwise: `(Unchanged,Unchanged)` → `KeepLocal`, `(Unchanged,Changed)` → `TakePeer`, `(Unchanged,Deleted)` → `Drop`, `(Deleted,Unchanged)` → `Drop`. Manual trace of `decide_one`'s match arms shows all four are implemented correctly per the C2 §7.2 table text, so this is a test-coverage gap rather than a logic bug, but it is a direct, named brief requirement left unmet. | Add four named tests exercising these rows through `decide_cells` (e.g. `decide_cells_unchanged_both_sides_keeplocal`, `decide_cells_unchanged_local_peer_changed_takepeer`, `decide_cells_unchanged_local_peer_deleted_drop`, `decide_cells_deleted_local_peer_unchanged_drop`). |
| Important | `core/src/workbook/merge/front_matter.rs:57-67,107` | Brief's Key Logic section: "`id`/`version` mismatch is `MergeError`, not a warning." Only `local.id != peer.id` is checked; `local.version`/`peer.version` are never compared, and the merged document unconditionally takes `local.version`. The code's own doc comment argues this is moot because a successfully-parsed `WorkbookDoc`/`FrontMatter` is always version 3 (parser is fatal otherwise) — a defensible reading, but it silently drops the brief's explicit instruction rather than adding the (cheap, always-passing today) defensive check the brief asked for, so a future relaxation of the parser's fatal-on-non-3 rule would let a version mismatch through unmerged-and-unreported instead of erroring. | Add `if local.version != peer.version { return Err(MergeError { .. }) }` (extend `MergeError` with version fields, or reuse existing fields with a documented convention) even though currently unreachable, matching the brief's literal instruction and guarding the invariant it currently relies on implicitly. |
| Minor | `core/src/workbook/merge/cells.rs:16-21` | `CellState`'s four variants (`Unchanged`, `Changed`, `Added`, `Deleted`) have no per-variant doc comments — only the enum-level comment documents them collectively. `CellOutcome` and `MergeWarning` in the same PR document every variant individually. Inconsistent with CLAUDE.md §5 ("doc comment on every public symbol") and with this PR's own style elsewhere. | Add a one-line doc comment per variant, matching `CellOutcome`'s style. |
| Minor | `CHANGELOG.md` (idl1-app, `ee482cd`) and `core/src/workbook/merge/cells.rs:394-412` | The CHANGELOG bullet claims "the six `Added`-crossed grid cells… are… unit-tested directly against the internal decision function." In fact only one of the six (`Unchanged×Added`, via `decide_one_an_added_by_peer_times_unchanged_by_local_pair_takes_peer_and_warns`) has a dedicated test; the other five (`Changed×Added`, `Added×Unchanged`, `Added×Changed`, `Added×Deleted`, `Deleted×Added`) have no test anywhere. The claim overstates coverage (CLAUDE.md §6: CHANGELOG must be true). | Either add the five missing `decide_one` tests, or narrow the CHANGELOG wording to name only the one cell actually tested and note the rest are implemented-but-untested defensive fallback. |

**Notes (not findings):**
- *MergeStateInconsistency widening.* `decide_one` emits `MergeStateInconsistency` on all six structurally-impossible cells, not only the two (`Unchanged×Added`, `Changed×Added`) whose warning text C2 §7.2 spells out explicitly. C2 §7.2 does not forbid extra warnings, and none of the six cells is reachable from `decide_cells`'s real execution path anyway (`side_state` never returns `Added` when `base` holds the id, so `decide_one` is only ever called from `decide_cells` with both states drawn from `{Unchanged, Changed, Deleted}`) — the widening is dead code from `decide_cells`'s perspective, documented as intentional symmetry in the source comments, and never drops data. No fix needed; flagging per the dispatch's specific ask.
- `peer_name` is threaded through `merge_front_matter`'s signature exactly as specified, correctly unused (`let _ = peer_name;`) and commented as reserved for Task 5's conflict-marker rendering. Matches the brief.
- The six structurally-impossible cells' chosen defensive outcomes (`KeepLocal` for the `Added`-row family, `TakePeer` for the `*×Added`/`Deleted×Added` family) all satisfy C2 §7.2's "never panic, never silently drop data" requirement on manual trace; none of them can affect `decide_cells`'s actual output today.
- Front-matter and cell interfaces (`CellOutcome`, `MergeWarning`, `MergeError`, `merge_front_matter`, `decide_cells`) match the brief's specified signatures and variant sets exactly, using the landed parser's own `WorkbookDoc`/`FrontMatter`/`CellDoc` types — no parallel type, no second scanner, no regex, no file I/O, no panics on malformed input, no `unwrap()` outside test code.
- `MarkedDeletion { deleted_by_peer }` attribution direction (`true` = peer deleted, surviving content is local's; `false` = local deleted, surviving content is peer's) matches C2 §7.2's Changed×Deleted / Deleted×Changed rows exactly, confirmed against both the doc comment and the two dedicated tests.
- Byte-identical-content-is-never-a-conflict (`conflict_or_keep`) is correctly applied to both `Changed×Changed` and the real `Added×Added` (id-collision) cell, each with a dedicated same-content and different-content test pair.
- Arrange/Act/Assert formatting with blank lines and `thing_condition_result`-style names (Rust identifiers can't carry em dashes) are followed throughout both test modules.

**Verdict rationale.** The core algorithm is correct wherever it was checked by hand against the C2 §7.2 table and §7.1's front-matter rule — no wrong outcome was found in any of the sixteen cells or the front-matter/constants merge, and the implementation is properly pure, typed, and scoped to only the files the brief named. The two Important findings are both about the brief's own explicit, named instructions going unmet (a required test-per-row list four short of complete, and a named "id/version mismatch → MergeError" rule silently narrowed to id-only) rather than about incorrect behavior; combined with a CHANGELOG line that overstates test coverage, this is a small, mechanical fix cycle, not a design problem.

VERDICT: NEEDS_FIXES
