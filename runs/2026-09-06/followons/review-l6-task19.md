# Review — L6 Task 19 (FFT chart, Steps 1–4)

Commit reviewed: `96cdd6f` on branch `wave2-l6-followon`
(worktree `C:\Users\isaac\Documents\Saucy\saucyeng\idl1-app-worktrees\wave2-l6-followon`).
Files touched (from `git show 96cdd6f --stat`): `CHANGELOG.md`,
`app/src/routes/pages/Notebook/host/SandboxHost.ts`,
`app/src/routes/pages/Notebook/host/protocol.ts` (+`protocol.test.ts`),
`app/src/routes/pages/Notebook/host/rebuildReplay.ts`,
`app/src/routes/pages/Notebook/model/fftDriver.ts` (+test),
`app/src/routes/pages/Notebook/model/fftRequest.ts` (+test),
`app/src/routes/pages/Notebook/sandbox/main.ts`, `docs/IDL0_SPEC.md`.
Scope matches the brief exactly: only `Notebook/**`, `CHANGELOG.md`,
`docs/IDL0_SPEC.md` §26.6/§26.7. No stray files.

Test command run once: `cd app && npx tsc --noEmit && npx vitest run
src/routes/pages/Notebook` → `tsc` clean; **Test Files 36 passed (36), Tests
336 passed (336)**. Matches the implementer's report.

| Severity | file:line | Finding | Fix |
|---|---|---|---|
| Note | `app/src/routes/pages/Notebook/model/fftRequest.ts:20-24` | Implementer added a named, exported `FftSegmentation` interface where the brief's Interface 1 used an inline anonymous type for `fftRequestFor`'s third parameter. Purely additive/cosmetic — the exported surface (`fftRequestFor`, `segmentCount`, `binFrequencyHz`, `frequencyAxisHz`, `fftRequestEquals`) is unchanged and Task 20's brief never references `FftSegmentation` by name. | None needed. |
| Note | `docs/superpowers/specs/2026-09-03-idl1-c2-workbook-v3.md` (R80 Q4, not this commit) | R80 Q4 places `spectrumKey`/`FftParams` in a dependency-free module under `plotForm/`, not beside `fftRequestFor` in `model/fftRequest.ts` as Task 20's brief Interface 4 originally specified. Task 19 does not implement `spectrumKey` at all (correctly — that is Task 20's Step 5), so this is not a Task 19 finding, but flagging it so Task 20's reviewer checks the brief's Interface 4 against the later R80 ruling. | Task 20's concern, not this commit's. |

No Critical, Major, or Minor findings.

## Verification detail

- **`fftRequestFor`**: `"none"` forces `window_size = hop_size = sampleCount`; every other averaging passes `segmentation`'s own window/hop through unmodified. Confirmed by reading the code and by the test suite (`fftRequestFor — averaging none — forces…`, `…ignores segmentation's own window/hop`, `…averaging mean — passes segmentation's window/hop through unmodified`). Edge cases (`sampleCount` 0/1, `hopSize <= 0`, `windowSize > sampleCount`) are pinned as documented — the function stays total/never-throws as the brief requires, deferring the "unrequestable" decision to Task 20's `jsCellBinding.ts`.
- **`segmentCount` vs core**: hand-computed 128 samples, window 32, hop 16 against `rust/core/src/fft.rs`'s `segment_count`/`resolve_seg` (`nperseg=32 < n=128` so `seg=32`; `noverlap = window_size - hop_size = 16` per `rust/tauri/src/commands/rasters.rs:154`'s `resolve_spectrogram_params`; `step = seg - noverlap = 16`; stepping `0,16,32,48,64,80,96` all satisfy `start+seg<=128` → **7 segments**). TS `segmentCount(128,32,16) = floor((128-32)/16)+1 = 7`. They agree — the TS pre-check and the server's `invalid_argument` will not diverge. The core function takes `nperseg/noverlap` while the TS function takes `windowSize/hopSize`; since `noverlap = windowSize - hopSize` always (`hop_size <= window_size` is enforced server-side before this point), `step` reduces to `hopSize` on both sides, so the two formulas are equivalent for every reachable input, not just this example.
- **`binFrequencyHz`**: `k * sampleRateHz / (2 * binCount)`, byte-identical to C3 §3.6's stated formula (`docs/superpowers/specs/2026-09-03-idl1-c3-ipc-surface.md:1062-1063`, `k * sample_rate_hz / (2 * bin_count)`, i.e. rfft bins with `binCount = n_fft/2 + 1`). `binCount === 0` returns `0` rather than dividing by zero, tested. Grepped the two new modules and `sandbox/main.ts` for other arithmetic: the only other numeric operation is `segmentCount`'s `Math.floor(...)`, which is segment counting, not DSP, and is exactly the function the brief asked for by name — no FFT/window/magnitude/averaging math exists in TypeScript.
- **`runFft` driver**: injected `fetchFft`; `isStale()` checked once after the single `await`, in both the resolve and the `catch` branch (`fftDriver.ts:80-89`); a typed `IpcError`-shaped rejection (checked via a structural `kind`/`message` guard) passes through with `detail` intact when present; anything else becomes `{ kind: "internal", message }`. Tests cover all five interleavings (resolve→dispatch, reject→typed dispatch, reject→internal dispatch, stale-after-resolve→no dispatch, stale-after-reject→no dispatch), each asserting the exact dispatched action array.
- **Spectrum payload**: `spectrumPayload` puts `f`/`m` in the transfer list exactly once each (tested via `transfer.filter(b => b === f)` having length 1, same for `m`); a second test proves bit-exact `Float64Array` round trip including `NaN` survival, mirroring `channelPayload`'s existing proof for the stated reason. `materializeHostVar`'s new `spectrum` branch builds `{f, m}[]` records exactly as specified. `SandboxHost.setSpectrumHostVar` posts directly via `postToSandbox`, bypassing the JSON-cached `setHostVar` path; its doc comment and `rebuildReplay.ts`'s updated comment both state the spectrum is excluded from cached replay for the same reason channel payloads are, and both say re-pushing after a rebuild is **L6 Task 20's** job — consistent with R80 Q5 (the host retains the decoded spectrum and re-pushes it on rebuild), and correctly *not* implemented by this commit, which only documents the intent.
- **No `ipc/` value import**: `fftRequest.ts` imports `DecodedFft`/`FftAveraging`/`SpectrogramParams` from `../../../../ipc/rasters` as `import type` only (grepped both new modules and `sandbox/main.ts`); `sandbox/main.ts` does not import `model/fftRequest.ts` or anything under `ipc/` in this commit (that wiring is Task 20's Step 8). Confirms the module is safe to import from the sandbox bundle later, as C2 §5.3/R80 Q4 require.
- **Judgment calls**: total `fftRequestFor` at `sampleCount` 0/1 matches the brief's own worked description almost verbatim and does not conflict with anything Task 20 depends on (Task 20's `jsCellBinding.ts` computes "unrequestable" from `sampleCount` before calling `fftRequestFor` at all). The named `FftSegmentation` type is additive and doesn't appear in Task 20's interfaces. Both acceptable (see Note row above).
- **SPEC**: §26.6's FFT row now reads "Contracted (C2 §5.3, R79); shipping in L6 Tasks 19–20" with an accurate reason column (contracted, not delivered — correctly not claiming delivery). §26.7's N5 clause is updated to say the wire landed and Task 19 built the plumbing, Task 20 remains for the surface. Both true against the diff. CHANGELOG bullet accurately describes what shipped and explicitly states the Step-4 stopping point.
- **Task 19 → Task 20 prerequisite check** (verifying Task 20's GATE will pass): all five listed prerequisites are present — `fftRequest.ts` exports `FftRequest`, `fftRequestFor`, `segmentCount`, `binFrequencyHz`, `frequencyAxisHz`, `fftRequestEquals` with tests; `fftDriver.ts` exports `FftDeps`, `FftAction`, `runFft` with tests; `protocol.ts`'s `HostVarPayload` has the `{kind:"spectrum"}` arm and `sandbox/main.ts` has the matching `materializeHostVar` branch; `SandboxHost.ts` exports `setSpectrumHostVar`; `ipc/rasters.ts` is untouched by this commit (Task 19 wrote no decoder/wrapper, as directed). No mismatch found between what Task 19 shipped and what Task 20's brief expects, aside from the R80 Q4 module-location point noted above (a later ruling on Task 20's own interface, not a Task 19 defect).
- **Tests**: all new/changed test names follow `thing — condition — result` with blank-line Arrange/Act/Assert, and each one asserts the specific rule it names (spot-checked every test in `fftRequest.test.ts`, `fftDriver.test.ts`, and the two new `protocol.test.ts` cases against the code paths they exercise — none are vacuous).

## Verdict rationale

The commit does exactly what the Task 19 brief's Steps 1–4 specify, no more and no less: the two pure modules match their Interfaces byte-for-byte in exported shape, the frequency formula matches C3 §3.6 exactly, the driver follows the lane's tightened staleness contract, the spectrum payload is a proper sibling arm (not a `t`/`v` reuse) with a bit-exact round-trip test, and the rebuild-replay exclusion is documented and correctly deferred to Task 20 per R80 Q5. Scope is clean (only the directed files), the gate passes with real non-zero counts matching the implementer's report, and SPEC/CHANGELOG claims are accurate and appropriately conservative (contracted, not delivered). The two Notes are informational only — one documents an acceptable, non-blocking naming choice, the other flags a later-ruling detail for Task 20's reviewer, not a defect in this commit.

VERDICT: CLEAN
