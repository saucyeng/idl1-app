# idl1 rulings digest — every ruling still in force (2026-09-06)

Read this instead of `runs/2026-09-03/decisions.md`. Each line ends with the
R-number(s) to open in the ledger. Superseded rulings appear only in their
superseding form.

## Process rules

- One cargo process on the machine at a time, across all worktrees, implementer or reviewer. (CLAUDE.md §8, R13)
- Machine-wide `jobs = 4` in `~/.cargo/config.toml`; never override with `-j`. (R13, R13-addendum)
- Gate steps run in the **foreground**; capture with `tee`, never rerun to re-capture. (L2 gate note, L2 LANDED)
- A task's test gate is the filter the dispatch names and must report a non-zero `passed` count. (CLAUDE.md §8, R13)
- `cargo test` filters are substrings of the full test path; `idl-rs-tauri` nests under `commands::<module>::tests::`, so name the module or the test-fn prefix. (L8w gate 1)
- Full suite `cargo test -p idl-rs -p idl-rs-cli -- --test-threads=4` every four tasks and at the lane gate; never `--workspace`. (CLAUDE.md §8, R13)
- `cargo check -p idl-rs-cli --tests` on any `pub` change in core; `cargo check -p idl-rs-tauri` when a command signature moves. (CLAUDE.md §8, R68, R75)
- Never `cargo fmt`, `cargo tarpaulin`, `cargo doc` inside a task; readers never build. (CLAUDE.md §7/§8)
- No cargo in a UI worktree; UI gate is `npx tsc --noEmit && npx vitest run <filter>`. (operating brief §4)
- **Never amend a reported commit** — once a hash is reported, fixes are follow-up commits. (L7a LANDED, operating brief §4)
- **One writer per worktree**: never dispatch into a worktree with an outstanding dispatch; a fix counts as outstanding. (process near-miss 2026-09-03, R13)
- A reviewer verifying "does it fail without the fix" uses a disposable copy, never a live revert/reset in the lane's worktree. (R11.2)
- **IPC-effects rule**: the decision logic of any effect that starts IPC or `postMessage` work lives in a pure, unit-tested module with an injected async function; the effect only calls it. (IPC-effects process rule 2026-09-05)
- Tightening: effects key their dependency array on **data only** (callbacks in refs), never cancel in-flight work in cleanup, and decide staleness with a monotonic sequence guard. (operating brief §4 tightening)
- A function prop in a dependency array beside a cancelling cleanup is graded **Critical on sight**. (operating brief §4 tightening)
- `CHANGELOG.md` conflicts of independent bullets under one heading: keep both (main's first), note "CHANGELOG: kept both bullets"; any other conflict is STOP and report. (R19 pattern, operating brief §4)
- **NUL-byte check** every file touched: `grep -c -P '[\x00-\x08\x0B\x0C\x0E-\x1F]'` must print `0` per file before commit. (L7a/L6 brief step, standing)
- **Amendment-ancestry check**: a brief citing an amendment names its commit hash; the implementer's first step is `git merge-base --is-ancestor <hash> HEAD`, merging main first if it fails. (L6 Task 20 process note)
- **Report cap**: implementer and reviewer reports fill the brief's template and stop — target 15 lines, hard cap 30; detail goes in the findings/commit. Briefs point at files and sections, they do not restate contract text or code. (operating brief §7.2, §7.5)
- Docs-only commits get a lead spot-check; every code commit gets a review. (operating brief §7.4)
- No new dispatch in the last 30 minutes before a known session reset. (operating brief §7.3)
- Every lane owns its own worktree, created with `git worktree add` plus `git submodule update --init -- rust`; the shared checkout stays on `main`. (R9, R10)
- The "remote error: upload-pack: not our ref" message during submodule init is expected with unpushed local branches, not a blocker. (R11.1)
- Ambiguity policy: stop and ask rather than infer; a stop on a real gap is correct behaviour. (CLAUDE.md §1, R54, R58, R65)

## Contracts

- **C1** `docs/superpowers/specs/2026-09-03-idl1-c1-session-schema.md` — session/channel schema; `Channel` carries `t_recorded_us: Option<Vec<i64>>` and `unit: String`; `session.json` gates are decimal degrees. (R5/R7, R8)
- **C2** `…-c2-workbook-v3.md` — workbook v3 grammar; `channel()` yields `{t, v}` records materialised in the sandbox. (R52 Q2)
- **C3** `…-c3-ipc-surface.md` — the IPC surface; the §2 error-kind table is authoritative over any §3.x entry that disagrees. (R57)
- **C4** `…-c4-data-directory.md` — `<data>` layout; `profiles/<id>.idl0p` is synced last-write-wins by `updated_at_ms`. (R6, R8)
- C3 is frozen for UI lanes; a needed command goes in the lane plan's "IPC needs" and builds against a typed stub throwing `not_implemented`. (operating brief §3)
- **R59** — 17 new commands + a §3.10 App group; `save_session_metadata` is last-write-wins with no `conflict` kind (Q1); quarantine deferred to wave 3 (Q2); new binary headers pad to natural alignment, `IDLH` 24 bytes / `IDLF` 16 bytes, `IDLT` unchanged and decoders copy rather than view in place (Q3); new kind `device_rejected` with `detail { ack: "busy" | "precondition" | "write_not_permitted" | "not_implemented" }` (Q4); `set_data_dir` is the sole writer of `data_dir` and `set_settings` ignores it (Q5); `device_status` mirrors `idl_transport::ble_status::DeviceStatus` field for field (Q6).
- **R60** — `import_file` resolves `ImportOutcome { session: SessionSummary, warnings: string[] }`; new §2 kind `import_collision`. (R60.1, R60.2)
- **R67** — `WorkbookEvent` gains `hash: string` (sha256 of the file's bytes after the change) so the UI suppresses its own saves; Rust-side `ExpectedHashSet` already suppresses, so this is defence in depth. (R67 + addendum)
- **R70** — prose crosses the wire as `prose_before_html: string | null`, `prose_after_html: string | null` and `prose_spans: { id, expr }[]` in document order; core's `find_inline_exprs` is the only span scanner; raw HTML in prose is escaped. (R70)
- **R79** — C2 §5.3 gains an FFT chart production: `windowSize`/`hopSize` accept `"all"`, hop is in samples, no bin-count grammar token (host-side cap with a note), y-label seeds "Magnitude (unit)" / "PSD (unit²/Hz)", one spectrum per cell in wave 2. (R79)
- `list_math_builtins` ships `{ name, arity: number[], status: "implemented" | "not_implemented" }`; `unit_rule` is dropped until C2 states unit propagation. (R64.2)
- `eval_workbook` takes `lap_context: { main_lap, overlay_laps[] } | null`; overlay laps are laps of the **same** session in wave 2. (R52 Q5, R64.1)

## Rust / L8w

- `device_rejected` ships as specified but is unreachable on the desktop BLE backend; map it only where an `AckCode` is really surfaced, never by parsing error text. (R63.1)
- `pull_config`'s `config` kind is likewise unreachable: btleplug's Windows `write` returns `Result<()>` and never yields the 0x81 ack. **Task 7b is withdrawn** — documented, not built, until a stack with ack readback (L9 mobile). (R71 + 2026-09-06 correction)
- `preview_channel_registry` covers the SPEC-fixed subset only (IMU, wheel, pressure, HR); configured analog/digital channels have no fixed wire id. (R63.2)
- Pressure channels 20/21 emit no preview row until SPEC §8 states their scale/offset source. (R64.5)
- `fetch_fft`: core `Averaging` gains `None` and `Max`; C3's union is `"none" | "mean" | "median" | "max"`. (R63.3)
- `averaging: "none"` requires exactly one segment — more is `invalid_argument` with `detail: { segments: n }`; a non-finite/non-positive rate or a channel under two samples is `invalid_argument` before any FFT runs; rate derivation `1e6 / median(Δt_us)` lives in core. (R76)
- Catalog SQL stays in `core::store::catalog`; `idl-rs-tauri` does not depend on `rusqlite`. (R68)
- Front matter is serialised by `workbook::v3::front_matter::render_front_matter`; no `format!`-built YAML anywhere in tauri. (R75)
- `overlay_laps`' first entry is the overlay window until lap indexing lands; `session_id: null` with a `lap_context` ignores it silently. (R73)
- C3 §3.4's "name sanitises to empty ⇒ `invalid_argument`" branch is unreachable with the shared sanitiser; kept as documented defence. (L8w gate 3)
- The `list_math_builtins` catalog is proven one-directionally (⊆ the real dispatch); the converse would need the duplicate name list R64.2 forbids. (L8w gate 4)

## Notebook / L6

- Properties + Code editor as designed (D13); the React Flow graph view is wave 3 and re-homes the same Properties form component. (operating brief, R52)
- Eight npm packages pinned by a lead shell task, `htl` at `1.0.0` added to the M0 pins table. (R52 Q1, R53 shell task)
- Editor cell segmentation is a narrow, non-authoritative TS fence scan; Rust remains the only evaluator. (R52 Q3)
- `read_workbook(id) → { markdown, hash, path }`; reading the file through a Tauri fs plugin is rejected (it bypasses `<data>` resolution). (R52 Q4)
- L6 designed the host-channel byte layout and filed it as a C3 §3.4 amendment. (R52 Q6)
- FFT chart is in wave 2; the 1-D histogram is wave 3. (R52 Q7)
- GPS trace draws on plain axes; no basemap, no tile server. (R52 Q8)
- L6 Task 5 was authorised, that task only, to add the sandbox entry to `app/vite.config.ts`. (R56)
- Cursor readout has its own settle instance keyed to pointer position (default 150 ms, named constant) with its own stale sequence; the viewport settle also refreshes it. (R62)
- Axis labels are suggested as `"<label> (<unit>)"` from C1's per-channel `unit`; no quantity→unit table in TS, and unit conversion is a wave-3 engine concern. (R65)
- js cells that `plotForm.parse` bind their channels and mount `ChartCell`; custom-code cells keep the plain mount. (R66.1)
- Inline-span errors use a distinct `spanError { spanId, message }` message, never an overloaded `cellId`. (R66.2, R78 Task 17 Q2)
- Cell outputs (js results, Plot SVG, Inspector values, span values) render **inside the sandbox iframe**; `cellResult.html` is removed and the host receives `cellRendered { cellId, heightPx }`. (R69.1)
- The host keeps gestures, tile cache, settle-bound fetch, cursor readout and the Properties/Code panes; during a gesture it posts `transform { cellId, translateXPx, scaleX }` per frame. (R69.2)
- `ChartCell` is the host-side frame around the iframe-rendered output. (R69.3)
- **Prose is the exception**: `prose_before_html`/`prose_after_html` come from core, are trusted, and the host renders them (the one permitted `dangerouslySetInnerHTML`); span values arrive from the sandbox and are inserted by `textContent` only. (R78 Task 17 Q1, revising R77.2)
- A span error shows in place as text in its span; no banner. (R78 Task 17 Q3)
- The bound-channel registry holds **every** channel of a js cell — `setBoundChannels(cellId, BoundChannel[])`, iterated on settle refetch and rebuild replay. (R72)
- Definition channels re-fetch on rebuild (SPEC §26.4 amended); a definition-only cell mounts `JsCellFrame`, a mixed cell keeps `ChartCell`; `has_t: false` is not bound. (R78 Task 18)
- `CellList` takes an optional `frame?: (cell, output) => ReactNode` wrapper so every cell kind is selectable. (R74)
- FFT: `x` is required in the FFT arm with a required `type`; bin cap 16384 checked before fetch and after decode; `spectrumKey` lives in a dependency-free module under `plotForm/` and nothing under `sandbox/` imports from `ipc/`; the host retains and re-pushes the decoded spectrum on rebuild; the first mark's channel survives a Time → FFT switch. (R80)
- Notebook empty state: rebuild the catalog once per page open only when `list_workbooks` is empty; picker only above one workbook and disabled while edits are dirty; Rescan reports `workbooks_indexed` and `duration_ms`; Notebook UI prefs live in a local `idl1.notebook.ui.v1` key. (R81)
- The Notebook never writes `selection`; the "no session selected — choose one in the Data tab" note stays. (R81 Q3)

## Data / Device / Settings (L7)

- **Data** — has-GPS/has-gates facets are wave 3 (C4 §5 + C3 §3.2 amendment). (R53 Data Q2)
- **Data** — `AppState.selection` is `{ sessionId: string | null, lapContext: { mainLap: number, overlayLaps: number[] } | null }`; L7a writes it, L6 reads it. (R53 Data Q3)
- **Data** — lap counts and lap tables render "—"/empty honestly; no wave-1 import path populates the catalog's lap tables. (R53 Data Q4)
- **Data** — sector count only in wave 2; `LapDetail.sectors` is pinned when lap indexing lands. (R53 Data Q5)
- **Data** — the Track facet is dropped entirely for wave 2, not shown disabled. (R54)
- **Data** — the pasted-path field stays an import path; the native dialog is a Browse button beside it. (R55, R77.1)
- **Device** — no wire arithmetic in TypeScript; wave 2 previews enable state, rate and unit only, via `previewSources()`. (R53 Device Q1, R55)
- **Device** — any positive integer for `analog.sample_rate_hz`; the SPEC §8 gap is restated, not guessed. (R53 Device Q2)
- **Device** — connection state is "last attempt succeeded"; six forms plus the add-channel picker is the correct count. (R53 Device Q4/Q5)
- **Device** — `adc_pin`/`gpio_pin` are `number | null` (`null` = unassigned, key omitted on serialise); an unassigned pin is a validator **error** so `isPushable` is false; no pin range is invented and the UI never auto-selects a pin. (R58, R55)
- **Device** — IMU form defaults 833 Hz / 32 g / 2000 dps are the form's initial state from SPEC §8's worked example, not a firmware claim. (IMU-defaults tracked note)
- **Device** — both IMU mode flags set emits a warning, not an error; `isPushable` unaffected. (IMU mode-flags tracked note)
- **Device** — `device_status` polls at 1 Hz on mount plus a `visibilitychange` pause; after 3 consecutive poll failures show "link lost?" and keep polling; profiles save explicitly with a dirty marker. (R77.4, R78 L7b Task 10)
- **Settings** — sync status and pairing live in Settings; L11 may add a sync action to Data. (R53 Settings Q3)
- **Settings** — `settings.json` wins over `localStorage`; only fields still at engine default are imported; `ui` keys stay in `localStorage`; migration and `set()` failures surface in a `role="status"` line. (R78 L7c Task 8)
- **Settings** — data-directory changes take effect on restart; the BOM strip lives in `paths::resolve_data_dir`. (R53 Settings Q4)
- Settings migration: `settings.json` wins; a field skipped because the file already held a different value is NAMED in the migration `role="status"` line (never silent) — L7c Task 9. R82

## Rust / L2b (landed 2026-09-06, idl-rs 81a7db3)
- `index_session` inserts its own `blobs` row in the same transaction (FK `sessions.blob_sha256`). R84
- `fetch_fft` with a lap: slice first, then the R76 guards on the window's own samples; no new kind. R85
- Multi-overlay math: `overlay: Vec<MathOverlay>`, `variance_*` fold = elementwise NaN-aware mean (fresh decision; idl0 had one overlay). R73 closed
- L2b lap indexing: laps are a stamped cache in `session.json` (`lap_detector_version` added to C1 §6, additive); unresolvable lap flags cleared on renumber; deterministic `visit_id`; re-index only on a stale stamp; R73 shape `overlay: Vec<MathOverlay>`; `fetch_fft` lap via `resolve_lap_window`; `rescan_tracks` in-lane. Plan `runs/2026-09-06/lanes/l2b-laps/`. R83

## Rust / L8x (landed 2026-09-06, idl-rs 52efba8)
- Track writes: one `save_track` (nullable `track_id`), `delete_track`; both return `stale_session_ids` (user triggers `rescan_tracks`); duplicate sector/NZ names allowed; decimal degrees on the wire; no track-editor map UI (R54). R86
- Quarantine: producer is `verify_data_dir(repair:true)` (C4 §7), never import; `resolve_quarantine` actions `restore|discard`; `verify_data_dir` lives in the App group. R86
- Catalog indexes workbooks: rebuild step 6 walks `workbooks/*.idl1wb`; `create_workbook`/`save_workbook` upsert their row. R87

## Sync / L11 (in flight)
- Sync lives in `idl-transport/sync/`; transport depends on core one-directionally; no new crates beyond `axum` (pinned) + `idl-rs` path dep; plain HTTP + per-peer bearer tokens, LAN-only posture. R88
- `data.parquet` version pair orders: importer SemVer, then seam `v<N>`; unparsable ⇒ incomparable, no transfer + warning. R89
- Manifest names blobs by their CAS path (no rehash); locally malformed files are a local-only skipped list and are do-not-touch in `plan_sync`. R90
- `session.json` per-field merge: zero-value = not set; both changed differently ⇒ newer `updated_at_ms`, tie keeps local. `render_workbook` is core's only renderer. R91
- **Ids: one validator in `core::store::sync::ids`**, allow-list per class + post-join containment check; rejects `C:foo` and friends. Bodies capped. R100

## Open items

- Isaac questions 1–5: IMU defaults; IMU mode-flag exclusivity; valid pin sets; whether the firmware can report SD free bytes / GPS fix quality / satellite count / battery millivolts; whether generic channel ids are deterministic from config order. (tracked notes, R58, R59 Q6, R63.2; sub-item 5b pressure 20/21 per R64.5)
- Isaac question 6: a user-configured basemap tile URL as an explicit off-by-default exception to "no CDN, ever". (R52 Q8)
- Isaac question 7: whether lap indexing should move up ahead of the Rust backlog. (R53 Data Q4)
- Isaac questions 8–9: a real FIT/GPX archive for the speed/heading direct path; which sample rate the firmware intends for `IMU2_AccelX`. (R23 Q4/R60, L5 landing)
- Isaac question 10 is disk headroom for Tauri builds; question 11 is closed (build caches removed, 16.9 GB freed). (2026-09-06 disk entries)
- **L10 cosmetic pass**: L6 Task 20's two Minors; SPEC `§26.x` cross-reference cleanup; `JsCellFrame` `minHeight` when a note or error is present. (L6 follow-on LANDED, L6 Task 21 LANDED)
- **Rust backlog**: lap indexing at import; incremental `catalog::insert_session` in place of a full rebuild per import; the cross-session overlay amendment (`overlay: { session_id, lap }[]`). Quarantine commands landed 2026-09-06 (L8x, R86) and are no longer on this list. (R51 Q4, R64.1, R59 Q2, R73)
- MathOverlay multi-lap shape is decided in the same amendment that ships lap indexing. (R73)
- Not yet observed by the lead: 60 fps pan/zoom and end-to-end chart rendering on a real session. (L6 follow-on LANDED)

*Contained decision: the digest omits wave-1 execution-fix rulings (R9–R12, R14–R18, R20–R50) whose subject matter has landed and is now recorded in code, tests and the contracts themselves; the process rules they produced are carried above.*
- **R109** — Editor reaches the studio properties column by `createPortal` into a slot node published through a shell context; state stays in the Notebook page; external hosting is decided by slot presence, never by measured width.
- **R110** — n-D math values are a C2 language extension; the SPEC section lands before any graph UI (spectrogram to peak-frequency is the worked example).
- **R111** — `Selection` becomes an ordered list of sessions with per-session lap context and colour; one representation, no single-session special case; C1/C3 change, done before the lanes that read it.
- **R112** — Calibration lives in the bike profile and binds to a session through the header's existing `config_crc32`; no firmware change, no second source of truth.
- **R113** — Device must report `LoggingElapsed: N` (seconds, monotonic, only while RUNNING) in the §7.3 status payload; firmware work, app degrades to dimmed client-side timing meanwhile.
- **R114** — Wave 3 runs W3.1 foundations, W3.2 maths graph + time/cursors, W3.3 errors + device, W3.4 bike sheet + PDF report (`runs/2026-09-07/WAVE3-PLAN.md`).
- **R117** — S1 selection: session-relative range origin; repeated sessionId legal; `_v2` commands with one deprecation revision; per-window eval (does NOT deliver cross-window maths/ghost delta — that is the R73 amendment); deleted-session windows dropped with a stated reason; colour is a `--chart-N` token; `fetch_host_channel` lap-context gap fixed in Task 5. `main_lap_window` indexing defect confirmed real.
- **R118** — n-D shapes: additive C3 error `detail`; `t`-alias kept through wave 3 and *migrated* (not just dropped) at the next version bump; `"t:lap"` unambiguous under per-window eval and `"t:win"` NOT specified (it is cross-window maths, R73 amendment); `fetch_raster(kind:"matrix")` deferred; iEKF components unnamed.
- **R119** — A `range` window that does not overlap its session is a typed `InvalidArgument` error, not a zero-width `(t,t)` tuple (inclusive slicing makes `(T,T)` select ONE sample, not zero); partial overlap still clamps. C1 §6.1's "empty window" sentence struck.
- **R120** — `range` windows with `t0_us >= t1_us` are `InvalidArgument` too (amends R119): C1 states the ordering as part of the type, and inclusive slicing turns an in-span `(T,T)` into a silent one-sample result. `session_span_us` in microseconds accepted.
- **R121** — `eval_workbook_v2` returns a per-window result (outputs or that window's error); window-attributable failures (bad session, unknown lap, R119/R120 range errors) fail only their entry, call-level failures (unknown/unparseable workbook) fail the call. C3 §3.4 amended; the lap-to-lap test must be able to tell the two windows apart.
- **R122** — A window must slice the `ChannelLookup` handed to evaluation (`SessionHandle::slice_by_time`), not just supply `MathLapContext` bounds: today bare channel refs evaluate over the whole session, so per-lap definitions silently return session-wide numbers. Slice, do not NaN-mask.
- **R123** (supersedes R122) — The window is the domain of **aggregation and display**, never of computation: definitions evaluate over the whole session (filters settle, integrators keep history), reductions (`max`/`mean`/`rms`/`variance_*`/single-spectrum `fft`) aggregate over the window, viewport is independent. Do NOT slice the `ChannelLookup`; extend window-awareness to every reduction.
- **R124** — All 11 scalar aggregates (`mean`/`max`/`min`/`rms`/`std`/`sum`/`count`/`first`/`last`/`median`/`p`) plus single-spectrum `fft` are window-scoped; do NOT ship a subset. Aggregate over an index subslice, never a NaN mask (NaN already means "gap", and masking allocates session-length per call). Rate-0 args unwindowed; `spectrogram` stays session-wide (retains a time axis, STFT is local).
- **R125** — Per-session evaluation cache deferred to a follow-up, keyed `(session_id, definition, workbook revision)` with **no window in the key** — valid precisely because R123 makes computation window-invariant. Trigger: the lap-time table, where per-lap columns cost O(N laps × session length).
- **R126** — Resolved windows are half-open `[t0, t1)`; `SpanDto::Session` must resolve to `[first_us, last_us + 1)` or a whole-session window silently drops the final sample (verified). Lap spans are gate crossings and already correct. R119 range clamping uses the same end.
- **R127** — Sandbox host vars stay keyed by definition name (never window-qualified); payload gains `w` (window index per sample) plus a `windows` descriptor carrying colour; a single window is byte-identical to today; **windows are separated by a NaN break** so pre-multi-window cells render n segments instead of one bogus joined line; `spectrumKey` gains the window index.
- **R128** — Half-open bounds require half-open overlap operators (`t1 <= start || t0 >= end`); and `window_index_range` must map any non-sentinel `start >= end` to an EMPTY range, never the whole channel — a range past the session end was returning the session-wide aggregate. Follow-up: replace the `(0.0,0.0)` gate-off sentinel with an `Option`.
- **R129** — (a) Amends R127 item 5: spectra carry the window dimension in the payload like channels (`{length,f,m,w}` + descriptor), `spectrumKey` gains NO window index, and C2 §5.3 needs no grammar change — the cell-side addressing gap disappears. (b) An interim shim may narrow scope, never widen it: `lapFromWindow` silently turned a `range` window into a whole-session FFT; a range span must fail visibly until Task 11 migrates the driver.
- **R130** — `resolve_window`'s `Lap` arm validates `start_time_secs < end_time_secs` (`InvalidArgument` otherwise), mirroring R120: an empty lap `(0.0,0.0)` from a corrupted `session.json` was indistinguishable from the "no window" sentinel and returned the whole channel. Detector cannot produce it; validation closes the trust boundary anyway. R128's `Option` follow-up still stands.
- **R131** — Per-window eval state mirrors the wire union (`Map<windowKey, {kind:"ok",outputs} | {kind:"error",error}>`); order stays in `AppState.selection` only; the reducer prunes deselected windows. The chart viewport is **window-relative** (offset from each window's own start, per decision 55), so overlaid laps align from their own starts; a shorter window renders absence past its end. `channelBindDriver` is Task 11b; until then >1 window raises a typed cell error rather than showing window 0.
- **R132** — With >1 window selected, non-chart cells (math, table, prose `${…}`, completions) may keep reading the primary window but MUST name which window they are showing, via `jsCellNote`'s existing `windowCount`; single-window output unchanged. Real multi-window layout for non-chart kinds deferred to the maths lane. FFT cell-shape check against the primary window only: filed follow-up (needs windows over different sessions).
- **R133** — An effect's dependency array and an inner identity/memo cache are two staleness gates **in series**: fixing the deps does not fix a cache that re-answers the same question. Channel-bind identity becomes per-(cell, window), mirroring the FFT effect that already worked. When fixing a stale-dependency defect, check every gate between the dependency and the work.
- **R134** — Time/cursor lane: sandbox is NOT on the pointer path (iframe is `pointer-events:none`), so cursor state lives in the host on a pub/sub bus, no new interaction-path messages. Strip = one stacked lane per window; shift-drag pans; dragging a lap boundary converts it to a `range` **with a visible label change**; viewport not persisted; distance-on-X ships disabled with its reason (core follow-on); playback stops at the primary window's end, named; hover drives the card, pin freezes it; optional `plotRect` on `cellRendered` is the only protocol change. The `endUs = Infinity` sentinel is removed before the strip exists.
- **R135** — Maths-graph plan accepted. Node positions live in an optional front-matter `graph:` key — the only home where a move is not a cell-content change (C2 §7.1 merges front matter per key, §7.2 compares cell bytes). Ports show **unknown** shapes as unknown, never guessed; the edge scanner reuses `tokenizeMath` (no second parser); rename is atomic; grey by reachability; node status worst-wins with the card naming the split, a whole-window failure being a canvas banner. **Pre-existing defect found: `render_front_matter` drops unknown top-level keys, silently stripping data on save (decision 75) — fixed in the lane's Rust task, ships regardless.**
- **R136** (supersedes R134 item 5) — Distance-on-X stays disabled in W3.2; a naive cumulative-distance axis is easy and **wrong for comparison** (line choice makes each lap's arc length differ, so overlays misalign by a growing error). Lap-distance alignment becomes its own spec-first **core** lane. Isaac's known failure mode: gates get noisy at switchbacks. Lead's recommendation to test, not assume: reference-path **station** projection with heading disambiguation + monotonic progression, which makes gates a derived quantity rather than a competing estimator.
- **R137** (amends R134 item 2) — Pointer gestures are a **swappable input map** (event → action) with runtime-selectable presets in user prefs: trackpad (pinch=zoom, two-finger=pan), two-wheel mouse (wheel=zoom, horizontal wheel=pan), basic mouse (shift-drag=pan). Decision 56's drag-to-zoom-region is the default in every preset. Renderer-only, never synced, never part of a workbook; the table is a pure tested module so a new preset needs no handler change.
- **R138** — "Resolved" has ONE definition: an effect's readiness gate must use the same code path as the consumer's inclusion test, never a parallel `has()` check. Third occurrence of the R133 shape (gate and work disagreeing about readiness), this time in the span dimension. Also: click-to-unpin blessed explicitly; lazy-init the cursor bus ref.
- **R139** — The cursor card reads the combined `{t,v,w}` payload the host already builds for the sandbox (retained in a ref, filtered by `w`), NOT per-window tiles and NOT primary-only. No new fetch, no IPC on hover, correct for N windows by construction; a pin may still do IPC for the exact readout (R134 item 7).
- **R140** — R135's "no second tokenizer" is about the *expression grammar*; a line-splitting layer above `tokenizeMath` is fine (precedent: `cells.ts` mirrors `scan_cells`), **provided comment/label detection comes from `tokenizeMath`'s own `comment`/`labelComment` tokens, never an independent `#` scan**. Gap filed: `tokenizeMath` does not tokenize string literals, so a `#` inside a string argument likely reads as a comment — reconcile with core's tokenizer when C2 §3.6 lands.
- **R141** — `ChannelSummary.channel_id` **is** the `[Name]` reference text (not a hash); `graphStatus.ts` reuses `jsCellBinding.ts`'s existing `findChannel`/`unresolvedChannelId` predicate rather than a third spelling of "resolves". Graph status takes **both** the ordered `SelectionWindow[]` and the resolved `windows` map: selection gives denominator and order, the map gives outcomes, absent-but-selected = pending (spinner). Never add a "pending" entry to the map — that gives absence two meanings again.
- **R142** — Recorded design criterion (Isaac): the math language is measured against **scipy/numpy/xarray** convention because *models are trained on it*. Catalog is already ~80% conventional. Priorities: (1) **false friends** like `angle` (vs `numpy.angle` = complex phase) are worse than invented names — rename first; (2) free renames `p`→`percentile`, `clamp`→`clip`, `if`→`where`, `integrate`→`cumulative_trapezoid`, `differentiate`→`gradient`; (3) C2 §3.6's named-axis reductions are **xarray** semantics — adopting `dim=` needs keyword arguments (open for Isaac); (4) `[Channel]`/`{cell}` refs kept as deliberate DSL. Cheapest to do now — renames are a version bump plus migration (decision 75).
- **R143** — Keyword arguments approved for C2 §3.2 (enables xarray-style `mean(x, dim="f")`). **Naming policy:** mirror scipy/numpy when semantically equivalent; when NOT equivalent, choose a deliberately different name — a false friend is worse than an invented name. **First application: `fft` actually computes Welch** (`core/src/fft.rs` is `welch()`), so rename `fft` -> `welch`, freeing `fft` for a real DFT. Plus `angle`, `p`->`percentile`, `clamp`->`clip`, `if`->`where`, `integrate`->`cumulative_trapezoid`, `differentiate`->`gradient`. Migration required (decision 75): old spellings parse one revision, rewritten on save.
- **R144** — `CellDefResult` gains `unit: string | null` (additive; `null` = genuinely dimensionless, never "unknown"). The engine already derives units (C2 §3.3 output-unit column, C1 §4.1 channel units) and drops them at the IPC boundary, so the node card, cursor card, inline `${…}` spans and the PDF report all show bare numbers. Consumers must never infer a unit from a name or label. Scheduled as its own Rust + C3 task.
- **R145** — A rename rewrites `[Name]` refs in math cells **and** form-generated plot code (safe: we generated it), **never** arbitrary JS in hand-written cells or prose `${…}` spans, and **reports every reference it could not update** ("renamed; 2 references in cell X not updated"). The broken-reference *consequence* is already handled by grey/unresolved rendering; the defect was that it happened silently. Refusing the rename is not the answer — decisions 45a/76 make it one gesture.
- **R146** (corrects R143) — The language `fft` is **not** welch: `eval.rs:940` is a single windowed magnitude spectrum, while charts call `welch` (`rasters.rs:508`) — **the notebook and the charts compute different spectra under one name today**. Fix: expose `periodogram` (single-segment) and `welch` (averaged) as scipy names them, retire `fft` and reserve it for a true complex DFT. `differentiate`→`gradient` withdrawn (backward vs central difference). Worst false friends are `variance_time`/`variance_dist` (lap deltas, not σ²). Two reachable panics filed (`clamp`, `butter`). R136's GPS-Doppler claim is unverified in-repo.
- **R147** — The maths lane landed 12 tasks and 126 tests but **wired only 2 gestures**: `graphEdits`'s five mutations are pure, tested and unreachable. Decision 76 measures the lane in *gestures*, so it currently scores near zero while every gate passes — unwired correct code is still correct code, so no gate can see this. **Rule: a task plan must land one gesture end to end early, and each capability must be reachable in the task that introduces it.** Every lane closes with an honest gap list (this is the second real find today). Lane merges as-is; 45a gesture wiring is the next dispatch, ahead of polish.
- **R144 amended** — `CellDefResult` gains `sample_rate_hz: number | null` alongside `unit`: the maths lane hit the same wire gap from the node-details side (only `ChannelSummary.nominal_rate_hz` exists; a computed definition has no rate on the wire though `eval.rs` constructs one). Second consumer to hit it — a contract defect, not a UI want.
- **R148** — Charts were blank because `bindingFor` requires `plotForm/parse` to recognise the **whole cell**; any unrecognised key (`height`, `opacity`, a `var()` stroke, a preceding statement) makes it "custom" and the host never binds or fetches its channels, so `channel(...)` returns `[]` **silently**. Two fixes: (1) an unparseable cell must show a note saying no channels were bound — the silence violates section D; (2) binding must be driven by **extracting `channel(…)`/`spectrum(…)` calls** (as `mathExpr` extracts refs) rather than parsing the whole cell, or hand-written cells can never receive data — which contradicts the runtime-editable premise.
- **R149** (amends R137) — Plain `wheel` is `"none"` in **every** preset: it scrolls the notebook, because the wheel's meaning belongs to the document and a chart that captures it makes the page unscrollable. Zoom moves to **`ctrl+wheel`** (the universal browser/map convention, cannot collide with scrolling). Horizontal wheel keeps `pan-x` (no document scroll competes for it). General rule: an embedded view may not capture a gesture owned by the surrounding document unless a modifier distinguishes it.
- **R150** — (1) `bindingForTime` is all-or-nothing (correct) but **silent** (not): one bad definition blanked a chart whose other channel was fine, with no message. Decision 58 applies — name which mark failed and why. R148 a second time. (2) C2 §3.6.7's worked example taught the bug (unbracketed definition references) — fixed; a worked example is executable documentation. (3) `classifyWheelEvent` never emits the new `ctrlWheel` verb, so ctrl+wheel zooms only via the `pinch` slot — accidental, filed.
- **R151** — All ten scipy-alignment questions accepted. Key: `periodogram` gets scipy's `density`/`spectrum` **plus** a deliberately-named `raw_magnitude` pinned by the migration, so **no existing workbook's numbers move** — adopting scipy names changes what things are called, never an existing result. Rename `hilbert`/`resample` now (both `NotImplemented` = zero migration cost); defer `butter`/`round`. One versioning axis (`version: 4`); rewrite on save only (opening is not consent to modify); an old name in v4 is a typed error naming its replacement. Scipy lane starts at task 2 — `engine-fixes` already holds the panic fixes.
- **R152** (corrects R144) — The engine does **not** know units: `ChannelValue` carries none, and C2 §3.3 states units only for named functions, with **no rule for arithmetic between channels of different units**. So: ship `sample_rate_hz` now; `unit` becomes its own spec-first task (a unit model for the operators, then propagation, then exposure); and **never ship partial units** — `None` cannot mean both "not applicable" and "unknown", and a wrong unit is worse than none. IMU flag validation moves to TS (`Device/config/validate.ts`) — no Rust config validation exists. Lesson: two consumers wanting a value is evidence it is wanted, not that it exists.
- **R153** — A `catch` (or a `null`/empty return standing in for one) must be **as narrow as the case it handles** and re-raise everything else; where impractical the broad path must fail *visibly*, never return a value that renders as ordinary output. All four blank-chart bugs today were this shape: `parse` nulling on any unrecognised cell, all-or-nothing binding discarding a good mark, lookups returning `[]` for unbound names, and `compileCell` swallowing an illegal parameter name and dropping the `return` so every cell rendered "undefined".
- **R154** — Unit model accepted: a small **symbolic algebra** (opaque C1 atoms → rational exponents, never canonicalised, never converted — catches `mm + m`, which a dimension vector cannot); inference is a **separate `Ast` pass**, so 38 construction sites and every `pub` signature stay untouched; a **three-state `UnitLabel`** (`known`/`dimensionless`/`unknown{reason}`) is mandatory, so R144's `string | null` must not ship. Mismatched `+` is a **visible on-cell diagnostic**, not a hard error (our own inference bugs must not break a user's file); `# unit:` annotations allowed; prose spans expose `.unit` rather than auto-appending. Live defect found: `cursorCard.ts:49` already ships the ""-means-both ambiguity.
- **R155** — Decision 70 (IMU channels all-or-nothing) stands; **SPEC §8's worked example is corrected**, not the policy weakened. The layers differ: the **wire permits** a partial channel mask and the importer must keep reading such files forever (Isaac has them), while the **app refuses to create one**. `validate.test.ts`'s fixture follows the corrected example. General rule: a spec example is executable documentation, so one violating current policy keeps teaching the violation — and here it silently anchored a test.
- **R156** — Decision 62's importer-version banner is unbuildable: `SessionSummary.importer_version` records what a session was imported with, but **no C3 command exposes an importer's current version** (`ImporterInfo` has no version field). Ruling: `ImporterInfo` gains `version`, additive, C3 amended — "`data.parquet` is a function of (blob, importer version)" is unenforceable if the current version is unknowable. Also: a shipped decision-61 violation was found and fixed (a deselected lap's spectrum lingered because `pushCombinedSpectrumFor` was not called after pruning).
- **R157** — Overnight results. Task 5 **struck** (its premise was false: R30 cut `migrate_workbook` from wave 1; the four real migration doors already cover a user workbook). Live device status (decisions 66/87) and HRM filtering (68) both need a **small Rust + C3 task** threading §7.3's new lines plus advertised service UUIDs into `DeviceStatus` — absent means unknown, never zero. Accepted: reordering kwargs before the `fft` split (its migration text would not otherwise parse), and retired-name errors now naming their replacement. Open: task 8 blocks task 12; C2 §3.3 count is now 72; R151 item 8 needs scheduling.
- **R158** — A status column reports **what the engine does**, never what the contract specifies. C2 §3.3 claimed `spectrogram` was `Implemented` (flipped by R110 on the strength of §3.6 *defining* its `[t,f]` value) while `catalog.rs` marks it `N` and `eval.rs` groups it with the unimplemented builtins — corrected. Specifying and implementing land in different commits; the engine is the source of truth, and the `list_math_builtins` parity check keeps the TS mirror honest.
- **R159** — A sync merge lost a cell. **Not** the rename normalisation (my hypothesis, corrected): `render_workbook` closed a fence with a bare ``` and relied on the next cell's `prose_before` already starting with a newline — an **unstated invariant** true of parsed cells and false of a merge-minted conflict copy, whose marker string has no leading byte. The fence fused onto the marker line and `scan_cells` swallowed the next cell whole. Fix adds the newline only when missing (R103's fixed point intact). **`cargo test -p idl-transport` joins the lane gate** — no lane ran it, which is how this reached main.
- **R160** — `migrate-workbook` is **struck permanently** (Isaac declined; the four existing migration doors cover a user workbook — do not re-propose). The graph's source palette is a **collapsible rail on the canvas**, not a studio column: it answers *which signal* where the Data tab answers *which session/lap*, and it is only useful while the graph is open (R107's lesson). Contents: raw channels grouped by prefix, workbook constants, and declared definitions. Search-first, drag-to-add via `addNodeFromChannel`, rows show name + rate (**omit the unit column until R154 lands** — a blank one invites guessing), union across selected windows with absent-in-some channels **greyed, never hidden** (decision 44).
- **R161** — Notebook gets a **CAD-style top bar** spanning the tab, above `ColumnFrame`, so the preview column runs **full height**. Leading group = column show/hide toggles (maths graph, properties/code, cells) — `ColumnFrame` already supports this via R107's optional-column work, and it generalises the Graph/Cells toggle. Everything else merges in: workbook selector, Save, content switch, playback, X mode, gesture preset. **Banners stay banners** (statements about the document, not controls) but move under the toolbar spanning the tab. One row, overflow menu rather than wrapping. Column visibility persists in `columnPrefs.ts` (renderer-only, never synced). Lands after the unit-model and palette lanes — all three touch `index.tsx`.
- **R162** — (1) `g` is substituted to `Ast::Number` at parse time, so inference cannot see it; treating it as dimensionless is **actively wrong** (`[accel] / g` would infer the accelerometer's units where the truth is dimensionless). Ruling: `g`/`pi`/`tau`/`e` survive parsing as **named constants** — add the `Ast` variant, report if the blast radius exceeds its match arms. (2) The design's function list predates the scipy renames; per-function unit rules become a **column in C2 §3.3** (where the prose already states them), making the engine table a transcription rather than a second source of truth — task 3 is now spec-first.
- **R163** — C2 §3.3's unit-rule column accepted (72 functions, §3.3.1 vocabulary). Rules **compose** (`Product`/`Quotient`/`PowN`/`SelectByLiteral` take other rules), so `[ch]·s` and `[ch]²/Hz` need no special kinds; `SelectByLiteral` expresses `periodogram`/`welch`'s three units by `scaling=` literal. **My brief was wrong**: `lap_delta_time` is `SameAsArg(0)`, not `Fixed(s)` — it is a delta of the channel. **Standing rule:** a unit rule derived from prose or an unimplemented function is **marked contingent** and re-verified at implementation (R158's discipline applied to units) — an unmarked guess becomes a fact by sitting still.
- **R164** — (1) `# unit:` joins C2 §3.2's existing trailing-comment annotation scan beside `shape:`/`label:`, same terminator rule; it is **authoritative** (an author can *correct* inference, not just fill a blank), and a disagreement with a `Known` inferred unit is a **visible cell diagnostic**, never a silent preference. Spec-first. (2) A host channel's unit attaches to `HostChannel` itself as an optional field — a sandbox-only wrapper would make two shapes for one concept; report if it reaches beyond the struct and constructors. (3) Conservative sibling propagation accepted: it under-claims, which is the right direction under R152.
- **R165** — The IDLH binary payload (`fetch_host_channel`, C3 §3.4) **stays samples-and-framing only**; a value's unit/rate/shape travel with its `CellDefResult`. Putting metadata in a bulk sample transport would force a byte-layout version for every future metadata field. C3 §3.4 must state the absence is deliberate — an unexplained absence is the shape R148/R150 removed. Accepted: `norm`/`angle_between` as algebraic simplifications of their §3.3.1 rules (same output, documented), and `pow`'s literal-exponent recovery limited to integers and halves (under-claims, per R152).
- **R161 amended** — Notebook column visibility lives in a **sibling** `Notebook/model/notebookColumns.ts` under its own key, not inside `shell/columnPrefs.ts`: that file's `ColumnId` describes the shell's four docked columns (`output` never collapsible), a different concept sharing a storage convention. Merging them would make one `ColumnId` mean two things by caller.
- **C2 §5.1** now documents `.unit`/`.unitState` as two non-enumerable properties on a `channel(...)` result — two, not one, because dimensionless and unknown are different claims that both render as no text (R152/R154). A prose `${…}` never auto-appends a unit (R154 item 5).
- **R166** — Report lane accepted (9 tasks, end-to-end at task 2). Charts reach the page by **host-side re-render** (the host owns props, data, theme — no user code run, no trust boundary moved); a sandbox→host **SVG string is rejected** because it reintroduces the `dangerouslySetInnerHTML` R69 removed. Recorded fact: the notebook column **cannot be printed** — chart containers are `position: fixed` in viewport px in a different document. Vendor jsPDF/svg2pdf/Plex TTF (no CDN); custom-code cells are a **named absence**; `# report: false` reuses C2 §3.2's annotation scan; save-dialog export, never cached under `<data>`; per-window sections + a comparison table; a Setup block that states the sheet is missing rather than appearing silently later. Live gap: `MathCell.tsx` still shows no unit/rate though both landed.
- **R167** — The raster wire adopts the **language's three scaling names** (`density`/`spectrum`/`raw_magnitude`) and exposes all three; the raster's old `Magnitude` **is** `raw_magnitude` (`fft.rs:454` is un-normalised `sqrt(avg_power)`). Two surfaces naming one computation differently is R143's false friend on a wire, and the old 2-vs-3 split let a user pick a chart scaling with no maths equivalent. `RasterMetaOut` gains the magnitude unit via `spectral_output_unit` (metadata path — R165 keeps `fetch_fft`'s bytes samples-only). Watcher flake **root-caused** (a mid-flush partial read scheduled a debounce that a later matching event never cancelled), fixed, and honestly reported as unproven without an injectable seam.
- **R168** — The app's `scaling` union keeps `"magnitude"` as a **fourth accepted name** (stored workbooks, generated cell text and the spectrum host-var key all carry it; the engine's serde alias makes it the same computation) but **no picker offers it**. Queued as its own UI lane: the FFT properties form still cannot spell `spectrum`, and `defaultFftPlotProps`/`plotForm/generate.ts`/`suggestSpectrumAxisLabel` still emit the retired name — that lane changes generated cell text and the host-var cache key, so it gates separately.
- **R169** — **One** session display-name formatter, in `state/selection.ts` beside `describeWindow` (whose caller-supplied `sessionName` parameter is the seam the copies grew in). Three had accumulated — `Data/sessionRow.ts::venueLabel`, `shell/topBarSelection.ts::sessionLabel`, `report/document.ts::sessionDisplayName` — each individually justified as "minimal, no coupling", disagreeing on the empty-venue text (`(none)` vs `Session`) and the date. The report is the surface where you cannot hover a chip to check. `venueLabel` moves there too so `(none)` is spelled once; it stays a separate function (venue-without-date is a real second need). Not a licence to centralise every string: the test is two surfaces naming the same thing for the same reader.
- **R170** — **No codegen for the IPC mirrors.** A full audit (58 commands, `runs/2026-09-09/MIRROR-PARITY.md`) found **zero `runtime-wrong` and zero `accepts-less`** — my "systemic drift" premise was wrong; the two stale mirrors I hit were the same week's in-flight lane work lagging by hours, which R159's gate already covers. Two real findings: `RenamedFunction.line` was documented 0-based but is **1-based** (fixed — a line number for a human, not an index), and **`set_sync_device_name` is unwired** (a live C3 §3.9 command with no TS reachability: the user cannot rename their device, so peers see the minted default). Queued for the Settings lane.
- **R169 amended** — The **fourth** copy (`Notebook/index.tsx`'s chart-legend `NONE_VENUE`) **is** folded in: a legend, a chip and a report line label the same window for the same reader, so the legend disagreeing with the chip above it is the exact failure R169 names. It had drifted because it holds a `SessionDetail` and `sessionLabel` demanded a whole `SessionSummary` — so `sessionLabel` now takes `Pick<SessionSummary, "venue_name" | "timestamp_utc_ms">`. **A signature that demands more than it reads manufactures its own copies.** Accepted consequence: the chart legend now carries the date (it should — telling two windows apart is its job). `report/document.ts`'s thin `windowSessionLabel` null-wrapper is fine: it makes no venue-or-date decision, so it is not a formatter.
- **R171** — **Never `git worktree remove --force` a worktree whose `node_modules` is a junction** — it follows the link and empties the *main* checkout's `node_modules`, surfacing later as a bogus "not the tsc command you are looking for" in an unrelated lane's gate. Unlink first: `cmd /c "rmdir node_modules"` (removes the link, not the target), then remove the worktree. Recovery is `npm ci` in `app/`; verify with `ls app/node_modules | wc -l` (136 today) before blaming a worktree for a toolchain error.
- **R172** — `set_sync_device_name` was unreachable (R170) and wiring it was blocked on an invisible gap: **nothing on the wire reports this device's own name**. `SyncStatusDto` gains **`this_device: { peer_id, name }`** rather than a new command — what peers see you as *is* sync status, `SyncSection.tsx` already polls it (no second lifecycle/error path/stale state), and `set_sync_device_name`'s previously-dead return value lets the pane update immediately and reconcile on the next poll. `peer_id` rides along because R104 forbids guessing a peer id and the app never displays this one's. The pane must **state that a rename is not retroactive** — an already-paired peer keeps the name copied into its peer file at pairing time.
- **R170/R172 closed 2026-09-09** — `sync_status` carries `this_device`, `ipc/sync.ts` gained `setSyncDeviceName`, and Settings → Sync has a **This device** block: editable name, selectable id, and the plain statement that a rename affects new pairings only. `syncState.ts` gained a `renamed` action so the setter's returned name shows at once instead of waiting for the next poll (a rename before the first poll is dropped rather than fabricating a `peer_id`). App gate 177/1757.
- **R173** — Report charts: `renderChart` takes **`ReadonlyMap<channelId, CombinedChannelPayload>`**, not the plan's literal single payload — `combinedChannelDataRef` is keyed `${cellId}::${channelId}` and multi-channel overlay is live on screen, so a singular signature would print a three-trace chart with one (R169's failure, with no hover to check it against on paper). `document.ts` gains the **`chartSlot`** block kind (the plan's R1 row already listed it; R2 stubbed it as absence to reach end-to-end) — *rejected*: `ReportView` post-processing absences, which would put cell-kind dispatch in a renderer beside the copy in `document.ts`. **Purity holds**: a `chartSlot` carries inputs, never an `SVGSVGElement`; `renderChart` is the only DOM toucher; charts stay host-rendered (R166/R69). Also folded in: `buildReportDocument` converts to a **single options object** — seven positional params, three of them same-shaped `Map`s, is a transposition bug that typechecks.
- **R173 amended** — **No jsdom.** The plan said R5 would be "tested against a jsdom SVG root"; jsdom is absent, vitest is `environment: "node"`, `include` is `.ts` only, coverage excludes `.tsx`, no pragmas exist — that config *is* CLAUDE.md §4 in tooling form. `renderChart.ts` splits: pure `buildPlotOptions(...) -> PlotOptions` (fully tested, all decisions live here) + a one-line `renderChart` that calls `Plot.plot(...)` (untested). **Doc-comment rule:** any conditional appearing in `renderChart` moves into `buildPlotOptions` — untested code making a decision turns "UI rendering is not unit-tested" into "this logic is not tested". Assert `PlotOptions` structurally, never a snapshot; do not stub `Plot` to half-test a one-liner. Twice this week the plan was corrected by the code — a plan is evidence about intent, not about the tree.
- **R174** — The report resolves **its own print palette and plot theme**, never the screen's. `ReportView` currently feeds dark-theme values (`--chart-3` amber, a `#353a32` grid, `#9a968a` axis text) onto a page `report-print.css` forces to black-on-white. **The deeper reason is the legend**: R1's selection block draws window chips from the same `--chart-N` tokens and R173 made it the report's colour key (Plot has no scale to legend against) — so charts and chips must come from **one** resolver called once per build, or a reader matches a chip to the wrong line, R169's failure *inside one document*. Series colours are **derived from the existing hues** (keep hue + slot order, adjust lightness to a stated contrast ratio against white, named constant with rationale), not replaced: there is no light palette (UI decision 5) and inventing one in a report lane is not this lane's call. Chart chrome follows the stylesheet already there. Values are explicitly a first pass, meant to be retuned.
- **R175** — `peer_appeared` carries a **tagged** `PeerSightingDto` (`status: "paired" | "discovered"`), not a flat `paired: bool`: `PeerStatusDto.paired_at_ms` is a fact an unpaired peer has not earned, and flattening would demote it to an `Option` meaning "wrong variant". Two events also rejected (every subscriber would interleave them). Event keeps its name. **The unraised risk, now ruled:** the loop fires today only for the few paired peers, so level-triggered firing would have gone unnoticed — widening it to every LAN device could make it a per-tick firehose. **Edge semantics preserved for both variants**, tested directly (a peer seen on two consecutive ticks fires once). If it turns out already level-triggered, that is a separate ruling, not a quiet fix. A peer pairing mid-session arrives as `Paired` next sighting.
- **R176** — **Finding: `peer_appeared` is level-triggered today** (fires per mDNS resolve, no comparison against `discovered`) — so R175's "preserve edge semantics" had nothing to preserve. **Ruling: emit on *change*** — on appearance, or when any DTO field differs (name/protocol_version/address/port); an identical re-resolve refreshes silently. Not the plain transition check R175 called for: an address can change while a peer stays visible and pairing needs it, so id-only edge-triggering trades a firehose for a **silent staleness bug**. Not level either: an event carrying no new information is a slow pane and a flat battery nobody traces back. Changes paired-peer behaviour (safe — one consumer, strictly fewer events, same information; offline stays poll-only). **C3 §3.9 must state the emission rule**, not just the payload.
- **R177** — `RasterMeta`'s `x_label`/`y_label`/`scale.vmin`/`vmax` are fetched and drawn nowhere (siblings of the `magnitude_unit` gap closed this evening), so a spectrogram has unlabelled axes and no colour meaning. **Split:** axis labels + vmin/vmax as **text** land now, app-only. A gradient colour-bar legend is **deferred** — drawing one needs the ramp, and the ramp is `idl_rs::colormap::turbo_rgba8`; reimplementing Turbo in TypeScript would put one colour ramp in two languages where the legend can drift from the pixels it describes and no test would catch it (R143/R169, in colour). When it lands, **the engine hands over RGBA stops** (~16, visually continuous) and TS builds a CSS gradient from stops it was *given*. Sampling the ramp out of a fetched raster's pixels is explicitly not an acceptable shortcut.
- **R178** — Gate 3's flake is **a 512 MiB allocation in a test**, not lane contention: `download_item_a_raw_file_tier_...` does `vec![b'a'; MAX_RAW_FILE_BODY_BYTES + 1]` (512 MiB), writes it to disk and serves it — four threads of that on a 16 GB swap-bound machine is the OOM. Reproduced on quiet `main`; passes alone, fails under `--test-threads=4`. **And it is unnecessary**: `bounded_bytes` refuses on `Content-Length` before streaming a chunk, so the test needs a response that *advertises* an over-cap length, not a real over-cap file. **Fixed, not silenced** — keep the assertion, change how the condition is manufactured; never shrink the production cap; the mid-stream branch needs its own small-cap test. Until then a named `TASKS.md` flake (rerun by name once, §8). **A gate that fails for unrelated reasons teaches every lane to disbelieve it.**
- **L11 Task 14 closed 2026-09-10** — TS mirror of `discovered_peers` and the tagged `peer_appeared` landed; "Nearby devices" prefills the peer id. Ratified two lane calls: a `paired` action removes the peer from `discovered`, and `pairForm.ts`'s stale "no discovered list exists" comment is gone. Poll replaces the discovered list wholesale (offline emits nothing, R176), TS never gates on `protocol_version` (Rust is the authority, `pair_peer` fails typed).
- **R178 closed 2026-09-10** — the 512 MiB allocations are gone: header-cap tests use a raw TCP peer lying about `Content-Length`; mid-stream tests stream chunked past a 4 KiB cap through a `download_item_with_cap` seam. Lane finding worth keeping: the raw peer must drain the request headers before responding, or the client sees an RST before it reads anything. Gate 3 has one named flake left (the watcher timing test).
- **R179** — R7 PDF export **parked** by Isaac; pipeline first. No PDF deps.
- **R180** — Mobile L9 **starts now** in parallel: read-only survey first (machine SDKs + code seams), cargo scaffold after.
- **R181** — **Opus lane-owner trial** approved: legend lane runs implement→review→gate→merge under an Opus owner; may self-rule on lane-local reversible choices (listed for ratification), must escalate contract/`pub`/spec/cross-lane with a proposed answer. Counts: escalations, self-rulings, reversals.
- **R182** — The **lead pushes `main`** (both repos, submodule first) after each green gate; hook rule removed, §7 updated. Lanes never push. No attribution trailers.
- **Watcher flake closed 2026-09-10** — the test's `std::fs::write` surfaced as Create+Modify and the first event could be hashed before bytes landed; tests now self-write via `write_atomic` and assert ordering. No watcher change. No named flakes remain in the lane gate.
- **R183** — L9 is **Android-only** on this machine (iOS = separate lane, needs a Mac); `gen/android/` committed; MulticastLock lands in-lane; BLE returns a typed `unsupported_platform` (new C3 §1 error kind); data-dir override desktop-only (C4 §1); min API 26 provisional; task 8 (mobile paper view) is TS-only and runs now.
- **R184** — Mobile paper view **is** the narrow `"sheet"` placement: static `renderChart` SVG, `cover`/`appendix` dropped on screen via a sibling filter, no desktop toggle, `math`/`table` taps no-op, every selected window, width-only detection (landscape gap named), theme inherited. Six-task TS plan accepted; second Opus lane-owner trial.
- **R185** — Mobile: **viewer and editor, alternating** (amends R184 item 4: `math`/`table` taps open the code editor in the sheet; paper task 7); the wide-screen side-by-side option **is** the existing `medium` layout, no toggle; **`paperTheme: "app" | "light" | "dark"`** as a second pref in the Settings prefs store, shown in ThemeSection (paper task 8).
- **R186** — Paper's duplicated light series colours **reversed**: one shared on-white series module imported by both `printPalette.ts` and `paperPalette.ts`; R185's clause means only "don't call `buildPrintPalette`". Trial note: the lane merged past its own escalation (harmless here, counted). Paper lane counts: 1 escalation, 7 self-rulings, 1 reversed. Tasks 7/8 in-lane ratified.
- **R187** — Wave 3 = **the data pipeline** (`ROADMAP-WAVE3.md`): CI first (gates off the dev machine), M4a phone front door + auto workbooks, M4b bucket transport as C5 beside LAN, M4c library (inbox, names from the log, metadata, batch re-import), M5 cross-session cells (C2), M6 setup sheet + rigid-body calibration validated on a simulated body; synthetic session generator in core; idl0 firmware untouched.
- **R188** — LAN sync is v1; bucket transport = **C5 spec draft only** for now. Firmware stays Isaac's (separate session). Phone is a **Pixel 8 Pro** (API 34; portrait ~412 px, landscape ~915 px → `medium` layout).
- **R189** — Cloud sync must scale to **~1000 users**, any sign-in: C5 = hosted **OIDC identity** + **stateless broker** minting presigned, prefix-scoped storage access + any **S3-compatible store** (`users/<sub>/devices/<peer_id>/…`). Merge stays client-side. Still design-only (R188).
- **R189 amended** — **Local this phase.** Forward-compat constraints on current work: sync behind the trait, prefixable content-addressed layouts, client-side merge, secrets in `app_config_dir`, sync triggers plural; C5 draft to R189's shape when a spec slot is free.
- **R177 closed 2026-09-10** — colour-bar legend from engine `ramp_stops` landed (both repos). Legend lane trial counts: **0 escalations, 5 self-rulings, 0 reversed**; ratified incl. `print-color-adjust` on the bar element (the print stylesheet never scopes a raster). R181 trial total across two lanes: 1 escalation, 12 self-rulings, 1 reversed, 1 process deviation.
- **R190** — Isaac's setup sheet received (`docs/setup-sheet/`): it is a **per-session snapshot** (Date/Bike/Rider/Session header), sectioned, model-dependent blanks = not applicable, units everywhere, an analysis input synced like a profile and queryable by M5. Spec-first when M6 opens; click convention to confirm.
- **R190 amended** — Setup parameters are **user-defined**, usable in the maths editor as **per-window scalars** (not workbook constants: windows span sessions), absent never zero. Clicks **from closed**. Sample original deleted; `docs/setup-sheet/` is the reference.
- **R190 closed** — Setup sheet = **start-of-session state, one number per session**; mid-session changes are notes. No open shape questions for the M6 spec.
- **R191** — M4c contracts written: C4 `inbox/` (desktop, delete on success, `failed/` on error, 2 s stable-size), C1 `timestamp_source` + user start in `session.json` only, C3 `set_session_start` / `scan_folder` / `list_stale_sessions` / `reimport_sessions` / `inbox_status`. Metadata editor and CAS dedupe already done. No bulk-import command: per-file `import_file` enqueues.
- **R192** — Rigid-body calibration draft accepted: two bodies + steering hinge, Wahba→LM in nalgebra; record in reserved typed read-only `calibration.*`; cross-body lever arm **authored** from sheet geometry; no wheel/bar sensors; `IMU2` on the rear body with a residual check; scale/non-orthogonality out of v1. Stays DRAFT until validated on the synthetic body.
- **R193** — Linux CI: `idl-transport` used a Windows-only `PeripheralId: From<BDAddr>`. BLE `device_id` is now the **Bluetooth address** everywhere; `connect` finds the peripheral by address via `adapter.peripherals()`. Second real portability bug CI has caught today.
- **R128 follow-up closed 2026-09-10** — `main_lap_window` returns `Option`; no `(0.0, 0.0)` sentinel in the maths path; the variance layer keeps its own `start >= end` convention at the boundary (self-ruling, ratified: pushing the Option further would widen `pub` scope). Sentinel lane trial counts: 0 escalations, 2 self-rulings, 0 reversed. R181 running total across three lanes: 1 escalation, 14 self-rulings, 1 reversed, 1 process deviation.
- **R194** — m4c escalation ruled: C1 §6 additive `timestamp_utc_ms` (only when source is `"user"`) + `timestamp_source`; C1 §2 `Session.timestamp_source` (not in parquet); C3 `StaleSession` versions are **strings**, `set_session_start` returns `SessionDetail` and updates the catalog row.
- **R191/R194 closed 2026-09-10 (M4c library landed)** — inbox watcher (2 s stable-size, delete on success, `failed/` + JSON `.error.txt`), `timestamp_source` + user start, `list_stale_sessions`/`reimport_session`, folder scan + "Import folder…" preview, maintenance-panel rebuild. `IpcErrorKind::UnsupportedPlatform` now exists in code (R183's kind); the Android lane reuses it, never redefines it. Ratified: watcher failure logs, never panics. Gates: core 1336, cli 53, tauri 333, app 188 files / 1898. Lean-owner mode (no implementer subagents, one reviewer) worked.
- **R195** — Delete audit (`runs/2026-09-10/DELETE-AUDIT.md`): no code path clears the data root, config dir or `blobs/`; the only blob delete is `delete_session(delete_blob: true)` after a reference check; verify is report-only; set_data_dir never moves or deletes. Dev and release share `com.saucyeng.idl1`, so past clearings were outside the app. Queued: (1) dev builds get a distinct identifier so `tauri dev` never opens the real data dir; (2) a source-scan test in `idl-rs-tauri` that fails if any non-test `remove_*` call appears outside the audited allowlist.
- **R196** — Data root: override may be Drive-mirror/external; **missing root fails loudly** (C4 §1); **`move_data_dir`** copies+verifies+rebuilds catalog then switches (C3 §3.10); dev identifier `com.saucyeng.idl1.dev`; delete-guard scan test. One library; subsets are views.
- **R197** — CLI gains `library fold-in|scan|stale|rebuild` over the same core functions as C3 (`--move` deletes source after hash verify); `move_data_dir` **moves** per file (C4/C3 amended); firmware **OTA lane** starts (transport has `/ota` + confirm; add C3 §3.8 commands + Settings Firmware; Flutter flow extracted first).
- **R198** — OTA design: sources = manual `.bin` always, GitHub Releases catalog from a Settings `firmware_repo` (empty = disabled; Flutter only had a placeholder), assets `idl1-firmware-<semver>.bin` + `.sha256`, stable/beta channels; state machine in `idl-rs-tauri` (push→reboot→reconnect 3 s×60 s→pending_verify→confirmed/rolled_back/failed); auto-confirm only for hash-verified catalog pushes; manual pushes wait for the user. Brief `runs/2026-09-10/BRIEF-ota.md`; queued behind the dataroot lane.
- **R196/R197 closed 2026-09-10 (dataroot landed)** — missing root fails loudly (Retry/Choose end at "restart idl1": `<data>` resolves once per process, ratified); `move_data_dir` moves per file, resumable (target accepted if absent/empty/only moved-tree names, ratified); dev identifier via a wrapper so `npm run tauri dev` is covered; delete-guard scan test pins every non-test delete site (it caught `delete_track` missing from the audit). Gates: core 1336, cli 53, tauri 350, app 190 files / 1918. **R171 incident:** `cmd /c rmdir` from bash did not unlink the junction; main `node_modules` was emptied and restored by `npm ci` (now 138 entries). Use PowerShell `Remove-Item` on the junction.
- **R199** — `app/src-tauri` compiled by nobody but `tauri dev/build`: the M4c inbox wiring landed with a `match`-arm type error that every crate gate and CI missed (Isaac hit it running dev). Fixed; CI gains a `cargo check` of the app crate; CLAUDE.md §8: any lane touching `idl-rs-tauri` or `app/src-tauri` runs that check at its merge gate.
- **R198 closed 2026-09-10 (OTA landed)** — `push_firmware`/`confirm_firmware`/`firmware_catalog`/`ota_state` + Settings Firmware; state machine in `idl-rs-tauri` with a `downloading` state; GitHub Releases client in `idl-transport` (wire rule, ratified); asset names amended into SPEC §27.7; review blocker fixed (auto-confirm never arms on an unknown version). Gates: transport 141, tauri 373, core 1336, app 1953. Watch CI: reqwest `rustls` pulls `aws-lc-rs` (cmake/NASM). R199 gate spelling: `cargo check -p app` (the hook refuses a bare check).
- **R200** — First launch on a fresh data root failed with `no such table: workbooks`: the catalog schema was only ever created by an explicit rebuild, so `open_catalog` on a new file left an empty database (the dev identifier from R196 exposed it; Isaac's real dir had never had a catalog either). `open_catalog` now bootstraps the schema when the file has no tables and `user_version` 0; populated files are untouched.
- **R197 item 1 closed 2026-09-10 (CLI library landed)** — `idl-rs library fold-in|scan|stale|rebuild` (`--move` deletes source only after hash verify, never renames; `--recursive`, `--dry-run`, `--json`); no core change. Gates: cli 61, core 1337. Two full-suite runs died in a rustc crash under memory pressure while Isaac's dev build ran alongside; third passed. Ratified: one catalog rebuild per fold-in run; start times printed as raw UTC ms.
- **R201** — Folder import froze the app: Tauri runs sync commands on the main thread and `scan_folder` hashed 6.9 GB. Rules: heavy commands are `#[tauri::command(async)]` (audit all, C3 §1 sentence); `scan_folder` never hashes (`already_imported: null`); global import-status chip in the top bar; per-row selectable preview; "Stop after current". Amends R191. Brief queued behind the fold-in build.
- **R202 (2026-09-10 evening, Isaac away)** — Three bugs from his first real run. (1) **OOM** opening a multi-hour session in the debug app (`memory allocation of 60391864 bytes failed`): memory survey dispatched (`MEMORY-SURVEY.md`), then a fix lane: no duplicate copies of a channel in flight, and a failsafe that degrades (refuse/stream) rather than aborts. (2) **Maths graph column shows a placeholder** though `graph/` is built and tested and mounted nowhere: graph-wire lane (TS-only) mounts it per R160/R161. (3) **"Shakedown" workbook missing**: it exists, in the release identifier's override root `C:\tmp\idl1-data-l5` (3 workbooks, 1 session, 15 MB); the dev identifier has no override so its library is empty. Plan: library root `C:\Users\isaac\Documents\idl1-library`; fold the archive in with `--move`; move the l5 root's workbooks/session/track into it; point BOTH identifiers' `settings.json` `data_dir` at it; relaunch. Lead does this tonight.
- **R203** — OOM root cause (`MEMORY-SURVEY.md`): **no session cache**; every tile/cursor/workbook call decodes the whole `data.parquet` for one channel; import holds ~3 full copies (raw read + parsed + Arrow) with no size check. Rulings: parquet **column reads**; a bounded LRU **`SessionCache`** in tauri state (min(2 GiB, 25 % RAM)); import via **mmap** + per-channel batch writes; **`resource_exhausted`** C3 §1 error kind, never abort; `combinedChannelDataRef` evicts. Brief `BRIEF-memory.md`, queued behind the async lane. Debug profile is not the cause (core already opt-level 3 in dev).
- **R202 item 2 closed 2026-09-10 (graph column)** — the placeholder was the shell's leftmost maths column (`RouteHost.tsx`), not the graph itself, which was already mounted in-page. Fix mirrors R109's slot-and-portal: `shell/graphSlot.ts` + `GraphSlotColumn.tsx`, page portals its single `GraphCanvas`; `model/graphHost.ts` decides by slot presence, never width. Gate 194 files / 1965. No spec change.
- **R201 closed 2026-09-10 (async lane landed)** — 46 commands `#[tauri::command(async)]`, 6 left sync (constants/managed state only); `scan_folder` never hashes (`already_imported: null`; CLI keeps a blob-checking variant); import chip lives in `AppShell` (top bar hides on narrow); "Stop after current" drops the queue and lets the running import finish. Gates: tauri 373, core 1339, cli 61, app 195 files / 1985. Ratified all. Note: stale `.rmeta` in the shared target dir after a concurrent build; rebuild fixed it.
- **R204 (disk full, 2026-09-10 evening)** — C: reached 0 bytes free (459 GB used) mid fold-in; the fold-in process died on an allocation failure (pagefile cannot grow on a full disk) after 71/193 imports, 0 import failures, sources removed only for verified imports. Library is 6.6 GB (blobs 3.5, parquet 3.1). The CLI's `--data-dir` is the `<data>` folder itself, not the C4 root; the output was moved under `idl1-library\data\` by rename. Cargo is blocked machine-wide until space is freed; the 5.45 GB `.cargo-shared-target\debug\incremental` cache is the obvious reclaim, **awaiting Isaac** (a lane's delete was denied by its permission classifier and the lead does not launder that). Resume: `idl-rs library fold-in <sessions> --data-dir <lib>\data --move` (idempotent).
- **R203 closed 2026-09-10 (memory lane landed)** — parquet column reads, bounded `SessionCache`, mmap import with per-channel batch writes, `resource_exhausted`, app-side eviction. Import of a 377 MB log: peak private bytes 4,939 MB → 3,542 MB. Manual proof caught a real bug: `HR_RR` timestamps repeat and step backwards (C1 §3.5 invariant 1 fails on a real source); column writer now falls back to a search, two regression tests. Gates: core 1359, cli 61, tauri 388, app 196 files / 1993. Open (in TASKS.md): eval host-channel path still decodes whole sessions; `finish_import` keeps the parsed Session for lap indexing; rebind-retention keyed by host-var names. App-side pan proof needs a person at the window (Isaac).
- **R202 item 3 closed 2026-09-10** — library root is `C:\Users\isaac\Documents\idl1-library` for BOTH identifiers (`settings.json` `data_dir`, old value backed up as `.bak`); the old `C:\tmp\idl1-data-l5` root's three workbooks (shakedown among them), track, session and blob were copied in. Archive fold-in resumes on the memory-lane importer (release CLI rebuilt first); the earlier resume died on the old importer's allocation spike while lane tests ran.
- **R205** — UI-thread survey (`UI-THREAD-SURVEY.md`): no Web Worker exists; tiles/tiers are point-budgeted, but three synchronous passes run per bind/settle (`tileToChannelData` collect-then-downsample, sandbox `materializeHostVar` per-element objects, `protocol.ts` window combining). Lane **queued, not dispatched** (Isaac: "maybe a separate project for later"): worker-ise decode/combine, yield between `channelBindDriver` phases, audit `renderChart` for full-resolution touches. Dispatch when the pipeline work is quiet.
- **R206 (host memory, 2026-09-10 night)** — Every "OOM" today was one cause: the machine sits at its **commit limit** (15.7 GB RAM, 0.4 GB free; 63.7 GB commit with ~1 GB free; pagefile system-managed and unable to grow on the full disk). 394 processes hold ~25 GB private: VS Code 6.8 GB/24 procs, 16 leftover `claude` processes 3.4 GB, Spotify 2 GB, an idle ipykernel 1.1 GB, 16 powershell 1 GB, stale `dart` 0.5 GB. A 60 MB allocation failing in `parse_v3` is this, not the importer (the new importer needs 3.5 GB peak for a 377 MB log, which the box cannot give right now). VS Code's crashes are the same pressure. **Action:** reboot before the fold-in resumes (idempotent: `idl-rs library fold-in ... --move`), then run heavy jobs one at a time; the lead's `up.ps1` waits on compilation but not on memory, so a memory check joins it.
- **R207 (2026-09-11)** — Archive fold-in complete: 159 sessions in `idl1-library` (87 this run, 35 already present, 1 collision on a reference file duplicated via the old root). On first open the app spent minutes at ~3 cores with the spinner "looking for workbooks": catalog had 159 sessions, 3 workbooks, **0 lap_summary rows**, so it was lap/track-indexing every session (full decode each) on the path to opening a workbook. Bugs filed: (1) the label lies; index work must show as its own progress ("Indexing laps 12/159") in the import chip; (2) lap/track indexing on a catalog rebuild must be incremental per session and never block opening a workbook; (3) the CLI fold-in should do that indexing itself at the end so the app opens ready. Lane after Isaac's pan measurement.
- **R208 (2026-09-11)** — (1) **Use the cores**: per-session work (lap/track indexing, rebuild, fold-in import) runs on a rayon pool sized physical cores − 1, with concurrency also capped by the R203 memory estimate so N parallel decodes never exceed the budget; joins the R207 lane. (2) The shell's graph-column placeholder says "shown via the toolbar's Graph toggle" on every route, but the toggle exists only on the Notebook tab: on other routes the column must hide or say "open the Notebook tab". (3) Properties tab needs proper form components (Isaac): UI-polish lane, queued. (4) Open question under investigation: after selecting a session the app ran ~3 cores for 10+ min with 0 lap_summary rows written and the catalog untouched since 06:56; either one giant transaction or a decode loop on the notebook's host-channel path (R203 follow-up). Baseline for the pan measurement: app private 179 MB at 07:05.
- **R209 (2026-09-11)** — Two UI bugs from Isaac: (1) charts scroll correctly under the notebook toolbar now, but still paint over the shell's top bar: `TopBar` needs the same stacking/background treatment R161's toolbar row got; (2) charts "jump around a little" during a pan and snap back: a layout shift on the gesture path (legend/strip re-measure or scrollbar appearance), to be found with the pan trace, and the rule is no layout-affecting re-render during a gesture. (3) App **crashed loading the 2026-09-07 session** (largest, ~490 MB parquet) with 17.7 GB commit free and no WER event: an abort the R203 failsafe should have caught; console now captured to `idl1-library\app-console.err` for the repro. TS lane for (1)(2); (3) joins the R207/R208 Rust lane once the message is known.
- **R210 (2026-09-11, Isaac)** — Notebook feedback is **per cell, Jupyter-style**: each cell shows its own busy state while its evaluation runs, and results land cell by cell as they settle, never one page-level spinner. Evaluation already settles per definition/window in `channelBindDriver`; the work is a per-cell status (queued / evaluating / settled / error) surfaced in `CellFrame`, with cells rendering independently. Joins the UI lane after R209.
- **R211 (2026-09-11)** — The 9/7 session crash reproduced under capture: `memory allocation of 181132664 bytes failed` (one 22.6 M-sample channel as f64) with 17 GB commit free, right after two rounds of host-channel binds. Cause: the eval host-channel path still decodes the **whole session** per request, requests run concurrently, each passes the per-request failsafe, the sum does not. Rulings: eval/`fetch_host_channel`/`fetch_fft` read through the `SessionCache` per column; the budget becomes a **process-wide byte semaphore** (wait, don't fail; `resource_exhausted` only for a single oversized request); `finish_import` indexes laps without keeping the parsed Session; `try_reserve` everywhere a Vec scales with a session. Brief `runs/2026-09-11/BRIEF-memory-2.md`.
- **R208.2/R209/R210 closed 2026-09-11 (ui-fixes landed)** — top bar stacks over scrolled charts; pan snap-back cause: `ChartCell`'s viewport-commit effect reset gesture state while the pointer was still down (settle at 150 ms + fetch), fixed by pure `commitViewport`; per-cell queued/evaluating/settled/error via `model/cellStatus.ts`; **the Graph toggle was invisible because all three column toggles were bare `<button>`s with no className** (Tailwind preflight = unstyled text), now a labelled segmented group; Graph OFF collapses the maths column (R161's "toggles never remove a studio column" superseded for maths). Gates 198 files / 2014. Ratified all four self-rulings. Note for retirement: `rmdir` on the junction worked where `Remove-Item` would prompt.
- **R212 (2026-09-11, Isaac's first look at the maths graph)** — Notebook density pass: one density scale (22 px controls, 11 px labels) across graph, toolbar and properties; **infinite canvas** (pan/zoom unbounded, Fit/Reset/minimap-with-viewport); **auto layered layout** on first open + Tidy, positions per machine, never in the workbook file; **toolbar one row, 32 px, groups collapse whole into a "⋯" menu**, window chip and transport never collapse, scroll strip deleted; **Properties form gets real controls** (scrub-able unit inputs, compact select, swatches, switches). Brief `runs/2026-09-11/BRIEF-notebook-density.md`; TS-only lane, runs alongside memory-2.
- **R212.3 amended** — Graph node positions live **in the workbook** per C2 §3.7 (`graph: nodes/cells` front matter, R135), schematic semantics as Isaac described; auto-layout only for nodes without a stored position; no per-machine store. My earlier "renderer state" clause contradicted the spec and is withdrawn.
- **R213** — **Layout presets, not a tiling manager.** The shell gains named arrangements over the existing `ColumnFrame`: *Output* (notebook output full-width, everything else collapsed), *Maths* (graph + properties + cells, output narrow), *Split* (today's studio), and a vertical variant where the graph column becomes a **row** above the output for 16:9 screens. The active preset is remembered **per aspect class** (ultrawide ≥ 2.1, wide 16:9, narrow) so the same machine on a different monitor recalls its own arrangement; one keyboard shortcut cycles presets, and Paper/Studio stays orthogonal. A drag-anywhere tiling window manager is out: three presets and an orientation cover the two monitors Isaac has, and the mechanism (column visibility + a row orientation on `ColumnFrame`) already exists. Lane after density lands.
- **R211 closed 2026-09-11 (memory-2 landed)** — every sample path reads columns through the `SessionCache`; `load_session`/`read_session_parquet` have **zero** non-test callers (grep guard with an empty allowlist); process-wide decode semaphore (in-flight decodes counted, residency left to the LRU under the same figure, ratified); `get_session` taken off whole-session decode; `try_reserve` everywhere. Gates core 1369, cli 61, tauri 395. **Import peak 3.7 GB, R211.3's < 2 GB not met and not reachable by the prescribed change**: the peak is the parquet write (parsed session ~2 GB + union axis + a full Arrow column + all row-group writers open + the whole output buffered in a `Vec<u8>`). Follow-up filed: row-group-major writer streaming to the temp file. Fold-in and a 400 MB session import stay ~3.7 GB until then.
- **R212 closed 2026-09-11 (density landed)** — density scale; one-row 32 px toolbar (labels drop ~1240 px; groups fold right-to-left into "⋯" at ~1190/660/555/310 px; session chip + transport never fold; transport moved from TopBar into the toolbar; notices in a strip below); infinite canvas with Fit/Reset/minimap-viewport; layered auto-layout + Tidy; properties controls (reviewer caught the swatch narrowing stroke to eight tokens; free text restored). Ratified self-rulings incl. a sixth `document` toolbar group and `ColourPicker` promoted to `components/ui/`. **Positions travel with the file** per C2 §3.7 (`dragCommit.ts` already implemented the writer; `commitTidy` writes once per gesture, preserves orphans); per-machine store deleted. Gates 203 files / 2065. Process deviation: the correction was started as a branch in the MAIN checkout, breaking the launcher's pull; rule added to owner prompts: lanes never create branches in the main checkout.
- **ResizeObserver loop fixed 2026-09-11** — the toolbar's row-width observer set state synchronously (the density lane's `useRowWidth`); now rAF-deferred like ChartCell/JsCellFrame; TimelineStrip audited clean. `GlobalErrorBanner` swallows the two benign RO messages via `isBenignWindowError`. Gates 204 files / 2070. Sonnet owner, transcription-level brief: clean in one pass (A/B data point 1).
- **R214 (2026-09-11, Isaac's graph feedback)** — Node kind by **shape + glyph** (source: square edge + waveform glyph + mono; derived: rounded + ƒ; chart: type icon + bottom rule), colour stripe as an **optional Settings toggle**; subgraph fallback name **"Cell N"** by document order (the hex id was C2 §3.7.3's fallback), double-click renames by writing `# label:`; **the code column shows the whole workbook** with per-cell folding and two-way selection sync (node/chart/cell → scroll + gutter highlight; caret → selection). Chart types: survey dispatched (`CHART-TYPES-SURVEY.md`) before a port lane. Brief `runs/2026-09-11/BRIEF-graph-semantics.md`.
- **R215 (2026-09-11)** — Chart types (`CHART-TYPES-SURVEY.md`): idl0 had 9, idl1 ports 1 (time series, minus options). **Tier A now** (engine already serves them): FFT picker entry, histogram, g-g scatter, lap variance trace, and the dropped time-series options (lap-pair overlay, zero line, signed scales); distance-on-X stays disabled per R136. **Tier B needs design** (spec-first, lead): spectrogram as a chart beyond the raster underlay (R158), gpsMap, lapTable, lapProgression, none of which the v3 grammar can express. Brief `runs/2026-09-11/BRIEF-chart-port-a.md`; dispatch after graph-semantics merges (same files).
