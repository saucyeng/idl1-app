# L6 Task 13b review — bind js cells to the sandbox (R66, R69) + Task 14 review minors

Worktree: `C:\Users\isaac\Documents\Saucy\saucyeng\idl1-app-worktrees\wave2-l6-notebook`,
branch `wave2-l6-notebook`. Commits under review:

- `0ccf160` — sandbox-side rendering, `spanError`, js-cell binding logic (R66, R69)
- `5706b80` — session-span/binding-identity drivers, `openEvalDriver` staleness test
- `ef34b73` — bind js cells to `ChartCell`/`JsCellFrame` gesture frames (R66, R69)
- `6223a66` — CHANGELOG bullet for Task 13b
- `c4bca62` — Task 14 review minors (`ConflictBanner` confirm, `toIpcErrorOrUnknown`) + R70 seam note

Files touched (union, via `git show --stat` on each commit): `CHANGELOG.md`;
`app/src/routes/pages/Notebook/host/{SandboxHost.ts,protocol.ts,protocol.test.ts}`;
`app/src/routes/pages/Notebook/sandbox/main.ts`;
`app/src/routes/pages/Notebook/model/{jsCellBinding.ts,jsCellBinding.test.ts,cellLayout.ts,cellLayout.test.ts,sessionSpan.ts,sessionSpan.test.ts,sessionSpanDriver.ts,sessionSpanDriver.test.ts,openEvalDriver.test.ts,saveFlow.ts,saveFlow.test.ts}`;
`app/src/routes/pages/Notebook/components/{CellList.tsx,ChartCell.tsx,JsCellFrame.tsx,ProseSpan.tsx,ConflictBanner.tsx}`;
`app/src/routes/pages/Notebook/index.tsx`. All under lane ownership
(`app/src/routes/pages/Notebook/**` plus `CHANGELOG.md`) — no `package.json`,
`docs/`, `rust/`, `App.tsx`, or `AppState.tsx` touch in any of the five commits.

Out of scope, present but ignored: an uncommitted working-tree change to the
`rust` submodule pointer (`git status --porcelain` shows `M rust`) — not part
of any reviewed commit, not staged, left untouched.

## Gate command and result

```
cd app
npx tsc --noEmit
npx vitest run src/routes/pages/Notebook
```
`tsc` — silent, no errors.
`vitest` —
```
Test Files  29 passed (29)
     Tests  239 passed (239)
```
0 failed. Reproduces the implementer's reported "29 files / 239 passed"
exactly. `npx vite build` was not re-run (not required by this dispatch's
gate; the implementer separately reported it clean at 811 modules).

## Findings

| Severity | file:line | Finding | Fix |
|---|---|---|---|
| Critical | `Notebook/index.tsx:433` and `:521` (`const channel = binding.channels[0];`); `CHANGELOG.md:9` | Lead pre-ruling #3 (`brief-task13b.md`) requires "bind every distinct channel" for a multi-channel `js` cell, mounting `ChartCell` for the first only. The landed code does neither the binding effect (`index.tsx:414-470`) nor `onViewportSettled` (`index.tsx:~570-590`) ever calls `setChannelHostVar`/`setBoundChannel` for any channel beyond `binding.channels[0]` — the other distinct channels in `binding.channels` are read out of `bindingFor`'s result and then silently discarded. `sandbox/main.ts`'s `channelLookup(name)` (line ~249) returns `[]` for a name never sent, not an error, so a two-mark cell on two channels (exactly the scenario `jsCellBinding.test.ts`'s own `twoDistinctChannelsCode` fixture constructs) would render its second mark as an empty series with no visible error — silently wrong output for a case this task's own tests exercise the binding logic for but the wiring never completes. The `CHANGELOG.md` bullet compounds this: it states "Multi-channel cells bind every distinct channel but render only the first through `ChartCell`" and cites a `// TODO(idl0)` — no such TODO exists at either `channels[0]` call site (the only TODOs nearby are for `lapContext` and lap-window narrowing), so the changelog claims a behavior that was not implemented. | In both call sites, loop over `binding.channels` and call `setChannelHostVar(c.channelId, ...)` (with a fetch per channel) for every entry, not just index 0; `NotebookSession.setBoundChannel` is keyed one-per-`cellId` today, so only `channels[0]` can be registered for rebuild-repriming — add a `// TODO(idl0)` at the loop citing this brief and the registry's one-binding-per-cell shape as the reason the others aren't restored after a sandbox rebuild. Correct the CHANGELOG bullet to match whatever is actually shipped. |
| Major | `Notebook/index.tsx:414-470` (the channel-bind effect's `ensureTiles(...).then(...)`) | This effect's dependency array is data-only (`[state.cells, state.markdown, sessionDetail, sessionSpanUs, sessionId]`) per the tightened IPC-effects rule, and correctly gates on `bindingIdentity` before starting a fetch — but the async continuation inside `.then()` has no staleness check before calling `sandboxHostRef.current?.setChannelHostVar(...)`, `sessionRef.current.setBoundChannel(cellId, ...)`, and `setChartWindows(...)`. If a cell's binding changes again (a fast edit re-selecting a different channel, or a session change) before the first fetch resolves, `boundIdentityRef.current` is updated to the new identity and a second fetch starts, but the *first* fetch's `.then()` still fires later and unconditionally overwrites `chartWindows`/the `NotebookSession` registry entry for that same `cellId` with the stale (old-channel) data — exactly the "stale result ⇒ dropped" case the operating brief's tightened rule requires a pure guard for, and no such guard exists here (contrast with `openEvalDriver.ts`/`sessionSpanDriver.ts`'s `isStale()` checks after every `await`, which this effect's own inline logic does not mirror). | Capture the `identity` value at fetch-start and, inside `.then()`, check `boundIdentityRef.current.get(cellId) === identity` before writing any of the three pieces of state; drop the stale write otherwise. |
| Minor | `Notebook/model/saveFlow.test.ts` (`toIpcErrorOrUnknown` describe block) | The dispatch asked whether `toIpcErrorOrUnknown` is tested for non-object, string, `Error`, and `IpcError` shapes. Only two of the four are exercised (a well-formed `IpcError` pass-through, and a bare-string throw); a plain `Error` instance (`reason instanceof Error` branch) and a non-string primitive (e.g. `undefined`/a thrown number) have no test, even though the implementation's branches visibly cover them correctly on inspection. | Add two cases: `toIpcErrorOrUnknown(new Error("disk full"))` → `{ kind: "unknown", message: "disk full" }`, and `toIpcErrorOrUnknown(undefined)` → `{ kind: "unknown", message: "undefined" }`. |
| Note | `Notebook/components/ConflictBanner.tsx:22-27` | The Task 14 review-minors fix added a `window.confirm` before "Reload from disk" fires. Neither the design doc nor CLAUDE.md forbids a native confirm dialog, and the code's own doc comment states this is a deliberate coarse stand-in pending L11's real merge UI — consistent with the banner's existing framing. No design-doc or brief text is silent-but-forbidding here, so this is a Note, not a Major, per the dispatch's own instruction. | None required; worth revisiting when L11's per-cell merge UI replaces this banner. |

No other Critical/Important findings.

## Checks performed (all pass)

- **R69 (a) — outputs render inside the sandbox iframe, never injected into the host.** `dangerouslySetInnerHTML`/`innerHTML` grepped across `Notebook/**`: zero live call sites (two doc-comment mentions in `sandbox/main.ts` explaining the removal). `cellResult.html` is gone from `SandboxToHostMessage`; `cellRendered { cellId, heightPx }` replaces it end to end (`protocol.ts`, `SandboxHost.ts`, `sandbox/main.ts`'s `CellContainers`/`makeObserver.fulfilled`). `renderCellValue` in the sandbox appends a DOM node directly or sets `textContent` (never `innerHTML`), so even a string value can't be interpreted as markup inside the already-isolated realm.
- **R69 (b) — `transform` is `postMessage`, never Tauri IPC.** `SandboxHost.sendTransform`/`sendLayout` both call `this.postToSandbox(...)`, which posts to `this.iframe.contentWindow` — no `invoke` import anywhere in `host/`, `components/ChartCell.tsx`, or `components/JsCellFrame.tsx`. `ChartCell`'s `handlePointerMove`/`handleWheel` call `sendTransform` on every live drag/wheel frame (matching design §6's "during a pinch/scroll the existing picture scales via a canvas/CSS transform"); no `fetchTile`/`invoke` call is reachable from either handler — confirmed by reading both callbacks in full.
- **Sandbox origin stays opaque.** `iframe.sandbox.add("allow-scripts")` only, in both the original `createIframe` and this task's untouched surrounding code; no `allow-same-origin` added anywhere in the diff.
- **postMessage validation both directions.** Host: `onMessage` checks `event.source !== this.iframe.contentWindow || !isHostMessage(event.data)` before touching any field (`SandboxHost.ts:108`) — `isHostMessage`'s new `spanError`/`cellRendered` cases both type-check every field (`protocol.ts`, tested in `protocol.test.ts`'s new "missing spanId"/"missing message" rejection cases). Sandbox: `window.addEventListener("message", ...)` checks `event.source !== window.parent` before dispatch (`sandbox/main.ts:365`) — no payload-shape check on this side, which is the documented, intentional asymmetry (host is a trusted, self-authored party; the sandbox is the untrusted one from the host's perspective) and matches the dispatch's own description of what's required.
- **Nothing from the sandbox reaches an `invoke` argument.** `onCellRendered`/`onCellError`/`onInlineResult`/`onSpanError` only ever call `setState` setters (`setCellHeights`/`setCellErrors`/`setInlineResults`/`setSpanErrors`); grepped for any `invoke(` call reachable from those four handlers — none.
- **Protocol shapes.** `transform { cellId, translateXPx, scaleX }` and `cellRendered { cellId, heightPx }` match R69/the brief exactly; `spanError` is a distinct message from `cellError` end to end (`protocol.ts`'s union, `isHostMessage`'s switch, `SandboxHost`'s `onMessage` case, `sandbox/main.ts`'s `evalInline` catch block, `index.tsx`'s `onSpanError` handler, `ProseSpan.tsx`'s `spanErrors` prop) — `cellError`'s case and callback are otherwise untouched and now only ever carry a real fence-string cell id.
- **`OutboundQueue`/generation-tagged flush on `ready`.** Untouched by this diff; `sendTransform`/`sendLayout`/`setChannelHostVar` all still route through `postToSandbox`, which is generation/`ready`-gated the same as before.
- **Rebuild order (`init` → JSON host vars → channels → `setCells`).** `rebuild()`'s doc comment and call sequence are untouched by this diff; `NotebookSession.test.ts`'s existing rebuild test (unchanged, not touched by any of these five commits) already seeds two populated `BoundChannel` entries (`cell-a`/`cell-b`) before asserting `sandbox.calls` — it is **not** vacuous against an empty map, so no strengthening was needed here (the brief's "if it does but seeds an empty registry, strengthen it" branch did not apply; this satisfies the brief's "state which" requirement even though the implementer's own report text wasn't available to this review — the code confirms it directly).
- **Tightened IPC-effects rule, effect by effect** (`index.tsx`): the `SandboxHost` mount effect (`[]`), the open/eval effect (`[sessionId]`), the new session-span effect (`[sessionId]`), the file-watcher effect (`[state.handle?.id]`), and the cell-set/inline-span effect (`[state.cells, state.markdown]`) are all data-only dependency arrays with every callback read through a ref (`onCellRenderedRef`/`onCellErrorRef`/`onInlineResultRef`/`onSpanErrorRef`) and no cancelling cleanup. `sessionSpanDriver.ts`'s `runSessionSpan` and `openEvalDriver.ts`'s (pre-existing) `runOpenAndEval`/`runEval` both check `isStale()` after every `await` and never dispatch past a stale point — confirmed by reading every branch and cross-checking against `sessionSpanDriver.test.ts`'s 8 cases (unrelated-change-mid-flight, stale-after-`listSessions`, stale-after-`getSession`, rejection paths). The one effect that does **not** follow this shape all the way through is the channel-bind effect flagged as a Major finding above — its dependency array is correct, but its `.then()` body lacks the same staleness guard the driver-based effects have.
- **`ChartCell`'s new layout/transform-identity effects** (`frameRef` layout effect, `[]` deps; the `viewport`-reset effect, `[viewport]` deps) read `cellId`/`sendTransform`/`sendLayout`/`viewport` through `layoutDepsRef`/`transformDepsRef`, reassigned every render — no function prop in either effect's dependency array, and the layout effect's cleanup only removes `ResizeObserver`/`resize`/`scroll` listeners (not a cancel of in-flight work). `JsCellFrame`'s identical layout effect follows the same shape.
- **Multi-channel binding identity** (`bindingIdentity`) is keyed off `channels[0]` plus the initial span, which is consistent with mounting only the first channel — confirmed correct for its stated purpose (detecting "should the mounted channel re-fetch"), separate from the binding-completeness gap in the Critical finding above.
- **`setBoundChannel` call sites.** Exactly two: the initial-bind effect's `.then()` (`index.tsx:~455`) and `ChartCell`'s `onViewportSettled` closure built in `renderJsCell` (`index.tsx:~570`) — never from a render body, matching the "Do not" list's ban on calling it from JSX.
- **No `invoke` in Tasks 13b's new/changed files** outside the pre-existing `ipc/*` wrappers imported into `index.tsx` (`getSession`, `listSessions`, `fetchTile`, `cursorReadout`) — all IPC calls happen inside driver `.then()`/injected-dependency bodies, never inside a gesture handler.
- **Ownership/hygiene.** `git show --stat` on all five commits touches only `app/src/routes/pages/Notebook/**` and `CHANGELOG.md`; no `package.json`/lockfile touch (no new dependency); no `docs/` touch; no cargo invocation anywhere in this review; each commit message is a single line with no AI attribution trailer; `git status --porcelain` shows only the unrelated, out-of-scope `rust` submodule pointer change, not a tracked-file mutation from this review.
- **Doc comments and units.** Every exported symbol added across the five commits carries a doc comment; numeric values are annotated with units (`heightPx`/`translateXPx`/`top`/`left`/`width` in CSS px, `startUs`/`endUs` in µs, `sampleRateHz` in Hz); every `TODO` found is `// TODO(idl0):`-prefixed and cites either this brief or a specific IPC-need number — except for the missing TODO at the multi-channel gap noted in the Critical finding.
- **No `any`.** Grepped all five diffs for `: any`, `<any>`, `as any` — zero hits.
- **Purity.** `model/jsCellBinding.ts`, `model/cellLayout.ts`, `model/sessionSpan.ts`, `model/sessionSpanDriver.ts` import no `react`, `@tauri-apps/api`, or DOM global; `sessionSpanDriver.ts` takes `getSession`/`listSessions`/`fetchTile` as injected `SessionSpanDeps`, never importing `ipc/*` directly.
- **`toIpcErrorOrUnknown`'s `kind: "unknown"`.** `IpcError.kind` is typed `string` in `ipc/workbook.ts` (not a closed union), so `"unknown"` is a valid literal, not a type violation.
- **Task 14 review-minors (`c4bca62`).** `ConflictBanner`'s new copy and `title` attribute make the discard-on-reload consequence visible per `review-task14.md`'s Minor; `saveFlow.ts`'s rejection path now goes through `toIpcErrorOrUnknown` instead of an unguarded `as IpcError` cast, closing the `undefined`-message edge case for the common non-`IpcError` throw shapes it does test.
- **R70 seam note (`c4bca62`).** `ProseSpan.tsx`'s doc-comment-only addition accurately describes the interim-scanner seam per `decisions.md`'s R70 — no behavior change, matches the CHANGELOG bullet for it.

## Verdict rationale

The security-boundary work (R69) is done correctly and thoroughly: no HTML crosses `postMessage` in either direction, the sandbox stays origin-opaque, both directions validate what a message actually came from, and the new `spanError` message cleanly replaces the old `cellError` reuse end to end. The tightened IPC-effects rule is followed in every effect except one, and that one exception is a real, if narrow-window, bug (Major) rather than the self-cancelling-effect pattern the rule was written to catch on sight. The most serious problem is the Critical finding: the lead's own pre-ruling #3 ("bind every distinct channel") was only half-implemented — mounting only the first channel is correctly scoped and documented, but the code never even sends the other channels' data to the sandbox, which is a silently-wrong-output bug for a scenario this same task's own test fixtures construct, and the CHANGELOG bullet describes the ruling as fully honored when it isn't. Both the Critical and Major findings have small, mechanical fixes (a loop instead of an index-0 read; a staleness check before three state writes) — this is a fix-up dispatch, not a rework of the approach, which is otherwise sound.

VERDICT: NEEDS_FIXES
