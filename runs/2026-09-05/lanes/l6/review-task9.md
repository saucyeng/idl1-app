# L6 Task 9 review — raster layering, plus Task 8 fix-up review

- Worktree: `C:\Users\isaac\Documents\Saucy\saucyeng\idl1-app-worktrees\wave2-l6-notebook`, branch `wave2-l6-notebook`.
- Commits under review: `b98080d` ("app: Rust raster underlay (spectrogram, 2-D histogram) beneath Plot axes") and `617e785` ("app: fix transformFor zoom-anchor bug and add settle stale-response guard"), with merge `9ea89f4` in between (CHANGELOG-only, not separately reviewed — it does not touch any file scoped here beyond a bullet append).
- Out of scope, present but untouched by these commits: at review time the shared worktree had uncommitted changes from a concurrent Task 10 implementer (`ChartCell.tsx` modified in the working tree, new untracked `CursorReadout.tsx`/`model/cursor.ts`/`model/cursor.test.ts`). These are **not** part of `b98080d`/`617e785` and are ignored below; running the gate directly against the dirty worktree fails `tsc` (`CursorReadoutPanel` unused-import errors from the in-progress Task 10 work), so the gate was instead run against a disposable detached worktree (`git worktree add --detach <throwaway path> 617e785`, later removed with `git worktree remove --force`) to get a clean tree at exactly `617e785` without touching the shared worktree's index or working tree at all.

## Gate command and result

```
cd app && npx tsc --noEmit && npx vitest run --coverage.reporter=json-summary src/routes/pages/Notebook/model
```
Run once, from the disposable worktree at `617e785` (node_modules copied over, not reinstalled). `tsc` was silent. Vitest: `Test Files 9 passed (9)`, `Tests 63 passed (63)`. This reproduces the implementer's reported 63 passed. `coverage/` deleted from the disposable worktree before it was removed; no artifact left in the shared worktree.

## Section 1 — Task 9 (`b98080d`, raster layering)

### Findings

| Severity | file:line | Finding | Fix |
|---|---|---|---|
| Critical | `components/RasterUnderlay.tsx:93-130` (the fetch effect's dependency array and `cancelled` cleanup) | The effect's cleanup (`cancelled = true`) runs on **any** dependency change, and the dependency array includes `fetchRaster`/`fetchRasterMeta` — caller-injected function props with no guaranteed stable identity (`RasterUnderlayProps` documents no memoization contract, and nothing in `ChartCell.tsx` wraps `raster.fetchRaster`/`raster.fetchRasterMeta` in `useCallback` before passing them through). If the eventual page-level caller re-creates these closures on every render (the ordinary case for an inline arrow function bound to `sessionId`/`channelId`), every unrelated re-render of that caller — not just a settled-viewport change — tears down the effect, marks the in-flight fetch `cancelled`, and starts a duplicate one; a fetch that was about to resolve with viewport-correct data is discarded and re-issued for no visible reason. This is exactly the "effect's own cleanup cancels the work it started because it depends on unrelated state" pattern the operating brief's IPC-effects rule names as Critical (the same shape as the cited L7a import-queue incident), and this task's own sibling code in the very same commit's file (`ChartCell.tsx`'s `onSettleRef` ref-indirection, carried over unchanged from Task 8) demonstrates the fix was already known and available: Task 8 explicitly routes `fetchTile`/`onViewportSettled` through a mutable ref specifically "so the debouncer instance ... stays stable across renders instead of being torn down and recreated whenever a parent passes a fresh ... closure identity" (`ChartCell.tsx`, the `onSettleRef` comment) — `RasterUnderlay`'s `useEffect` reintroduces the identical hazard for `fetchRaster`/`fetchRasterMeta` that Task 8 went out of its way to avoid for the structurally identical `fetchTile`. `width`/`height`/`devicePixelRatio`/`sessionId`/`channelId`/`kind`/`params` in the same array are not the same class of problem — each genuinely determines what to fetch, so a change to any of them legitimately invalidates an in-flight request — only the two function props are the unrelated-state hazard. | Capture `fetchRaster`/`fetchRasterMeta` (and the other request-shaping values) in a ref updated on every render, the same pattern `ChartCell.tsx`'s `onSettleRef` already uses, and drop them from the effect's own dependency array — the effect should re-run only when `viewport` (and the other content-determining values: `kind`, `params`, `width`, `height`, `devicePixelRatio`, `sessionId`, `channelId`) changes, never merely because the caller re-rendered with a fresh closure. |
| Important | `components/RasterUnderlay.tsx:132-140` (the `<canvas>` element's `width`/`height` attributes) | `rasterRequestFor` (and this component's own `devicePixelRatio` prop) exist specifically to fetch the raster at **device**-px resolution so a high-DPI display gets a crisp image (explicitly tested: "a devicePixelRatio of 2 — requests twice the CSS pixel dimensions"). But the destination `<canvas>` here is created with `width={width} height={height}` — the **CSS**-px props, never multiplied by `devicePixelRatio` — and no CSS `width`/`height` style is set to make the element's own box larger than its backing store (only `position: absolute; inset: 0`, which sizes the CSS box to the parent, not the bitmap). The canvas's internal pixel buffer therefore has only CSS-px resolution regardless of `devicePixelRatio`, so `drawRaster`'s `ctx.drawImage` call scales the fetched device-px-resolution source down into a 1×-density destination — defeating the entire point of the device-px fetch and producing a blurrier-than-fetched result on any `devicePixelRatio !== 1` display. This degrades visual fidelity silently; it does not corrupt data or crash, so Important rather than Critical. | Set the canvas element's `width`/`height` attributes to the device-px size (`width * devicePixelRatio`, `height * devicePixelRatio`, i.e. the same values `rasterRequestFor` computes) and add matching CSS `width`/`height: ${width}px`/`${height}px` (or `100%`, given the parent already sizes it) so the CSS box stays at the CSS-px size while the backing store is device-px resolution — the standard high-DPI-canvas pattern. |
| Minor | `model/rasterLayer.ts:149-184` (`alignRasterToAxes` signature) | The function takes `rasterWidthPx`/`rasterHeightPx`/`heightCssPx` as separate parameters beyond the plan's Task 9 pseudocode signature (`alignRasterToAxes(meta, viewport)`). This is disclosed explicitly in the doc comment and in the implementer's own report as a necessary, flagged deviation (a source rectangle in raster-pixel units cannot be computed without the raster's own pixel dimensions, which `RasterMeta` doesn't carry) — a real and correctly-reasoned necessity, not an oversight. Noting per the standing brief's instruction to record even a merely-matched pre-existing gap; this one is actively justified rather than merely inherited, which is mitigating. | None required; the deviation is sound and disclosed. |

No other Critical or Important findings.

### Checks performed (all pass)

- **`rasterRequestFor`.** All 4 brief-specified tests present, named verbatim, A/A/A with blank lines. Hand-verified: `histogram2d` sets `x_bins`/`y_bins` to the request's own (clamped) `width`/`height`, never the caller's input values (R42) — confirmed by reading `rasterLayer.ts:81-82`, which spreads `params` then unconditionally overwrites `x_bins`/`y_bins`. u16 clamp (`clampDevicePx`, `[1, 65535]`) and `clamped` flag correctly computed by comparing against the raw (unrounded, unclamped) value, not the rounded-then-compared value — avoids a false "clamped" report on values that only round, not clamp (re-derived by hand: `70_000 * 1` clamps to `65535`, `clamped: true`, matches the test). Spectrogram params pass through byte-identically (`toEqual(params)`), confirmed by reading — no field is touched for `kind !== "histogram2d"`. `devicePixelRatio = 2` test: `800 * 2 = 1600`, `400 * 2 = 800`, matches.
- **`alignRasterToAxes`.** Both brief-specified tests present, named verbatim, A/A/A. Hand-verified the non-null case: raster domain `[0s, 20s]` → µs `[0, 20_000_000]`; viewport `[10_000_000, 20_000_000]`; overlap is exactly the viewport's own window; `usPerRasterPx = 20_000_000 / 800 = 25_000`; `srcX = (10_000_000 - 0) / 25_000 = 400`; `srcWidth = (20_000_000 - 10_000_000) / 25_000 = 400`; `usPerViewportPx = 10_000_000 / 800 = 12_500`; `destX = 0`; `destWidth = 800` — matches the test's expected `{srcX: 400, srcWidth: 400, destX: 0, destWidth: 800}` exactly. Null case: raster `[0s,5s]` vs. viewport `[10s,20s]` → `overlapStartUs (10e6) >= overlapEndUs (5e6)` → `null`, correct. The seconds→µs conversion is isolated to one named `SECONDS_TO_US = 1_000_000` local constant applied exactly once at the top of the function, with a load-bearing doc-comment paragraph calling out the unit mismatch explicitly — matches the brief's explicit ask that a silent factor-of-1e6 bug be spottable by reading one function.
- **`drawRaster`.** Not unit-tested, correctly per CLAUDE.md §4 (DOM-touching) and the brief's own instruction; doc comment explains why (jsdom doesn't implement `drawImage`/`putImageData` faithfully) and documents the offscreen-canvas + `drawImage` scaling approach chosen over `createImageBitmap` (sync-signature requirement, stated explicitly) — no legend/axis/Plot mark drawn here, matching the "do not" list; `RasterMeta.scale.vmin`/`vmax` is never read or recomputed in this file (grepped: no reference to `.scale` anywhere in `rasterLayer.ts` or `RasterUnderlay.tsx`), consistent with R38/P8.
- **P1/P3/P4 — no raster fetch reachable from a gesture handler.** `git show b98080d | grep -n "invoke(\|onPointerMove\|onWheel\|onTouchMove\|requestAnimationFrame"` shows only doc-comment prose (no live call site); `fetchRaster`/`fetchRasterMeta` are called only inside `RasterUnderlay`'s `useEffect`, itself gated on the `viewport` prop that `ChartCell` only updates from its settle callback (confirmed: `ChartCell.tsx` passes `viewport={viewport}` — the settled prop — to `RasterUnderlay`, not `liveViewport`, the in-gesture local state).
- **R42 (bins = pixels).** Confirmed above; `Histogram2dParams.x_bins`/`y_bins` on the caller's input `params` are read nowhere in `rasterRequestFor` — only written.
- **R38 (resolution-independent colour bounds).** No file in this commit computes, adjusts, or reads `scale.vmin`/`vmax`/`kind` at all; legend drawing is explicitly out of scope and stated as such.
- **IPC-effects rule / escape hatch applicability.** The effect's *sequencing* logic (compute request → fetch pixels + meta → align → draw) is genuinely trivial with no independent decision beyond React's own effect-dependency re-run, so the "no driver module needed" half of the escape hatch is correctly claimed. However, per the rule's own second, independent clause ("the effect ... never cancels an in-flight promise because unrelated state changed"), the escape hatch does **not** waive the self-cancel requirement — see the Critical finding above.
- **Ownership/hygiene.** `git show --stat b98080d` touches only `CHANGELOG.md` plus four files under `Notebook/components`/`Notebook/model` — no `rust/`, `app/src-tauri/`, `docs/`, `package.json`, or lockfile diff. No new npm dependency. Single-line commit message, no AI attribution trailer. CHANGELOG bullet matches the brief's specified wording exactly.
- **Doc comments and units.** Every exported symbol in `rasterLayer.ts` and `RasterUnderlay.tsx` has a doc comment; `width`/`height` documented as device px (`rasterRequestFor`/`RasterRequest`) vs. CSS px (`RasterUnderlayProps.width`/`height`) — the two units are named at each use, not left ambiguous; the seconds↔µs conversion factor and its single application site are stated explicitly per the brief's specific ask. `// TODO(idl0):` used correctly (no bare `// TODO`) in `drawRaster`'s and `RasterUnderlay`'s swallowed-failure comments.

### Verdict rationale

The pure math (`rasterRequestFor`, `alignRasterToAxes`) is correct, exactly matches the brief's six specified tests, and its one unit-conversion hazard is isolated and documented exactly as required. The DOM-touching `drawRaster` is reasonably designed and correctly left untested per CLAUDE.md §4. However, `RasterUnderlay`'s fetch effect has a genuine, reachable self-cancellation hazard: `fetchRaster`/`fetchRasterMeta` sit in the effect's dependency array with no stability contract, so an unrelated caller re-render can tear down and duplicate an in-flight fetch — precisely the pattern the operating brief was written to catch, and one this same commit's own `ChartCell.tsx` code (inherited from Task 8) shows how to avoid via ref-indirection. There is also a real, silent DPI-fidelity bug: the canvas backing store is sized in CSS px, not device px, undermining the entire purpose of the `devicePixelRatio`-aware fetch. Both are small, mechanical fixes (ref-indirection for the effect; correct canvas sizing) — not a rework of the approach, which is otherwise sound.

VERDICT: NEEDS_FIXES (Task 9)

## Section 2 — Task 8 fix-up (`617e785`)

### Findings

No Critical or Important findings.

| Severity | file:line | Finding | Fix |
|---|---|---|---|
| Minor | none | — | — |

### Checks performed (all pass)

- **`transformFor` fix, hand-derived.** Re-derived the composed transform algebraically from `translateX(t) scaleX(s)` semantics (`newPos = s·x + t`) independent of the diff's own derivation comment, and confirmed the new divisor is `currentSpanUs / current.pixelWidth` (current viewport's own µs-per-pixel), not `rendered`'s. Hand-computed both new tests against the fixed code:
  - Right-edge test: `rendered = {0, 1_000_000, 100}`; `zoomAt(rendered, 100, 2)` → `usPerPixel = 10_000`, `anchorUs = 1_000_000`, `newSpanUs = 500_000`, `anchorFraction = 1` → `current = {500_000, 1_000_000, 100}`. `transformFor`: `currentUsPerPixel = 500_000/100 = 5_000`; `scaleX = 1_000_000/500_000 = 2`; `translateXPx = -(500_000 - 0)/5_000 = -100`. Applying to `xOf(current.startUs)=50`: `2·50 − 100 = 0` ✓; `xOf(current.endUs)=100`: `2·100 − 100 = 100 = pixelWidth` ✓ — matches the test's assertions exactly.
  - Mid-anchor test: `zoomAt(rendered, 25, 2)` → `anchorUs = 250_000`, `newSpanUs = 500_000`, `anchorFraction = 0.25` → `current = {125_000, 625_000, 100}`. `translateXPx = -(125_000-0)/5_000 = -25`; `pixelAtAnchor = 2·25 − 25 = 25` ✓.
  - Confirmed by hand that the **old** (pre-fix) divisor (`renderedSpanUs/rendered.pixelWidth = 10_000`) would give `translateXPx = -50` for the right-edge case, producing `pixelAtCurrentStart = 2·50 − 50 = 50 ≠ 0` — i.e. the two new tests would genuinely fail against the pre-fix code, not merely restate the fix.
- **Stale-settle guard, applied not just exported.** `isStaleSettleResult` and `latestSeq()` are both actually wired into `ChartCell.tsx`'s `onSettleRef.current` (not merely present in `settle.ts`): `seqAtDispatch` is captured synchronously at the top of the settle callback (before `ensureTiles` starts), and `isStaleSettleResult(seqAtDispatch, settleRef.current.latestSeq())` gates the `.then()` continuation before any state/tile-cache read is used to call `onViewportSettled` — confirmed by reading the diff hunk directly, not just the exported symbols. Re-derived the semantics: `seq` increments exactly once per firing, immediately before `onSettle` runs, so `seqAtDispatch` always equals the firing's own `seq` at capture time; if a strictly newer settle fires before the async work resolves, `latestSeq()` has since incremented, so `isStaleSettleResult` correctly returns `true` and the result is dropped — and it is never `true` for the actually-latest settle's own result (its captured seq still matches at resolution time, assuming no *even newer* settle interleaves, which is exactly the intended semantics: only a truly superseded settle is dropped).
- **`settle.test.ts` new tests.** Both `latestSeq`/`isStaleSettleResult` tests present, A/A/A structure (the `latestSeq` test is more compressed but its Arrange/Act/Assert are still clearly delineated), assert what they claim: two settle firings via the fake timer produce sequence values `[1, 2]` read from inside `onSettle` itself, and `latestSeq()` afterward reads `2`; the two `isStaleSettleResult` unit tests directly assert the pure comparison's two branches.
- **No new npm dependency, ownership, hygiene.** `git show --stat 617e785` touches only `CHANGELOG.md` and five files under `Notebook/components`/`Notebook/model` — no `package.json`/lockfile diff, nothing outside `Notebook/**`. Single-line commit message, no AI attribution trailer. CHANGELOG bullet accurately describes both fixes and correctly cross-references `review-task8.md`.
- **The previously-Minor coverage gap (`REAL_TIMER` default branch).** Left as-is with an added doc-comment note explaining why it's an accepted gap — matches `review-task8.md`'s own "no action required" disposition; not re-litigated.
- **No regression.** `zoomAt`/`panBy`/`clampTo` untouched by this diff (confirmed via the diff itself — no hunk touches them); `chooseTier`/`tileRange`/`ensureTiles` calls in `onSettleRef.current` are unchanged except for the guard insertion, so the fetch range/tier selection logic is unaffected.

### Verdict rationale

Both `review-task8.md` findings are correctly and completely closed. The `transformFor` fix is algebraically correct (independently re-derived and hand-verified against both new test cases and against the old, buggy divisor to confirm the tests are discriminating, not coincidental), and the stale-settle guard is genuinely wired into the production call path, not merely exported and left unused. No new defect was introduced. This is clean.

VERDICT: CLEAN (Task 8 fix-up)

---

## Overall

VERDICT: FINDINGS (1 Critical, 1 Important, 1 Minor) — all in Task 9 (`b98080d`); the Task 8 fix-up (`617e785`) is CLEAN.
