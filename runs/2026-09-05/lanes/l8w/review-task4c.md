# L8w Task 4c review — prose Markdown → HTML on `CellOutput` (R69 item 4, R70)

**Worktrees / commits in scope:**
- Rust: `C:\Users\isaac\Documents\Saucy\saucyeng\idl-rs-worktrees\wave2-l8w-write-amendment`, branch `wave2-l8w-write-amendment`, commit `c383d4c` — `core/src/workbook/v3/prose.rs` (new), `core/src/workbook/v3/mod.rs`, `tauri/src/commands/workbook.rs`.
- App: `C:\Users\isaac\Documents\Saucy\saucyeng\idl1-app-worktrees\wave2-l8w-write-amendment`, branch `wave2-l8w-write-amendment`, commit `9d01634` — `docs/superpowers/specs/2026-09-03-idl1-c3-ipc-surface.md` only.
- Out of scope: a Task 7 implementer may commit to either worktree concurrently; this review examines only the two named commits' diffs, not the branch tip.

## Test command and result

Per the L8w standing reviewer brief ("Reviewers do not build or test," CLAUDE.md §8 hook-denies a reviewer cargo invocation), **no cargo command was run**. Verification is static: every new `#[test]` fn was traced against the landed `pulldown-cmark` 0.13.4 and `pulldown-cmark-escape` 0.11.0 vendored source and the already-landed `find_inline_exprs`/`CellDoc` types, and the implementer's reported filter match counts were checked by grep against the diff.

- `cargo test -p idl-rs render_prose_html` — implementer reports 4 passed. `core/src/workbook/v3/prose.rs` has exactly 4 `#[test]` fns, all named `render_prose_html_...` (substring match, filter is exact-count-plausible). Traced each for correctness (see Findings/Checks below) — all would compile and all fail if the behaviour they assert broke.
- `cargo test -p idl-rs-tauri eval_workbook_via` — implementer reports 1 passed. `tauri/src/commands/workbook.rs` has exactly one test fn whose name contains `eval_workbook_via` as a substring (`eval_workbook_via_first_cells_prose_before_renders_to_html_matching_render_prose_html_prose_after_none_on_non_last_cell`) — count matches.
- Both `cargo check` runs (idl-rs-cli --tests, idl-rs-tauri) reported clean — not independently reproducible without building; the new `pub fn render_prose_html`/`pub struct RenderedProse`/`ProseSpanRef` and the two new `CellOutput` fields plus `ProseSpan` are the only new `pub` surface, and every `CellOutput { .. }` construction site in the diff (one, in `eval_workbook_via`) is updated with all three new fields — no other construction site exists in the file (`grep -n "CellOutput {" tauri/src/commands/workbook.rs` was checked and returns only this one struct-literal site plus the definition), so the reported clean `cargo check -p idl-rs-tauri` is plausible.

## Findings

| Severity | file:line | Finding | Fix |
|---|---|---|---|
| — | — | No Critical, Important, or Minor findings. | — |

## Checks performed (all pass)

- **File scope.** Rust commit touches exactly the three files the brief names (`core/src/workbook/v3/prose.rs`, `core/src/workbook/v3/mod.rs`, `tauri/src/commands/workbook.rs`); app commit touches only the named C3 doc file. `js_cell.rs` diff against its parent is empty (0 lines) — confirmed untouched. No `app/src/` file in either diff. `Cargo.lock` not present in the commit diff (`git show c383d4c -- Cargo.lock` empty) — no new dependency added, consistent with reusing the already-present `uuid = "1.26.0"` (`features = ["v4"]`, confirmed in `core/Cargo.toml`) and no `pulldown-cmark-escape` addition.
- **Reuses `find_inline_exprs`, no second scanner.** `prose.rs` imports `super::find_inline_exprs` and calls it once (line 1 of the pipeline); no regex or second brace-walk introduced.
- **Sentinel substitution.** Byte ranges from `InlineExpr` are cut out and replaced by `IDL1WBSPAN{nonce}_{i}` where `nonce` is a fresh UUID v4 (32 hex chars) per call, alphanumeric only — no markdown/HTML-special characters, so it can't accidentally form emphasis/HTML syntax and survives the parse+render step unmolested for an exact final string-replace. Collision with user-authored text is not literally impossible (a UUID space is finite) but is a 2^122-order improbability per call — practically foreclosed, and the code comment above the `nonce` line states its purpose (insulating substitution from the prose's own text) even if it doesn't spell out the odds; not a finding.
- **Placeholder IDs, ordering, and document order.** Test 1 (`render_prose_html_heading_bold_and_two_spans_placeholders_in_document_order`) confirms two spans nested inside `**bold ... text**` and standalone both placeholder correctly, `prefix:0` appears strictly before `prefix:1` in the output string (nesting through `<strong>` doesn't disturb byte order), and no literal `${` survives. IDs are `{span_id_prefix}:{i}`, `i` assigned by enumerate over `found` (already document-order per `find_inline_exprs`'s own doc comment: "already sorted in document order — the scan is a single left-to-right byte walk") — stable per (cell, index) as R70 requires, and deterministic across calls (only the internal sentinel nonce is random, never surfaces in the returned `id`/`html`).
- **Raw HTML handling verified against vendored source, not just the brief's suggested code.** The implementer deviated from the brief's exact suggested `escape_html_text` helper (hand-escaping the string before wrapping as `Event::Text`) — instead reclassifying `Event::Html`/`Event::InlineHtml` as `Event::Text(s)` **unescaped**, relying on `push_html`'s own `Text` branch. Read `pulldown-cmark-0.13.4/src/html.rs` directly: `Text(text) => { escape_html_body_text(&mut self.writer, &text)? }` vs. `Html(html) | InlineHtml(html) => { self.write(&html)?; }` — confirms `Text` events are unconditionally escaped and `Html`/`InlineHtml` are not, so reclassifying to `Text` is sufficient and correct. Read `pulldown-cmark-escape-0.11.0/src/lib.rs`'s `escape_html_body_text`/`HTML_BODY_TEXT_ESCAPE_TABLE` machinery — escapes `<`, `>`, `&` (the three brief-specified characters) across the whole text run, so a chunk like `<b onerror="x">` becomes one escaped literal string, not selectively-escaped tag/attribute text — no raw markup, script tag, or attribute reaches the output unescaped. This is a **documented, justified, and verified-correct** deviation from the brief's literal code (the brief's own suggested hand-rolled escaper would have double-escaped, since `push_html` already escapes every `Text` event — the implementer's doc comment on `render_prose_html` explicitly states this was caught by their own raw-HTML test and fixed by dropping the redundant step). Per the brief's own framing ("this is specified, not open" refers to the *outcome* — escape, don't pass through — not a mandate to reject a more-correct mechanism that achieves the same outcome via the library's own established escaping path), this is acceptable, not a finding. Test 2 (`render_prose_html_raw_html_the_author_typed_is_escaped_not_passed_through`) asserts `&lt;b&gt;` present, `<b>` absent, and an HTML comment (`<!--`) becomes `&lt;!--`/`--&gt;` — a real proof, not a "compiles the same" check.
- **No pre-escaping of the whole input.** Confirmed only the `Html`/`InlineHtml` branches are touched in the `.map` closure; every other `Event` variant (including `Text`) passes through unmodified to `push_html`, avoiding the double-escaping bug the brief warned against.
- **Inert `${…}` in code spans.** Test 4 confirms a `${…}` inside backticks is not treated as a placeholder (`spans.len() == 1`, only the live `${real(x)}` counted) and the literal `${not_an_expr}` text (inside its own escaped-by-push_html `<code>` block via `Event::Code`) survives untouched in the HTML — correctly delegates to `find_inline_exprs`'s already-tested inert-range logic rather than re-testing it.
- **`prose_after_html` is `None` except the last cell.** The tauri-level test builds a two-cell workbook (`math` then `js`), puts `prose_before` only on the first (non-last) cell, and asserts `out[0].prose_after_html == None` — directly exercises C2 §2.4's rule, not just an incidental default.
- **C3 §3.4 amendment matches Rust exactly.** `prose_before_html: string | null`, `prose_after_html: string | null`, `prose_spans: ProseSpan[]` with `{ id: string; expr: string }` — field-for-field match to `pub prose_before_html: Option<String>`, `pub prose_after_html: Option<String>`, `pub prose_spans: Vec<ProseSpan>` and `pub struct ProseSpan { pub id: String, pub expr: String }` (serde-derived, snake_case preserved, no rename attributes needed). Post-sign note cites both R69 item 4 and R70 by name and explicitly states the R21-comment-vs-shape gap this task closes — matches the brief's "A pre-existing spec inconsistency you are closing" section.
- **R70's ruling followed precisely.** `prose_spans` is populated as `prose_before`'s spans then `prose_after`'s spans, in that order (`eval_workbook_via`'s two sequential `.extend()` calls) — matches "every ${…} span across prose_before_html then prose_after_html, in document order" in the amended C3 text.
- **Placeholder id convention matches the existing frontend.** `format!("{}-before", cell_doc.id)` / `"-after"` passed as `span_id_prefix`, producing `"{cell_id}-before:{i}"` / `"{cell_id}-after:{i}"` — matches `ProseSpan.tsx`'s existing `extractInlineSpans(text, \`${cell.id}-before\`)` convention the brief specifies, and is exercised in the tauri-level test (`aaaaaaaa-before:0`).
- **Doc comments.** Every new `pub` symbol (`render_prose_html`, `RenderedProse`, `RenderedProse::html`/`spans`, `ProseSpanRef` and its fields, `ProseSpan` and its fields, `CellOutput::prose_before_html`/`prose_after_html`/`prose_spans`) carries a doc comment, each citing the relevant ruling (R69/R70) and C2/C3 section. The module doc comment on `prose.rs` explains the security rationale (LAN-sync-sourced `.idl1wb`, sandbox iframe boundary) matching R69.
- **CLAUDE.md §4 test style.** All 5 new tests (4 core + 1 tauri) use blank-line-separated Arrange/Act/Assert and `thing — condition — result`-shaped names (using `_` word-separation, the established idiom in this file, matching `cell.rs`/`js_cell.rs` precedent already in the codebase).
- **CLAUDE.md §5.** No `Err(String)`, no unexplained `.unwrap()`/`.expect()` on production-path data (the `.unwrap()` calls in the test bodies are on `Result`s from the test's own controlled fixtures, consistent with existing precedent in this file). `// TODO` not used at all in the diff.
- **Hand style / no reformatting.** Diff is additive-only in `mod.rs` (2 lines) and `workbook.rs` (new struct, new fields, new branch logic, one new test) — no churn to surrounding lines. `prose.rs` matches `js_cell.rs`'s style (module doc comment, `#[derive(Debug, Clone, PartialEq)]` structs, `#[cfg(test)] mod tests` block) by hand, no `cargo fmt` artifacts (no reflowed long lines, matches this file's existing ~100-char-ish wrapping elsewhere in the crate).
- **Repo hygiene.** Both commits are single-line messages, no AI attribution trailer, `git show --stat` file lists match the task's named files exactly.
- **Additive-only.** No existing field, struct, or function signature was changed or removed; `CellOutput`'s three pre-existing fields (`cell_id`, `kind`, `value`, `defs`, `errors`) are untouched in both position and type.

## Verdict rationale

The implementation follows R70's ruling precisely (two `Option<String>` fields plus `prose_spans: Vec<ProseSpan>`, populated before-then-after in document order, ids stable per cell/index), reuses `find_inline_exprs` as the sole scanner, and closes the R21-comment-vs-shape gap with an honest post-sign note. The one place the code diverges from the brief's literal suggested implementation — reclassifying raw HTML events to `Event::Text` unescaped rather than hand-escaping them first — was checked directly against the vendored `pulldown-cmark`/`pulldown-cmark-escape` source and is provably correct and in fact avoids a double-escaping bug the brief's own code would have introduced; the implementer documented the reasoning and the test that caught it, satisfying CLAUDE.md §1's "document your call" bar for a deviation the brief itself left open ("this is specified, not open" specifies the *escape, don't pass through* outcome, which this achieves). Tests are real proofs (nesting/ordering, raw-HTML escaping, no-span no-op, inert-code-span), not "compiles the same as before" checks. C3 §3.4 matches the Rust types field-for-field with correct rulings cited. Scope, hygiene, and doc-comment requirements are all met with no deviations worth flagging.

VERDICT: CLEAN
